---
milestone: v1.1.0
name: Plugin Ecosystem
status: passed
audited: 2026-08-18
phases: [11, 12, 13, 14]
requirements_covered: 11/11
blockers: 0
warnings: 1
---

# v1.1.0 Milestone Audit — Plugin Ecosystem

**Verdict: PASSED.** All four phases individually verified (`passed` in each
VERIFICATION.md), all cross-phase seams wired, all 11 milestone requirements
covered, E2E suite 145/145 at zero retries, phase-14 security audit closed
26/26 threats.

## Definition of Done

"Open topos to third-party plugins behind an explicit trust boundary, prove
the external path by building a new source out-of-repo, and give users finer
control over what lands in a webspace." — achieved:

- Trust boundary: external tier + provenance-derived trust + pins + build
  manifest gate + untrusted warnings/badges (Phases 11, 13)
- External path proven: topos-plugin-gdrive built in a clean-room sibling
  repo against the published contract alone, installed through the untrusted
  path, live-UAT'd; 20 contract gaps triaged back into the published contract
  (Phase 14)
- Finer control: per-item include/exclude with undo, excluded view, marks as
  user-owned index data (Phase 13)

## Phase Verifications

| Phase | Status | Notes |
|-------|--------|-------|
| 11 external-plugins-the-trust-boundary | passed | 2 documented non-blocking warnings carried (stale config comment; TrustUpdateDialog empty-pin fallback, currently unreachable) |
| 12 filesystem-source | passed | 4th pass after gap-closure chain; real-NFS/SMB backstop truths remain operator-desktop-only by design |
| 13 per-item-curation-installable-app | passed | WR-01 UI-state clobber (ms-window, UI-only) open with proposed fix in 13-REVIEW.md — follow-up quick task recommended |
| 14 google-drive-source-built-out-of-repo | passed | Re-verified after G-14-2 gap closure; security audit 26/26 closed, threats_open 0 |

## Cross-Phase Integration (gsd-integration-checker, 2026-08-18)

All five seams WIRED with spec-level evidence:

1. Phase 11 external-tier machinery carries Phase 14's out-of-repo binary
   (external-only load, tier=external, badge, untrusted clause) —
   14-gdrive-external-rehearsal.spec.ts.
2. Phase 11 + Phase 12: filesystem plugin proven on the external tier —
   12-external-rehearsal.spec.ts.
3. Phase 13 trust hardening composes with Phase 11 tiers (manifest gate,
   shadowing advisory) — 13-manifest-unverified / 13-shadowed-advisory.
4. PLUG-09 extras passthrough is exactly what the gdrive plugin's provider
   config uses (client_id/client_secret secret:true, folder_id) — spec case 3
   + docs/plugin-contract.md.
5. Full E2E flow: add-with-warning → badge → sync → per-item exclude →
   health states incl. 375px reachability (post-14-06).

Requirements map: PLUG-06/07/08/09, SRC-04/05/06, KERN-09/10, UI-13/14 — all
WIRED (11/11).

## Warnings (non-blocking)

1. **Curation not explicitly e2e-tested on external-tier items.**
   Architecturally guaranteed (kernel/index/marks.go is source-blind; stream
   filtering has no tier condition) and exclude/include is spec-proven on
   trusted items. Recommended: a future spec excluding a Drive item, useful
   for third-party plugin-author documentation. Candidate to fold into
   backlog 999.1/999.2 work.

## Info

- Phase 14's hermetic spec runs credential-less by design (not-authorized
  health path); pin re-verification is guaranteed at kernel launch and
  separately spec-proven (11-binary-changed-repin, 12-external-rehearsal).

## Carried Debt (tracked, not blocking)

- Pending todos: kernel OAuth/secrets abstraction (requirement-grade,
  feeds next milestone), dev/test port 7777 clash, popover-clone tooltip
  click interception (latent, pre-existing), Signal schema tooling.
- Phase 13 WR-01 UI-state clobber (fix proposed in 13-REVIEW.md).
- Phase 14 advisory: Makefile gdrive-external-rehearsal recipe duplicates
  the e2e fixture-build recipe (14-REVIEW.md WR-02).
