---
phase: 13-per-item-curation-installable-app
plan: 06
type: execute
wave: 4
depends_on: [13-03, 13-05]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/format.ts
  - web/src/lib/components/SourceChip.svelte
  - web/src/lib/components/match-advisory.test.ts
  - web/src/lib/components/source-chip-tooltip.test.ts
  - web/e2e/fixtures/plugin-binaries.ts
  - web/e2e/fixtures/config-builder.ts
  - web/e2e/specs/13-manifest-unverified.spec.ts
  - web/e2e/specs/13-shadowed-advisory.spec.ts
  - docs/plugin-contract.md
  - docs/plugins/signal.md
  - docs/api.md
  - docs/testing.md
autonomous: true
requirements: [PLUG-07]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A source whose trusted-directory binary failed manifest verification shows a destructive-tone health dot and a tooltip reading `{display_name} — binary not in the trusted build manifest` (D-13, UI-SPEC E6)."
    - "A source whose pinned external plugin is shadowed by a same-named trusted-directory binary shows a warning-tone health dot — never destructive — and a tooltip reading `{display_name} — a same-named trusted-directory binary is shadowing this pinned plugin` (D-14, UI-SPEC E6)."
    - "Both new states are new inputs to the existing healthTone / isAdvisoryOnly precedence chain, not a parallel gate, so the dot's tone and the tooltip's copy cannot drift apart (Phase 12 CR-01 discipline, UI-SPEC E6)."
    - "Manifest verification produces no visible checking state: a source that fails it surfaces already in the failed state, exactly like the shipped pin-mismatch state (UI-SPEC E6 loading)."
    - "Several sources can carry either state at once; each chip renders its own state independently, exactly as the pin-mismatch state already scales (UI-SPEC E6 zero-one-many)."
    - "A long display name in either tooltip wraps through the same tooltip primitive every other chip tooltip uses, with no fixed-width truncation (UI-SPEC E6 long-text)."
    - "The manifest-unverified state adds no new dialog and no new remedial action to the chip menu — the existing external-tier consent and pin flow is the documented and only path to running such a binary (D-13, UI-SPEC E6)."
    - "The re-pin remedial action stays gated on the pin-mismatch value alone and is not extended to the manifest-unverified value."
    - "The published plugin contract describes trust as deriving from the build-provenance manifest, names the refuse-to-load behaviour, and states plainly that hashing here is an integrity control and not publisher authentication."
    - "The Signal plugin's documentation explains why a locally-built Signal binary refuses to load against a release kernel and walks the user through the consent-and-pin path that makes it run (D-15)."
    - "The two plugin directories are documented as install conveniences, with the manifest named as the trust authority (D-16)."
  artifacts:
    - web/src/lib/format.ts
    - web/src/lib/components/SourceChip.svelte
    - web/e2e/specs/13-manifest-unverified.spec.ts
    - web/e2e/specs/13-shadowed-advisory.spec.ts
    - docs/plugin-contract.md
    - docs/plugins/signal.md
  key_links:
    - "sourceStatus.launch_advisory -> api.ts SourceStatus -> format.ts healthTone/isAdvisoryOnly -> SourceChip dot tone and tooltip (the one chain every health-adjacent surface defers to)"
    - "docs/plugin-contract.md trust section -> what a third-party plugin author builds against (the contract must be republished coherently once the trust authority changes)"
    - "docs/plugins/signal.md -> the one place a Signal user learns why their locally-built binary needs explicit consent"
  prohibitions:
    - "No parallel tone or tooltip gate may be introduced — both new states feed the existing precedence chain."
    - "The shadowed state must never render in the destructive tone."
    - "The manifest-unverified state must never offer the re-pin action, and must never gain a new dialog or interstitial."
    - "No new hex colour, spacing value, or type size may be introduced."
    - "Documentation must not describe the manifest hash check as publisher authentication, a signature, or proof of origin."
    - "The published contract must not gain any statement implying a plugin can influence its own trust tier."
    - "No new e2e plugin binary may be built — both specs must be driven by binaries the existing make e2e recipe already produces."
---

<objective>
Make the two new trust states visible to a user, prove them in the browser, and republish the plugin contract so it describes the trust system that now actually ships.

Purpose: a refuse-to-load that only appears in a log line is a silent failure by another name. D-13 and D-14 both say the UI must say why. And the plugin contract is the document third-party authors build against — leaving it describing directory-derived trust after the authority moved would be worse than not documenting the change at all.
Output: the two named chip states wired through the existing precedence chain, two Playwright specs driving them with real binaries, and the republished contract, Signal, and API documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-per-item-curation-installable-app/13-CONTEXT.md
@.planning/phases/13-per-item-curation-installable-app/13-UI-SPEC.md
@.planning/phases/13-per-item-curation-installable-app/13-05-SUMMARY.md
</context>

## Requirement traceability

This plan and 13-05 implement the **folded todo** `.planning/todos/pending/2026-08-13-plugin-trust-tier-is-directory-location-not-provenance.md`, which has no requirement ID of its own. The `requirements` field names **PLUG-07** — the shipped requirement "kernel derives a plugin's trusted/untrusted status from provenance… never from anything the plugin declares about itself" — because the folded todo is the finding that Phase 11 delivered only a filesystem-location proxy for its trusted half. The phase's own four requirement IDs (KERN-09, KERN-10, UI-13, UI-14) are carried by plans 13-01 through 13-04.

## Planner decisions carried into this plan

- **PD-05** — The shadowing state arrives on a new `launch_advisory` field, not as a `launch_failure` value; see 13-05-PLAN.md for the full rationale. The *rendered* result is exactly 13-UI-SPEC.md E6: warning-tone dot, that exact tooltip sentence, routed through `isAdvisoryOnly`/`healthTone`. Only the wire field differs from the spec's prose sketch, and this plan is where that difference becomes visible in the UI code — do not "correct" it back to `launch_failure`.

## Artifacts this phase produces

New symbols, files, fields, routes, and build inputs created across all six Phase 13 plans:

**kernel/index** — `item_marks` table; `idx_item_marks_lookup` index; `MarkKindExcluded`; `MarkView` type with `ViewIncluded` / `ViewExcluded`; `(*Store).SetItemMarks`; `(*Store).ClearItemMarks`; `(*Store).CountItemMarks`; unexported `markFilterClause`, `pruneItemMarksTx`.
**kernel/httpapi** — `kernel/httpapi/marks.go`; `MarksHandler`; unexported `marksRequest` / `marksResponse`; `streamResponse.ExcludedCount` (`excluded_count`); `sourceStatus.LaunchAdvisory` (`launch_advisory`); route `POST /api/webspaces/{webspace}/marks`; query parameter `?view=` on `GET /api/webspaces/{webspace}/stream`.
**kernel/pluginhost** — `kernel/pluginhost/manifest.go`; unexported ldflags var `buildManifest`; `TrustManifest`; `VerifyTrustedBinary`; `ManifestEntriesForBinaries`; `FormatManifest`; `ParseManifest`; `OverrideBuildManifest`; `OverrideBuildManifestFromDir`; `ErrManifestUnverified`; `LaunchFailureManifestUnverified`; `LaunchAdvisoryShadowed`; `SourceHealth.LaunchAdvisory`; unexported `manifestUnverifiedError`, `resolveBinaryDetailed`, `Plugin.shadowed`.
**build** — `cmd/topos-manifest/` (the `topos-manifest` generator); Makefile variables `MANIFEST_PLUGIN_BINARIES`, `MANIFEST_PLUGIN_BINARIES_PORTABLE`, `MANIFEST_E2E_BINARIES`.
**web/src/lib** — `api.ts`: `setItemMarks`, `MarksResponse`, `MarkAction`, `StreamResponse.excluded_count`, `SourceStatus.launch_advisory`; `toast.ts`: `markSuccessToast`, `markFailureToast`, `pwaUpdatedToast`, `markPhrase`; `selection.ts`: `toggleSelection`, `selectRange`, `clearSelection`.
**web components** — `web/src/lib/components/ui/sonner/` (shadcn-svelte block, `Toaster`); `SelectionActionBar.svelte`; new props `bulkSelected` / `bulkModeActive` / `onbulktoggle` on `StreamRow.svelte` and `StreamList.svelte`; new props `excludedCount` / `view` / `ontoggleview` on `WebspaceHeader.svelte`; new props `excluded` / `onexclude` / `oninclude` / `markBusy` on `DetailPane.svelte`.
**web/PWA** — `web/pwa-assets.config.ts`; generated icon assets under `web/static/`; build-time `manifest.webmanifest` and the Workbox service worker; `virtual:pwa-register` wiring in `web/src/routes/+layout.svelte`.
**e2e** — `web/e2e/specs/13-exclude-tracer.spec.ts`, `13-multi-select-bulk-exclude.spec.ts`, `13-excluded-view.spec.ts`, `13-pwa-manifest-sw.spec.ts`, `13-manifest-unverified.spec.ts`, `13-shadowed-advisory.spec.ts`; a trusted-directory link helper in `web/e2e/fixtures/plugin-binaries.ts`.
**docs** — README "As an app (PWA)" section; `docs/plugin-contract.md` build-manifest trust section; `docs/plugins/signal.md` consent-and-pin section; `docs/api.md` marks route, `view` parameter, `excluded_count`, `launch_advisory`; `docs/testing.md` spec entries.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two new named chip states, fed through the existing precedence chain</name>
  <files>web/src/lib/api.ts, web/src/lib/format.ts, web/src/lib/components/SourceChip.svelte, web/src/lib/components/match-advisory.test.ts, web/src/lib/components/source-chip-tooltip.test.ts</files>
  <read_first>
    - web/src/lib/format.ts lines 105-190 (healthTone's full branch chain with its ordering rationale, isAdvisoryOnly and the trap its doc comment describes, shortHash)
    - web/src/lib/components/SourceChip.svelte (the `tone`, `isPinMismatch`, `advisoryOnly`, and `tooltipText` derivations; the tooltip render sites including the native `title` touch degrade; the chip-menu block gated on `isPinMismatch`)
    - web/src/lib/api.ts SourceStatus in full (the field-by-field doc comments the new field must match in care and specificity)
    - web/src/lib/components/match-advisory.test.ts (the existing healthTone/isAdvisoryOnly table and the local tooltip re-implementation it tests against — both must be extended, not replaced)
    - 13-UI-SPEC.md section "E6 — Trust-tier hardening: two new named chip states", the Color table rows for Destructive and Warning, and the Copywriting Contract rows for both tooltips
    - kernel/httpapi/sources.go sourceStatus (the wire shape 13-05 produced)
  </read_first>
  <behavior>
    - healthTone returns destructive for a source whose launch_failure is the manifest-unverified value, regardless of its sync history.
    - healthTone returns warning for a launched, reachable, successfully-synced source carrying the shadowed advisory.
    - healthTone still returns destructive for a pin-mismatched source, and a pin mismatch outranks a shadowed advisory on the same source.
    - healthTone still returns destructive for an unreachable source even when it carries a shadowed advisory — a real problem is never displaced by an advisory.
    - isAdvisoryOnly returns true for a shadowed source that would otherwise be healthy, and false for a shadowed source that is also unreachable, errored, never-synced, or pin-mismatched.
    - isAdvisoryOnly still returns true for a last_notice-only source and false when a real problem coexists.
    - The tooltip for a manifest-unverified source is the exact contract sentence; the tooltip for a shadowed-but-otherwise-healthy source is the exact contract sentence; a source with both a shadowed advisory and a real problem shows the real problem's sentence.
  </behavior>
  <action>
Extend the one precedence chain rather than adding a second.

**web/src/lib/api.ts.** Widen `launch_failure` to `'' | 'pin_mismatch' | 'manifest_unverified'` and add `launch_advisory?: '' | 'shadowed'`. Document the new field with the same care its neighbours get: a closed vocabulary describing a source that DID launch but whose provenance is ambiguous — a same-named trusted-directory binary shadowed the pinned external plugin the operator consented to. State explicitly that it is distinct from `launch_failure`, which keeps its published "never launched at all" meaning, and that a client must branch on the field rather than on any message text.

**web/src/lib/format.ts.** In `healthTone`, add the manifest-unverified check immediately beside the existing pin-mismatch check at the head of the chain — a source refused at launch never got a chance to sync and must not fall into the neutral branch. Add the shadowed advisory as a branch in the SAME position the existing `last_notice` advisory branch occupies, LAST among the problem states and immediately before the success return, for the identical reason its comment already gives: a launch refusal, a never-synced source, an unreachable source, and a failed sync are each a bigger fact than "also, here's a heads up". Reuse the existing `warning` tone; introduce no fifth tone.

In `isAdvisoryOnly`, widen the early return so it answers false only when BOTH `last_notice` and `launch_advisory` are empty, and clear BOTH when re-asking `healthTone` — otherwise a shadowed source would never satisfy the success re-ask and the advisory-gated branch would be dead code, which is exactly the trap that function's own doc comment warns about. Update the doc comment to name the second input.

**web/src/lib/components/SourceChip.svelte.** Add an `isManifestUnverified` derivation mirroring `isPinMismatch`'s shape exactly, and an `isShadowed` derivation reading the new advisory field. Extend `tooltipText` with both contract sentences, placing the manifest-unverified sentence beside the pin-mismatch sentence in the problem group and the shadowed sentence in the advisory group gated by `advisoryOnly`. Leave the chip menu's re-pin action gated on `isPinMismatch` alone — do not extend it to the manifest-unverified value, and add no new menu item, dialog, or interstitial; the remedy is the existing external-tier consent and pin flow, and the tooltip is the entire new surface.

**Tests.** Extend `match-advisory.test.ts`'s existing tables with every `<behavior>` bullet, including the local tooltip re-implementation it maintains, and extend `source-chip-tooltip.test.ts` with the two new sentences. Do not weaken any existing case.
  </action>
  <verify>
    <automated>npm --prefix web run test && npm --prefix web run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run test` passes with new cases for all eight `<behavior>` bullets.
    - `grep -n 'binary not in the trusted build manifest' web/src/lib/components/SourceChip.svelte` shows the contract-exact sentence.
    - `grep -n 'is shadowing this pinned plugin' web/src/lib/components/SourceChip.svelte` shows the contract-exact sentence.
    - `grep -n 'launch_advisory' web/src/lib/api.ts web/src/lib/format.ts web/src/lib/components/SourceChip.svelte` shows the field consumed in all three.
    - A test asserts an unreachable source carrying the shadowed advisory reads destructive, not warning.
    - A test asserts a pin-mismatched source carrying the shadowed advisory shows the binary-changed sentence, not the shadowing sentence.
    - Reading `SourceChip.svelte`, the chip menu's re-pin action condition is unchanged from its pre-plan form.
    - Source assertion: neither new state's rendering introduces a literal hex colour, a new spacing value, or a new type size — both tones resolve through the existing `HealthTone` return values and their existing token classes.
    - `npm --prefix web run check` reports zero errors.
  </acceptance_criteria>
  <done>Both new trust states have a dot tone and a named sentence, and they reach the screen through the same chain every other health state already uses.</done>
</task>

<task type="auto">
  <name>Task 2: Browser specs driving both states with real binaries</name>
  <files>web/e2e/fixtures/plugin-binaries.ts, web/e2e/fixtures/config-builder.ts, web/e2e/specs/13-manifest-unverified.spec.ts, web/e2e/specs/13-shadowed-advisory.spec.ts</files>
  <read_first>
    - web/e2e/fixtures/plugin-binaries.ts in full (KERN_BIN, linkPluginBinaries, the deliberate symlink-rather-than-copy decision, and how bin/plugins-external/topos-plugin-external-demo is handled)
    - web/e2e/fixtures/config-builder.ts in full (the FixtureConfigSpec shape, how the trusted and external plugin directories are set, and the pin-hash computation for external binaries)
    - web/e2e/specs/11-binary-changed-repin.spec.ts (the named-failure-state spec pattern, including how it tampers with a binary copy rather than the shared build output)
    - web/e2e/specs/11-external-tier-badge.spec.ts (how an external-tier source is configured and asserted in the fixture)
    - Makefile's `e2e` target after 13-05's reorder (which three binaries enter the manifest, and that external-demo is deliberately built into a different directory and therefore never in it)
    - docs/testing.md "Writing a new spec" and "Zero-retries policy"
  </read_first>
  <action>
Drive both states from real on-disk provenance, using only binaries `make e2e` already builds.

Extend the fixtures with a helper that links an ARBITRARY binary path into the hermetic kernel's TRUSTED plugin directory under a caller-chosen name — the mirror of the existing external-directory linking. Keep it symlink-based like the existing helper (hashing follows symlinks, so the hash is the target's). Thread whatever config-spec field it needs through `config-builder.ts` in the same style the external-directory support already uses.

**web/e2e/specs/13-manifest-unverified.spec.ts (new).** Link `bin/plugins-external/topos-plugin-external-demo` into the hermetic TRUSTED directory — it is built by the `e2e` recipe's `external-demo` dependency into a different output directory and is therefore, by construction, absent from the manifest that recipe links into the kernel. This is the file-drop bypass path made concrete. Configure a source using that binary name, then assert: the source's chip renders the destructive-tone health dot; its tooltip reads the contract-exact manifest sentence; the source is not reachable; and the chip menu offers no re-pin action. Also assert the kernel's own captured log (available through the kernel fixture's `logs()`) names the refusal, proving the log-AND-UI requirement of D-13.

**web/e2e/specs/13-shadowed-advisory.spec.ts (new).** Link `bin/plugins/topos-plugin-mock` into BOTH the hermetic trusted directory and the hermetic external directory under the same name, and record a pin for the external copy the way the existing external-tier fixture does. Configure a source using that binary name. Assert: the source launches and is reachable (the trusted copy is in the manifest, so it verifies); its chip renders the WARNING-tone dot, not destructive; and its tooltip reads the contract-exact shadowing sentence.

Both specs must satisfy the existing hygiene gate and the zero-retries policy: assert on user-visible text and roles, no arbitrary waits, and no new plugin binary built by the spec itself.
  </action>
  <verify>
    <automated>npm --prefix web run check:e2e && make e2e E2E_ARGS="--grep '13-manifest-unverified|13-shadowed-advisory'"</automated>
  </verify>
  <acceptance_criteria>
    - `make e2e E2E_ARGS="--grep '13-manifest-unverified|13-shadowed-advisory'"` passes.
    - `make e2e` passes end to end — the new fixtures break no pre-existing spec.
    - `npm --prefix web run check:e2e` reports zero TypeScript errors.
    - The manifest-unverified spec asserts on the kernel's captured log output as well as on the chip's tooltip.
    - The shadowed spec asserts the dot tone is the warning tone and explicitly asserts it is NOT the destructive tone.
    - `git diff Makefile` is empty for this plan — neither spec required a new binary.
  </acceptance_criteria>
  <done>Both bypass paths the folded todo documented are now closed by code and proven by a browser test that would fail if the gate were removed.</done>
</task>

<task type="auto">
  <name>Task 3: Republish the plugin contract, the Signal guidance, and the API surface</name>
  <files>docs/plugin-contract.md, docs/plugins/signal.md, docs/api.md, docs/testing.md</files>
  <read_first>
    - docs/plugin-contract.md sections `## Discovery and launch`, `## Trust tiers`, `## Pinning`, and `## What this document does not cover` (every sentence describing directory-derived trust needs revisiting)
    - docs/plugins/signal.md in full (the existing build and setup guidance a new section joins)
    - docs/plugins/_template.md (the per-plugin page structure a new section must fit)
    - docs/api.md `### GET /api/sources` (the field table the new field joins, and how `launch_failure` is currently described)
    - docs/releasing.md `## The Signal plugin binary` (why release artifacts exclude it — the fact D-15's explanation rests on)
    - kernel/pluginhost/binaryhash.go's doc comment (the "narrowly an integrity control, not a cryptographic authentication feature" framing the documentation must not overstate)
    - TODO.md section "Plugin Trust System" (the operator's own written design this implements; its distribution parts are deferred and must not be promised here)
    - 13-CONTEXT.md decisions D-12 through D-16
  </read_first>
  <action>
Bring every published document into line with the trust system that now ships.

**docs/plugin-contract.md.** Rewrite `## Trust tiers` so it states the new authority plainly:
- Trusted tier means the binary's SHA-256 appears in the build-provenance manifest embedded into the kernel at link time — that is, it was built together with this kernel by `make build` / `make build-portable` / `make dev`.
- Directory location is no longer the trust authority. The two directories remain as install conveniences (D-16) and still govern resolution order, including the trusted-shadows-external rule; state that a shadowing collision is now surfaced to the operator in the UI, not only logged.
- A binary in the trusted directory that is absent from the manifest, or whose bytes differ from its manifest entry, **refuses to launch**. No subprocess is created, the kernel logs it by name, and the affected source shows a named error state. The refusal also covers the add-source flow's trial launch.
- The only path to running such a binary is the existing external-tier route: put it in the external directory and accept it through the explicit consent and pin flow, after which it runs pinned and badged untrusted.
- Add a short, honest limitation paragraph: the hash comparison is an integrity control, not publisher authentication — there is no signature and no proof of origin, and it proves only that the bytes on disk are the bytes this kernel was built alongside. Do not overstate it.
Update `## Pinning` and `## Discovery and launch` wherever they currently imply directory-derived trust. Do not describe the deferred distribution work (published plugin artifacts, pull-by-URL install, a single managed directory) as forthcoming — it is backlog, not a promise.

**docs/plugins/signal.md.** Add a clearly-headed section explaining D-15 in the terms a Signal user will hit it in:
- Release artifacts do not include the Signal plugin, because it is the only cgo build in the repository and needs the system SQLCipher library — `docs/releasing.md` already documents this.
- It follows that a release kernel's manifest can never contain a locally-built Signal binary's hash, because that binary did not exist when the release kernel was linked. Dropping it into the trusted directory therefore refuses to load. This is the trust system working correctly, not a bug and not a special case for Signal.
- The fix, step by step: place the locally-built binary in the external plugin directory, add it through the untrusted-add consent flow, accept it, and it runs pinned with the untrusted badge. Re-running `make signal` produces new bytes and requires re-accepting the changed binary, exactly like any other pinned external plugin.
- Whoever builds the whole repository with `make build` is unaffected: the kernel and the Signal plugin are built together, so the hash is in that kernel's own manifest.
Link both directions between this section and the plugin-contract trust section.

**docs/api.md.** Add `launch_advisory` to the `GET /api/sources` field documentation, with its closed vocabulary, its distinction from `launch_failure`, and the do-not-parse-the-text discipline the neighbouring fields already carry. Add the manifest-unverified value to `launch_failure`'s documented vocabulary.

**docs/testing.md.** Add the two new specs to the spec index in the same style as the existing Phase 11 and 12 entries, naming which decision each pins.

Run `make docs-check` and fix anything the edits break.
  </action>
  <verify>
    <automated>make docs-check && grep -q 'build-provenance manifest' docs/plugin-contract.md && grep -q 'launch_advisory' docs/api.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugin-contract.md`'s `## Trust tiers` section states that manifest membership, not directory location, determines trusted tier, and states the refuse-to-load behaviour including that it covers the trial launch.
    - `docs/plugin-contract.md` contains an explicit limitation paragraph stating the check is an integrity control and not publisher authentication or a signature.
    - `docs/plugins/signal.md` contains a section covering all four points listed in the action, including the step-by-step consent-and-pin remedy.
    - `docs/plugins/signal.md` and `docs/plugin-contract.md` link to each other on this topic (`make docs-check` proves both targets resolve).
    - `grep -n 'launch_advisory' docs/api.md` reports a match inside the `GET /api/sources` section, and `grep -n 'manifest_unverified' docs/api.md` reports a match in the `launch_failure` vocabulary.
    - `grep -n '13-manifest-unverified' docs/testing.md` and `grep -n '13-shadowed-advisory' docs/testing.md` both report a match.
    - `make docs-check` passes.
  </acceptance_criteria>
  <done>A third-party plugin author reading the contract learns how trust is actually derived, and a Signal user who hits the refusal finds the explanation and the fix in the place they would look.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel `GET /api/sources` → browser | Trust state crosses into a rendering layer that must not soften or hide it |
| published documentation → third-party plugin authors | What the contract claims about trust shapes what authors and operators believe is guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-28 | Spoofing | chip health tone precedence | high | mitigate | Both new states are added to the single `healthTone` / `isAdvisoryOnly` chain rather than a parallel gate, and tests assert that a real problem (unreachable, errored, pin-mismatched) always outranks the shadowed advisory — so a compromised or ambiguous source can never render as merely advisory. |
| T-13-29 | Repudiation | refuse-to-load visibility | high | mitigate | The e2e spec asserts the refusal appears in BOTH the kernel's captured log output and the chip tooltip, so a future change that silences either half fails the gate (D-13's log-AND-UI requirement). |
| T-13-30 | Information Disclosure | overstated security claims in published docs | medium | mitigate | The contract gains an explicit limitation paragraph stating the hash comparison is an integrity control and not publisher authentication, mirroring `binaryhash.go`'s own framing, so no operator concludes the manifest proves origin. |
| T-13-31 | Elevation of Privilege | a remedial UI action applied to the wrong state | high | mitigate | The re-pin action stays gated on the pin-mismatch value alone; extending it to a manifest-unverified trusted binary would let one click grant execution to a dropped binary, which is exactly the bypass this phase closes. An acceptance criterion pins the condition unchanged. |
</threat_model>

<verification>
- `npm --prefix web run test`, `npm --prefix web run check`, and `npm --prefix web run check:e2e` pass.
- `make e2e` passes end to end.
- `make docs-check` passes.
- `make test-portable` passes.
</verification>

<success_criteria>
- A user whose plugin binary fails verification sees, on the source chip, a red dot and a sentence naming exactly why, with no remedial shortcut that would bypass the gate.
- A user whose consented external plugin is being shadowed sees an amber advisory saying so.
- The published contract and the Signal page describe the trust system that actually ships, without overstating what a hash proves.
</success_criteria>

<output>
Create `.planning/phases/13-per-item-curation-installable-app/13-06-SUMMARY.md` when done
</output>
