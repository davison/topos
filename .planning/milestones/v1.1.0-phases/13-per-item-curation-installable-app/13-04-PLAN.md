---
phase: 13-per-item-curation-installable-app
plan: 04
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/vite.config.ts
  - web/pwa-assets.config.ts
  - web/static/
  - web/src/app.html
  - web/src/routes/+layout.svelte
  - web/src/lib/toast.ts
  - cmd/topos/main.go
  - cmd/topos/mime_test.go
  - web/e2e/specs/13-pwa-manifest-sw.spec.ts
  - README.md
  - docs/testing.md
autonomous: false
requirements: [UI-13, UI-14]
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The built SPA emits a web app manifest and a Workbox service worker, and the kernel serves both from its embedded static filesystem with no third-party host involved (UI-13)."
    - "The kernel serves the manifest with the application/manifest+json content type regardless of the host OS mime database (UI-13, RESEARCH Pitfall 4)."
    - "A desktop browser pointed at the kernel's loopback address offers its own native install affordance, and the installed window opens standalone against the local kernel (UI-13)."
    - "After the kernel binary is upgraded and restarted, the installed app lands on the new build rather than a cached one — including for a client-side route served through the 200.html fallback (UI-13, RESEARCH Pitfall 3)."
    - "The service worker precaches the app shell only; no /api response is ever precached or runtime-cached, so the stream can never render stale synced data (REQUIREMENTS Out of Scope, RESEARCH Pitfall 5)."
    - "With the kernel process not running, the installed app still renders the existing StreamError copy rather than a browser error page, because the shell is precached (UI-SPEC E8)."
    - "The autoUpdate reload is browser-native: after it the app boots through its normal load path and the informational update toast explains the refresh — no custom update interstitial exists (UI-SPEC E8 loading)."
    - "The update toast is informational-toned, carries no action button, auto-dismisses, and reads exactly `topos updated to the latest version.` (UI-SPEC E3.3)."
    - "The app adds no custom install button or banner — the install affordance is entirely browser-owned (UI-SPEC E7)."
    - "The manifest's theme and background colours are the app's own background token, so the launch splash never flashes an unstyled light background (UI-SPEC E7)."
    - "The icon set is derived from the existing web/static/app-icon.png with tool-handled maskable safe-zone padding — no new icon artwork is authored (UI-SPEC E7)."
    - "The mobile/LAN install limitation is documented with its cause (browsers require a secure context, which a LAN IP over plain HTTP is not) and with the recommended user-provided HTTPS workarounds (UI-14)."
    - statement: "Re-running install or service-worker registration against an unchanged build performs no reload and shows no second update toast."
      verification: backstop
    - statement: "With two windows of the installed app open during an update, each window reloads independently onto the new build and no window ever renders a mix of old and new precached assets."
      verification: backstop
    - statement: "Multiple toasts firing in quick succession (a bulk exclude immediately followed by the update toast) rely on the toast library's own default stacking behaviour, with no custom stacking limit or priority ordering."
      verification: backstop
  artifacts:
    - web/vite.config.ts
    - web/pwa-assets.config.ts
    - cmd/topos/main.go
    - web/e2e/specs/13-pwa-manifest-sw.spec.ts
    - README.md
  key_links:
    - "SvelteKitPWA kit.adapterFallback + kit.spa -> the 200.html precache entry's revision (without it the SPA fallback page has no revision and a deep-link reload can serve stale markup — RESEARCH Pitfall 3)"
    - "mime.AddExtensionType at kernel startup -> http.FileServer's Content-Type for manifest.webmanifest (the browser's installability check reads this header)"
    - "virtual:pwa-register autoUpdate callback -> pwaUpdatedToast (the one place an unannounced reload is explained)"
  prohibitions:
    - "The service worker must never cache any /api response, at precache time or at runtime."
    - "The generated manifest and icons must never reference a third-party CDN, font host, or any external asset URL."
    - "No custom Install topos button or install banner may be added to the app."
    - "No kernel HTTPS listen mode may be introduced — the LAN/mobile limitation is documented, not engineered around."
    - "No new hex colour, spacing value, or type size may be introduced by the update toast or any PWA-related surface."
    - "The update toast must never carry an action button."
    - "No offline API caching or offline data mode may be added."
---

<objective>
Make topos installable as a desktop app that can never serve a stale UI against an upgraded kernel, and document honestly why that install path is desktop-only.

Purpose: the app already runs entirely against a loopback kernel, which browsers already treat as a secure context — so installability is a build-tooling and serving problem, not an architecture problem. The one genuinely hard requirement is the never-stale guarantee, and the one genuinely honest requirement is documenting the LAN/mobile limitation rather than pretending it away.
Output: `vite-plugin-pwa` wired through `@vite-pwa/sveltekit` with the SPA fallback correctly revisioned, a generated icon set, the correct manifest content type from the kernel, the informational update toast, a browser-driven spec, and the UI-14 documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-per-item-curation-installable-app/13-CONTEXT.md
@.planning/phases/13-per-item-curation-installable-app/13-RESEARCH.md
@.planning/phases/13-per-item-curation-installable-app/13-UI-SPEC.md
@.planning/phases/13-per-item-curation-installable-app/13-01-SUMMARY.md
</context>

## Planner decisions and flagged assumptions

- **PD-07 (flagged assumption, unresolved edge)** — The edge-coverage probe classified UI-14 as `unclassified` and left it unresolved. The planner's reading, carried forward explicitly rather than silently: **UI-14 is satisfied by documentation alone.** No kernel HTTPS mode, no self-signed certificate generation, no reverse-proxy helper, and no LAN install support are in scope (UI-15 is the deferred requirement that would cover them). If the reviewer or the operator disagrees, this is the item to challenge before execution.
- **PD-08** — The service-worker update strategy is `autoUpdate` (silent activation and reload) plus an informational toast, per 13-UI-SPEC.md E8's own reasoning: a prompt-based flow can sit un-acted-on indefinitely in a long-lived installed-app session, which is exactly the staleness UI-13 forbids. CONTEXT.md leaves this to Claude's discretion.
- **Verified this planning session** — `@vite-pwa/sveltekit@1.1.0` exposes `kit.adapterFallback`, `kit.spa`, and `kit.includeVersionFile`; `kit.spa` is documented as deriving the fallback page's precache revision from `.svelte-kit/output/client/_app/version.json`, which is precisely the gap 13-RESEARCH.md Pitfall 3 flags for this repo's non-default `200.html` fallback name. `vite-plugin-pwa@1.3.0` peer-depends on `vite ^8` (this repo pins `vite ^8.0.16`) and on `@vite-pwa/assets-generator ^1.0.0`, which must therefore be installed rather than optional.

## Artifacts this phase produces

New symbols, files, fields, routes, and build inputs created across all six Phase 13 plans:

**kernel/index** — `item_marks` table; `idx_item_marks_lookup` index; `MarkKindExcluded`; `MarkView` type with `ViewIncluded` / `ViewExcluded`; `(*Store).SetItemMarks`; `(*Store).ClearItemMarks`; `(*Store).CountItemMarks`; unexported `markFilterClause`, `pruneItemMarksTx`.
**kernel/httpapi** — `kernel/httpapi/marks.go`; `MarksHandler`; unexported `marksRequest` / `marksResponse`; `streamResponse.ExcludedCount` (`excluded_count`); `sourceStatus.LaunchAdvisory` (`launch_advisory`); route `POST /api/webspaces/{webspace}/marks`; query parameter `?view=` on `GET /api/webspaces/{webspace}/stream`.
**kernel/pluginhost** — `kernel/pluginhost/manifest.go`; unexported ldflags var `buildManifest`; `TrustManifest`; `VerifyTrustedBinary`; `ManifestEntriesForBinaries`; `FormatManifest`; `ParseManifest`; `OverrideBuildManifest`; `OverrideBuildManifestFromDir`; `ErrManifestUnverified`; `LaunchFailureManifestUnverified`; `LaunchAdvisoryShadowed`; `SourceHealth.LaunchAdvisory`; unexported `manifestUnverifiedError`, `resolveBinaryDetailed`, `Plugin.shadowed`.
**build** — `cmd/topos-manifest/` (the `topos-manifest` generator); Makefile variables `MANIFEST_PLUGIN_BINARIES`, `MANIFEST_PLUGIN_BINARIES_PORTABLE`, `MANIFEST_E2E_BINARIES`.
**web/src/lib** — `api.ts`: `setItemMarks`, `MarksResponse`, `MarkAction`, `StreamResponse.excluded_count`, `SourceStatus.launch_advisory`; `toast.ts`: `markSuccessToast`, `markFailureToast`, `pwaUpdatedToast`, `markPhrase`; `selection.ts`: `toggleSelection`, `selectRange`, `clearSelection`.
**web components** — `web/src/lib/components/ui/sonner/` (shadcn-svelte block, `Toaster`); `SelectionActionBar.svelte`; new props `bulkSelected` / `bulkModeActive` / `onbulktoggle` on `StreamRow.svelte` and `StreamList.svelte`; new props `excludedCount` / `view` / `ontoggleview` on `WebspaceHeader.svelte`; new props `excluded` / `onexclude` / `oninclude` / `markBusy` on `DetailPane.svelte`.
**web/PWA** — `web/pwa-assets.config.ts`; generated icon assets under `web/static/`; build-time `manifest.webmanifest` and the Workbox service worker; `virtual:pwa-register` wiring in `web/src/routes/+layout.svelte`.
**e2e** — `web/e2e/specs/13-exclude-tracer.spec.ts`, `13-multi-select-bulk-exclude.spec.ts`, `13-excluded-view.spec.ts`, `13-pwa-manifest-sw.spec.ts`, `13-manifest-unverified.spec.ts`, `13-shadowed-advisory.spec.ts`; a trusted-directory link helper in `web/e2e/fixtures/plugin-binaries.ts`.
**docs** — README "As an app (PWA)" section; `docs/plugin-contract.md` build-manifest trust section; `docs/plugins/signal.md` consent-and-pin section; `docs/api.md` marks route, `view` parameter, `excluded_count`, `launch_advisory`; `docs/testing.md` spec entries.

<tasks>

<task type="auto">
  <name>Task 1: PWA build tooling — manifest, icon set, and a service worker that never touches the API</name>
  <files>web/package.json, web/package-lock.json, web/vite.config.ts, web/pwa-assets.config.ts, web/static/, web/src/app.html</files>
  <read_first>
    - web/vite.config.ts in full (the sveltekit() no-arguments comment explaining why options must not be passed there, the dev proxy block, the vitest block)
    - web/svelte.config.js in full (adapter-static with `fallback: '200.html'` and the comment explaining why that name was chosen)
    - web/src/app.html (where the manifest link and theme-color meta go)
    - web/package.json (the pinned vite/@sveltejs/kit versions the new plugins must be compatible with)
    - 13-UI-SPEC.md section "E7 — PWA manifest, icons, install" (the exact manifest field table and the no-custom-install-button rationale)
    - 13-RESEARCH.md Pitfalls 3 and 5, and the Standard Stack table
    - kernel/webui/embed.go (what `all:build` embeds — every generated PWA artifact must land inside kernel/webui/build)
  </read_first>
  <precondition>`npm --prefix web ci` succeeds and the npm registry is reachable — this task installs three new dev dependencies.</precondition>
  <action>
Install and configure the PWA build tooling, deriving every artifact from assets already in this repo.

Install as dev dependencies in `web/`: `vite-plugin-pwa`, `@vite-pwa/sveltekit`, and `@vite-pwa/assets-generator` (the last is a hard peer dependency of both, not optional). After installing, confirm `workbox-window` resolved into the tree — `virtual:pwa-register` imports from it — and add it explicitly if npm did not hoist it.

Create `web/pwa-assets.config.ts` using `@vite-pwa/assets-generator`'s `minimal2023Preset`, sourced from the existing `web/static/app-icon.png` (1024×1024). Generate the icon set into `web/static/` and commit the generated files — they are build inputs to `adapter-static`, which copies `static/` into the output the Go binary embeds.

Register the PWA plugin in `web/vite.config.ts` alongside the existing `tailwindcss()` and `sveltekit()` entries. Configure `SvelteKitPWA` with:
- `strategies: 'generateSW'` and `registerType: 'autoUpdate'` (PD-08).
- `kit: { adapterFallback: '200.html', spa: true }` — this is the load-bearing setting for this repo. `adapter-static` writes the fallback page after the PWA plugin runs, so without it the fallback has no precache revision and a deep-link reload can serve stale markup even after the JS bundle updates. `includeVersionFile` stays at its default.
- `manifest` populated exactly from 13-UI-SPEC.md's E7 table: `name` and `short_name` `topos`, `description` `Bringing all your topics to one place.`, `display` `standalone`, `start_url` `/`, and both `theme_color` and `background_color` set to the app's own `--background` hex value read from `web/src/app.css` (do not invent a colour; copy the token's value).
- `pwaAssets` wired to the assets config so the manifest's `icons` array is generated rather than hand-written.
- `workbox` configured to precache the app shell only: glob the built HTML/JS/CSS/icon assets, set `navigateFallback` to the SPA fallback page, and add a navigate-fallback denylist entry matching paths beginning `/api/`. Declare no runtime-caching rules at all — an entry matching the API path prefix would silently reintroduce the offline API caching this milestone's Requirements document rules out.

Add the manifest link and a `theme-color` meta tag to `web/src/app.html` if the plugin's injection does not already place them, using the same colour value as the manifest.

Run `npm --prefix web run build` and inspect `kernel/webui/build/` to confirm the manifest, the service worker, and the icon files are all present in the embedded output.
  </action>
  <verify>
    <automated>npm --prefix web run build && ls kernel/webui/build/manifest.webmanifest && node -e "const m=require('./kernel/webui/build/manifest.webmanifest');if(m.display!=='standalone'||m.name!=='topos'||!Array.isArray(m.icons)||m.icons.length===0)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run build` completes and `kernel/webui/build/manifest.webmanifest` exists.
    - The built manifest parses as JSON with `name` `topos`, `display` `standalone`, matching `theme_color` and `background_color` values equal to the `--background` value in `web/src/app.css`, and an `icons` array containing at least a 192px entry, a 512px entry, and one entry whose `purpose` includes maskable.
    - A service-worker file exists in `kernel/webui/build/` (the Workbox output).
    - Source assertion: the PWA plugin's workbox options in `web/vite.config.ts` declare no runtime-caching rules at all — the options object contains a precache glob, a navigate fallback, and a navigate-fallback denylist, and nothing else.
    - `grep -n "adapterFallback" web/vite.config.ts` shows the value `'200.html'`, matching `web/svelte.config.js`'s fallback name.
    - `grep -rn "http://\|https://" kernel/webui/build/manifest.webmanifest` reports no match, proving no external asset host.
    - `npm --prefix web run check` reports zero errors.
  </acceptance_criteria>
  <done>The production build emits an installable manifest, a full icon set, and a service worker whose scope is the app shell and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Kernel serving, the update toast, and a browser-driven spec</name>
  <files>cmd/topos/main.go, cmd/topos/mime_test.go, web/src/routes/+layout.svelte, web/src/lib/toast.ts, web/e2e/specs/13-pwa-manifest-sw.spec.ts</files>
  <read_first>
    - cmd/topos/main.go (the serve path, where startup-time global registrations belong, and the existing isLoopback warning as the network-layer counterpart of the UI-14 limitation)
    - kernel/httpapi/routes.go spaHandler (http.FileServer over the embedded FS — the code whose Content-Type inference the mime registration fixes)
    - web/src/routes/+layout.svelte (your 13-01 Toaster mount — the update toast goes in the same file)
    - web/src/lib/toast.ts (your 13-01 helpers — pwaUpdatedToast joins them)
    - 13-UI-SPEC.md sections E3.3 and E8, and the Copywriting Contract row for the update toast
    - web/e2e/fixtures/kernel.ts and web/e2e/specs/09-static-assets-and-surfaces.spec.ts (the existing served-asset spec pattern this one follows)
  </read_first>
  <behavior>
    - A Go test asserts that after the startup registration, mime.TypeByExtension(".webmanifest") returns a value whose media type is application/manifest+json.
    - The e2e spec asserts GET /manifest.webmanifest returns 200 with a Content-Type header whose media type is application/manifest+json.
    - The e2e spec asserts the served HTML carries a manifest link element.
    - The e2e spec asserts a service worker registers successfully in the page and its scope is the site root.
  </behavior>
  <action>
Make the kernel serve the generated artifacts correctly, and explain the silent reload when it happens.

**cmd/topos/main.go** — register the manifest media type once at process start, before the HTTP server is constructed: call `mime.AddExtensionType(".webmanifest", "application/manifest+json")` and log-and-continue on error rather than failing startup. Go's `http.FileServer` infers Content-Type via `mime.TypeByExtension`, which falls through to the host OS's mime database for extensions it does not hard-code, and `.webmanifest` is not registered on every Linux distribution, macOS, or Windows install — browsers are strict about the manifest's declared type when deciding installability. Add a short doc comment naming that reason so a future reader does not delete the line as redundant. Add `cmd/topos/mime_test.go` asserting the registration takes effect.

**web/src/lib/toast.ts** — add `pwaUpdatedToast()`, firing a default/neutral-variant toast (never destructive, never accent) whose body is exactly `topos updated to the latest version.`, with no action button and a 4000ms auto-dismiss.

**web/src/routes/+layout.svelte** — import the register helper from `virtual:pwa-register` and call it with an `onRegisteredSW`/`onNeedRefresh` wiring appropriate to the `autoUpdate` strategy, so `pwaUpdatedToast()` fires once after the new build has activated and the page has reloaded. Guard the whole registration behind a browser check so a non-browser evaluation is a no-op. Do not add any install button, install banner, or update prompt UI — the reload has already happened by the time the toast renders, and the browser owns the install affordance.

**web/e2e/specs/13-pwa-manifest-sw.spec.ts (new)** — against the hermetic kernel fixture, assert every `<behavior>` bullet touching the served surface: the manifest's status and content type, the manifest link element in the served HTML, and a successful service-worker registration whose scope is the root. Read the registration state through the page context (`navigator.serviceWorker.getRegistration()`), not through a network intercept. Keep the spec free of arbitrary waits, per the zero-retries policy.
  </action>
  <verify>
    <automated>go test ./cmd/topos/... -count=1 && npm --prefix web run check && make e2e E2E_ARGS="specs/13-pwa-manifest-sw.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/topos/... -count=1` passes including `cmd/topos/mime_test.go`.
    - `make e2e E2E_ARGS="specs/13-pwa-manifest-sw.spec.ts"` passes with all four `<behavior>` assertions.
    - The spec additionally asserts, after a page load and a stream fetch, that every entry in the browser's Cache Storage has a request URL outside the API path prefix — the behavioural proof that no API response was cached.
    - `make e2e` passes end to end — the newly registering service worker breaks no pre-existing spec.
    - `grep -n 'topos updated to the latest version.' web/src/lib/toast.ts` shows the contract-exact string.
    - `grep -n 'virtual:pwa-register' web/src/routes/+layout.svelte` shows the registration wiring in exactly that one file.
    - `npm --prefix web run check` and `npm --prefix web run check:e2e` report zero errors.
  </acceptance_criteria>
  <done>The kernel serves the manifest with the type browsers require, the service worker registers against the served SPA, and a silent update explains itself.</done>
</task>

<task type="auto">
  <name>Task 3: UI-14 — document the mobile/LAN install limitation and its workarounds</name>
  <files>README.md, docs/testing.md</files>
  <read_first>
    - README.md sections `## Install`, `## Configure`, and `## Run` (heading levels, tone, and the existing prebuilt/from-source subsection structure the new section joins)
    - cmd/topos/main.go isLoopback and its warning text (the network-layer counterpart this documentation mirrors at the browser layer)
    - docs/releasing.md (how a user obtains an upgraded kernel — the never-stale claim is about this path)
    - docs/testing.md "What stays manual, and why" (where the human-verified install check is recorded)
    - 13-UI-SPEC.md E7/E8 (what the install affordance is and is not)
  </read_first>
  <action>
Write the UI-14 documentation honestly: what works, what does not, and what the user can do about it themselves.

Add a `### As an app (PWA)` subsection under README's `## Install` heading (after the existing prebuilt and from-source subsections, since it presumes an installed kernel). Cover, in the README's existing plain register:
1. **How to install it.** Run the kernel, open the loopback address in a Chromium-based browser or Edge, and use the browser's own install affordance in the address bar or app menu. State explicitly that topos adds no install button of its own.
2. **Why the desktop path works with no extra setup.** Browsers treat `http://localhost` and `http://127.0.0.1` on any port as a secure context, which is what service-worker registration and install eligibility require. The kernel's default listener is already loopback.
3. **The limitation (the UI-14 requirement).** Opening topos from another device on the LAN — a phone, a tablet, another desktop — over a plain-HTTP LAN address such as `http://192.168.1.20:7777` is NOT a secure context, so the browser will not register a service worker and will not offer to install the app. The page itself still loads and works; only installability is unavailable. Name this as a browser rule, not a topos bug, and cross-reference the kernel's own non-loopback listener warning as the network-layer counterpart of the same concern.
4. **Recommended user-provided HTTPS workarounds**, framed as things the user runs, not features topos ships: (a) put a reverse proxy the user already trusts in front of the kernel and terminate TLS there with a certificate their devices already trust; (b) use a mesh VPN that issues real certificates for its own hostnames (this repo's dev tooling already allowlists such hostnames in `web/vite.config.ts`); (c) for a single trusted device, a locally-generated certificate authority added to that device's trust store. For each, state the one thing the user must still do: the kernel must be reachable from the proxy, so `[server] listen` has to be widened beyond loopback, and the kernel logs a warning when it is — deliberately, because that widening is a real exposure decision.
5. **What topos will not do.** No built-in HTTPS listen mode is planned for this release; that is tracked separately. Do not promise a date.

Add a short entry to `docs/testing.md`'s manual section recording that the actual desktop install, the standalone window launch, and the post-upgrade freshness check are human-verified (they need a real browser install and a real kernel upgrade, which the hermetic Playwright harness cannot produce), and that the browser-drivable half is covered by `13-pwa-manifest-sw.spec.ts`.

Run `make docs-check`.
  </action>
  <verify>
    <automated>make docs-check && grep -q "secure context" README.md && grep -q "127.0.0.1" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '### As an app (PWA)' README.md` reports a match under the `## Install` section.
    - The new README section names all five points above; specifically, `grep -ci 'secure context' README.md` reports at least 1 and the section lists three distinct user-provided HTTPS approaches.
    - The section states that topos adds no install button of its own.
    - The section cross-references the kernel's non-loopback listener warning.
    - `grep -n '13-pwa-manifest-sw' docs/testing.md` reports a match.
    - `make docs-check` passes.
  </acceptance_criteria>
  <done>A user reading the README knows how to install topos as an app, why it will not install from their phone, and exactly what they would have to run themselves to change that.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — real install, real upgrade, real kernel-down launch</name>
  <what-built>PWA installability end to end: a generated manifest and icon set, a Workbox service worker on the autoUpdate strategy with the SPA fallback correctly revisioned, the correct manifest content type from the kernel, an informational update toast, and the README's install-and-limitations documentation.</what-built>
  <how-to-verify>
Two things a hermetic browser harness structurally cannot produce — a real browser install and a real kernel upgrade — need eyes.

**A. Install and launch (UI-13, criterion 3)**
1. `make build` then `./bin/topos serve`.
2. Open `http://127.0.0.1:7777` in Chromium, Chrome, or Edge.
3. Confirm the browser offers its own install affordance (an install icon in the address bar, or the app menu's "Install topos"). Confirm topos itself shows no install button of its own.
4. Install it. Confirm it launches in a standalone window (no address bar), the window/taskbar icon is the topos app icon, and the stream loads against the running kernel.

**B. Never stale after a kernel upgrade (UI-13, criterion 4)**
1. With the installed app open, make a visible text change in the SPA (for example, edit the header tagline in `web/src/routes/+layout.svelte`).
2. Also make a visible change that is only reachable through the SPA fallback page — load a deep link such as `/w/<your-webspace>` directly in the installed window before and after — this is the case 13-RESEARCH.md Pitfall 3 warns is most likely to have a precache-revisioning gap.
3. `make build`, stop the kernel, restart it.
4. Return to the installed app window. Confirm that within a few seconds it reloads on its own onto the NEW build, that the change is visible on both the root route and the deep-linked route, and that the toast reading `topos updated to the latest version.` appears once and then dismisses itself.
5. Revert your test edits.

**C. Kernel not running (UI-SPEC E8)**
1. With the app installed, stop the kernel process entirely.
2. Launch the installed app. Confirm it renders the app's own error state — `Couldn't load this webspace` / `The topos service didn't respond — check that it's running, then retry.` with a Retry button — rather than the browser's own network-error page.

**D. Documentation (UI-14, criterion 5)**
Read README's new `### As an app (PWA)` section and confirm it says what you would want a stranger to be told about why this will not install from your phone.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which of A/B/C/D failed and how</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker → kernel `/api` | A cache layer sits between the app and the API and could serve data the kernel no longer holds |
| npm registry → build output | Build-time dependencies produce code that ships inside the kernel binary |
| browser → embedded static filesystem | The kernel serves generated assets with headers the browser makes security decisions on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-20 | Tampering | Workbox service worker scope | high | mitigate | The service worker precaches the app shell only and declares no runtime-caching rules; the navigate-fallback denylist excludes the API path prefix. An acceptance criterion asserts the built worker registers no runtime route referencing that prefix, so an accidentally-added caching rule fails the build gate rather than shipping. |
| T-13-21 | Information Disclosure | generated manifest and icon assets | medium | mitigate | Every asset is generated at build time from a file already in this repository and served from the kernel's own embedded filesystem; an acceptance criterion asserts the built manifest contains no absolute external URL, so a CDN reference cannot slip in. |
| T-13-22 | Spoofing | install eligibility over a non-secure context | low | accept | Browsers refuse to register a service worker or offer installation over a plain-HTTP LAN address. This is the browser's own protection working correctly; UI-14 documents it and the recommended workarounds are all user-provided TLS, not a kernel HTTPS mode (PD-07). |
| T-13-SC | Tampering | npm install of `vite-plugin-pwa`, `@vite-pwa/sveltekit`, `@vite-pwa/assets-generator`, `workbox-*` | high | mitigate | 13-RESEARCH.md's Package Legitimacy Audit checked all five against the live npm registry this research session: every verdict `OK`, no deprecation flags, no postinstall scripts, well-known GitHub organisation repositories, high weekly download counts. No `[ASSUMED]`, `[SUS]`, or `[SLOP]` verdicts, so no blocking legitimacy checkpoint is required. Versions and peer-dependency compatibility with this repo's pinned Vite 8 / SvelteKit 2 were re-verified during planning. |
| T-13-23 | Denial of Service | autoUpdate reload loop | medium | mitigate | The reload is driven by service-worker activation, which fires only when the precache manifest's revisions actually change; the update toast fires from the post-activation callback, not on every page load. The human-verify checkpoint's step B explicitly watches for a repeated reload. |
</threat_model>

<verification>
- `npm --prefix web run build`, `npm --prefix web run check`, and `npm --prefix web run check:e2e` pass.
- `go test ./cmd/topos/... -count=1` passes.
- `make e2e` passes end to end.
- `make docs-check` passes.
- The human-verify checkpoint returns approved for A, B, C, and D.
</verification>

<success_criteria>
- topos installs from a desktop browser and launches as a standalone app window against the local kernel.
- After a kernel upgrade the installed app lands on the new build — including on a deep-linked client-side route — without a manual cache clear.
- The mobile/LAN limitation and its user-provided HTTPS workarounds are documented in the README.
</success_criteria>

<output>
Create `.planning/phases/13-per-item-curation-installable-app/13-04-SUMMARY.md` when done
</output>
