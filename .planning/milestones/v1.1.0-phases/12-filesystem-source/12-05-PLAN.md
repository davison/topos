---
phase: 12-filesystem-source
plan: 05
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - web/e2e/specs/12-external-rehearsal.spec.ts
  - docs/plugins/filesystem.md
  - docs/plugins/README.md
  - docs/plugin-contract.md
  - docs/api.md
  - docs/testing.md
  - config.example.toml
  - README.md
autonomous: true
requirements: [SRC-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The same topos-plugin-filesystem binary, resolved from the external plugins directory instead of the trusted one, is discovered, launched, pin-verified and synced end to end — and reports tier external, with the untrusted badge on its source chip (criterion 5)"
    - "The external path is proven against a real source plugin — not a fixture-only proof binary — before any Google Drive work begins"
    - "docs/plugin-contract.md documents the file:// deep-link convention as a contract-level rule keyed on URL scheme, so a third-party local-path plugin author can adopt it without reading kernel source"
    - "docs/api.md documents POST /api/items/{id}/open, its server-side path resolution rule, its error codes, and the new text/plain rendition type"
    - "docs/plugins/filesystem.md exists as an operator page covering the path and recursive settings, the include/exclude glob extras, which file types preview and which do not, and the sync interval as the real freshness bound on a network mount"
    - "config.example.toml carries a worked filesystem source block, and README.md's plugin list and count include it"
    - "scripts/check-doc-links.sh passes — every link added by this pass resolves"
  artifacts:
    - path: "docs/plugins/filesystem.md"
      provides: "operator page for the filesystem source"
    - path: "web/e2e/specs/12-external-rehearsal.spec.ts"
      provides: "criterion 5 proof: the real filesystem binary loaded from the external tier"
  key_links:
    - from: "web/e2e/specs/12-external-rehearsal.spec.ts"
      to: "web/e2e/fixtures/config-builder.ts"
      via: "externalPluginBinaries names the filesystem binary, so it is linked into the external dir and pinned"
      pattern: "externalPluginBinaries"
    - from: "docs/plugin-contract.md"
      to: "kernel/httpapi/stream.go"
      via: "the documented file:// rewrite convention is the behaviour that helper implements"
      pattern: "file://"
---

<objective>
Close the phase: prove the external plugin path against a real source plugin rather than a
fixture, and republish every document that a reader would now find wrong.

Purpose: criterion 5 exists because Phase 14 builds the Google Drive plugin out-of-repo against
nothing but the published contract, installed through the untrusted external path. That path has
so far only carried a purpose-built proof binary. Running a real, full-featured source plugin
through it — same binary, external directory, untrusted badge, pin verification — is the
checkpoint before that work starts. The documentation pass is the other half: this phase adds a
new contract convention, a new HTTP route, a new rendition MIME type and a new config key, and
Phase 14's author will read the docs, not the source.

Output: the rehearsal spec and the full docs republish.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-filesystem-source/12-CONTEXT.md
@.planning/phases/12-filesystem-source/12-RESEARCH.md
@.planning/phases/11-external-plugins-the-trust-boundary/11-CONTEXT.md
@.planning/phases/12-filesystem-source/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: External-tier rehearsal — the real filesystem binary, loaded untrusted</name>
  <files>web/e2e/specs/12-external-rehearsal.spec.ts</files>
  <read_first>
    - web/e2e/specs/11-external-tier-badge.spec.ts, in full — the two-tier `configSpec`, the tier assertion against GET /api/sources, and the "exactly one badged chip" browser assertion
    - web/e2e/specs/11-untrusted-add.spec.ts — the untrusted add-flow spec, for what is already proven and must not be re-litigated here
    - web/e2e/fixtures/config-builder.ts — `externalPluginBinaries`, `externalPluginBinariesSrcDir`, and the `[plugins.pins]` table it writes from the binary's own hash
    - web/e2e/fixtures/plugin-binaries.ts — `linkPluginBinaries` (it links only the explicitly named binaries, which is what keeps the filesystem binary out of the trusted directory here) and `hashPluginBinary`
    - .planning/phases/11-external-plugins-the-trust-boundary/11-CONTEXT.md — D-09/D-10 (external dir and discovery), D-11 (tier collision: a binary present in both directories resolves trusted, so this spec must place it in the external directory only)
    - web/e2e/specs/12-filesystem-tracer.spec.ts — this phase's corpus-fixture shape to reuse
    - web/src/lib/plugin-fields.ts — the untrusted label copy shared by the picker and the badge assertions
  </read_first>
  <action>
  Add `web/e2e/specs/12-external-rehearsal.spec.ts`. Build a module-scope corpus directory with
  one PDF and one markdown file. Configure one source whose plugin is the filesystem binary and
  whose path is that corpus, a webspace matching the corpus directory's own base name, and — the
  load-bearing part — name the filesystem binary in the external plugin binaries list and NOT in
  the trusted plugin binaries list, so the fixture links it into the external directory only and
  writes its content hash into the pins table. Phase 11 D-11 makes a binary present in both
  directories resolve as trusted, which would silently make this spec prove nothing; the trusted
  list must therefore name a different binary (or none at all).

  Assert, in this order:
  1. GET /api/sources reports the instance reachable with tier external — discovery, pin
     verification and launch all succeeded from the untrusted directory.
  2. The webspace stream carries the same item set the trusted-tier specs already prove for an
     equivalent corpus, including the item's rewritten open-route link — syncing is identical
     across tiers, which is the "loads and syncs identically" half of criterion 5.
  3. In the browser, that source's chip carries the untrusted badge, and it is the only chip
     that does.

  Do not re-prove Phase 11's own mechanics (the add-flow warning, the re-pin flow, the
  binary-changed state) — those already have specs. This spec proves exactly one new thing: a
  real, full-featured source plugin behaves identically on the external path.

  Remove the corpus directory in an `afterAll`.
  </action>
  <verify>
    <automated>make e2e E2E_ARGS='e2e/specs/12-external-rehearsal.spec.ts'</automated>
    <automated>make e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make e2e E2E_ARGS='e2e/specs/12-external-rehearsal.spec.ts'` exits 0
    - The spec's config names the filesystem binary under the external plugin binaries list and does not name it under the trusted plugin binaries list
    - The spec asserts all three of: tier external and reachable, the expected stream items including the open-route link shape, and exactly one untrusted-badged chip
    - `make e2e` exits 0 — the full suite, including every Phase 9/11 spec, still passes with the new plugin present in the harness
  </acceptance_criteria>
  <done>
  A real source plugin binary, sitting in the external plugins directory, is discovered,
  pin-verified, launched, synced and badged untrusted — the external path is proven against real
  source work before the Google Drive phase begins.
  </done>
</task>

<task type="auto">
  <name>Task 2: Republish the contract, the API reference, the operator pages and the example config</name>
  <files>docs/plugins/filesystem.md, docs/plugins/README.md, docs/plugin-contract.md, docs/api.md, docs/testing.md, config.example.toml, README.md</files>
  <read_first>
    - docs/plugins/_template.md — the page skeleton a new plugin page is created from
    - docs/plugins/signal.md — the closest operator page (a local-path source with no base_url/token), for section shape and depth
    - docs/plugins/README.md — the index list every plugin page is linked from, and its note about which fixture plugins deliberately have no page
    - docs/plugin-contract.md, in full — where deep-link fidelity, `Item` fields, extras declaration and the launch environment are documented, so the new convention lands in the right section rather than as an appendix
    - docs/api.md — the route reference's existing entry shape (especially the item content/thumbnail routes and the whatsapp-link routes, the closest precedent for a mutating, human-only `/api/config`-style route) and wherever the rendition MIME allowlist is described
    - docs/testing.md, in full — the testing map every phase extends, and where individual specs are listed
    - config.example.toml — the `[sources.signal]` and `[sources.whatsapp]` blocks (the two local-path worked examples) and the `[sources.paperless.extras]` commented example
    - README.md — the plugin list and any plugin count it states
    - kernel/httpapi/fsopen.go, kernel/httpapi/stream.go, kernel/httpapi/item.go, kernel/config/types.go, plugins/filesystem/ — the as-built behaviour every sentence written here must match
    - scripts/check-doc-links.sh — what the link checker validates
  </read_first>
  <action>
  Write documentation that matches what was actually built — read the code first and describe it,
  never restate this plan's intentions.

  **docs/plugins/filesystem.md** (new, from `_template.md`): what the source is, the `path`
  setting including that a leading `~` is expanded by the plugin itself, the `recursive` setting
  and its conservative default, the `include_glob`/`exclude_glob` extras with their
  comma-separated form and the exclude-beats-include precedence, the default document allowlist
  by category, which categories preview inline and which show metadata and a deep link only
  (with a pointer to the deferred office-conversion idea so a reader knows it is a decision, not
  an oversight), what the folder-vocabulary match values look like for top-level and nested
  files, the symlink and dot-file policy, what happens when a mount goes away, and — the item
  12-RESEARCH.md specifically calls out — that on a network mount the sync interval, not any
  change-notification mechanism, is the real freshness bound, because NFS and SMB never deliver
  local change events for another client's writes. Also state plainly that the plugin never
  writes to the folder and that a committed build-time guard enforces it.

  **docs/plugins/README.md**: add the filesystem page to the index list, in the same one-line
  "reads X, matching on Y" style as its siblings.

  **docs/plugin-contract.md**: document the `file://` deep-link convention as a contract-level
  rule, in the deep-link section rather than as an appendix — a plugin whose items are local
  files sets `deep_link` to a `file://` URI over the real absolute path, and the kernel rewrites
  it at serve time to a loopback open route that resolves the path from its own index. State
  explicitly that the trigger is the URL scheme and never the plugin's declared `source_type`, so
  a third-party local-path plugin gets the behaviour without a kernel change, and that the
  plugin-supplied URI is used as a marker only — the kernel never opens the value it was handed.

  **docs/api.md**: add `POST /api/items/{id}/open` in the existing route-entry shape — the
  request, the success body, and the error codes for a missing item, an item that is not a local
  file, a path resolving outside its source root, and a failed launch. Say clearly that the path
  comes from index state plus configuration and never from the request. Add `text/plain` wherever
  the rendition MIME allowlist is documented.

  **docs/testing.md**: add this phase's four specs to the testing map with a one-line description
  each, and record the one thing the browser cannot drive: whether `xdg-open` genuinely hands the
  file to the desktop's own handler is a live, machine-dependent check, so the automated gate
  proves the resolved path and the route contract with a stubbed opener instead. Note the same
  for the NFS/SMB claim: the polling design is mount-type-agnostic by construction because it
  calls no watcher API, and the specs prove it against a local directory.

  **config.example.toml**: add a worked `[sources.<id>]` block for a filesystem source in the
  style of the existing local-path blocks — `plugin`, `display_name`, `path`, `recursive`, a
  commented `[sources.<id>.extras]` example showing both glob keys, and the agent grant block
  every source carries. Comment it to the same depth as its neighbours, including what validation
  does and does not apply.

  **README.md**: add the filesystem plugin to the plugin list and correct any stated plugin count
  or module count the new module changes.
  </action>
  <verify>
    <automated>make docs-check</automated>
    <automated>./scripts/check-doc-links.sh</automated>
    <automated>make test-portable</automated>
    <human-check>
    On your own desktop, with a real filesystem source configured against a folder you actually
    use: (1) open a PDF item's detail pane and confirm the inline preview renders; (2) click
    "Open in …" and confirm the document opens in your desktop's own handler rather than a
    browser tab; (3) confirm an office document shows the no-preview state with a working open
    action; (4) point a second source at a folder on your NFS or SMB mount, add a file to it from
    another machine, and confirm it appears in the stream after the next sync; (5) unmount or
    disconnect that share and confirm the source chip reports it unreachable by name rather than
    the stream quietly emptying.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/filesystem.md` exists and `docs/plugins/README.md` links to it
    - `grep -n 'file://' docs/plugin-contract.md` reports the documented convention, and the same passage states the trigger is the URL scheme rather than the source type
    - `grep -n 'items/{id}/open' docs/api.md` reports the new route entry, and `grep -n 'text/plain' docs/api.md` reports the rendition-type addition
    - `grep -n 'recursive' config.example.toml` reports the new worked block's key, and `grep -n 'include_glob' config.example.toml` reports the commented extras example
    - `grep -n 'filesystem' README.md docs/plugins/README.md docs/testing.md` reports the new entries in all three
    - `make docs-check` exits 0
    - `make test-portable` exits 0
    - Every statement in `docs/plugins/filesystem.md` about defaults, precedence and file-type behaviour matches the shipped code, verified by reading `plugins/filesystem/scope.go` and `classify.go` while writing it
  </acceptance_criteria>
  <done>
  A reader who has never seen this repository can configure a filesystem source from the docs
  alone, and a third-party plugin author can adopt the local-file deep-link convention from the
  published contract without reading kernel source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external plugins directory → kernel | An untrusted-tier binary launched under pin verification |
| published documentation → third-party plugin authors | What the docs promise becomes what external code depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-21 | Spoofing | tier collision hiding a false rehearsal result | high | mitigate | The spec places the filesystem binary in the external directory only; Phase 11 D-11 resolves a name present in both directories as trusted, so a spec that seeded both would silently prove nothing. The spec asserts the reported tier is external rather than assuming it |
| T-12-22 | Tampering | external binary swapped between pin and launch | high | mitigate | Unchanged Phase 11 machinery: the fixture writes the binary's SHA-256 into the pins table and the kernel recomputes it from disk before every exec; this plan adds no bypass and asserts the launch succeeded under that verification |
| T-12-23 | Information Disclosure | documentation naming a real operator path or credential | medium | mitigate | Every path written into docs and `config.example.toml` is a placeholder in the same style the existing blocks use; no value from the operator's own machine is copied into a committed file |
| T-12-24 | Repudiation | documenting behaviour that was not built | medium | mitigate | Task 2's read-first list names every implementation file, and its acceptance criteria require each documented default and precedence rule to be checked against the shipped source while writing |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No npm, pip or cargo package is installed by this plan |
</threat_model>

<verification>
- `make e2e` passes in full — the new plugin's presence in the harness regresses nothing
- `make docs-check` passes
- The rehearsal spec asserts external tier, identical sync behaviour and exactly one untrusted badge
- The end-of-phase human check covers the two claims a headless browser cannot: a real
  `xdg-open` handoff, and a real network mount
</verification>

<success_criteria>
- Criterion 5 is met by a real source plugin, not a fixture binary
- Every document a reader would now find wrong is corrected, and the link checker passes
- The new contract convention, HTTP route, rendition type and config key are all published
</success_criteria>

<output>
Create `.planning/phases/12-filesystem-source/12-05-SUMMARY.md` when done
</output>
