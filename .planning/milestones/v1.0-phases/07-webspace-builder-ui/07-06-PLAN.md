---
phase: 07-webspace-builder-ui
plan: 06
type: execute
wave: 6
depends_on: ["07-04", "07-05"]
files_modified:
  - web/src/lib/instance-id.ts
  - web/src/lib/instance-id.test.ts
  - web/src/lib/components/AddSourceModal.svelte
  - web/src/lib/components/add-source.test.ts
autonomous: true
gap_closure: true
requirements: [KERN-08, UI-12]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The config write path never lets one user action silently corrupt an unrelated, already-configured source instance: every AddSourceModal path that writes a brand-new `[sources.<id>]` block resolves and collision-checks its candidate id through one shared helper before it reaches `upsertSourceInstance` (closes 07-VERIFICATION.md's single failed truth, 07-REVIEW.md CR-01, D-12/D-13's exactly-one-deliberate-destructive-place principle)"
    - "Clicking `Save anyway` after a failed connection test, with a display name that derives to an id already present in `config.sources`, writes nothing at all — that instance's connection block, its `${VAR}` token reference and its `agent.read`/`agent.handoff` grants are left exactly as they were, and no `PUT /api/config` is issued (D-03's clobber guard cannot catch this case, so the client must)"
    - "A rejected `Save anyway` shows the same instance-id collision message the `Next` path already shows, and leaves the `Save anyway` action on screen so the user can correct the display name and retry without re-running the failed connection test (D-09's shared error contract: the modal stays open with entered values intact on rejection)"
    - "A blank or punctuation-only display name at `Save anyway` surfaces the same `Enter a display name so this instance has an id.` message the `Next` path surfaces, rather than a silently dead button"
    - "Instance-id derivation and collision-checking exist in exactly one place in the codebase, and a third new-instance write path cannot be added to AddSourceModal without the guard suite failing (07-REVIEW.md CR-01's own suggested fix: factor id-derivation + collision-check into one shared helper both paths call)"
    - statement: "Against a live kernel via `make dev`, the two-step `New {plugin type}…` flow's `Save anyway` path refuses a colliding display name in the browser exactly as the unit and structural guards assert, and the victim instance's chip, connection and agent grants are unchanged afterwards"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT call `upsertSourceInstance` for a brand-new instance id from any AddSourceModal path without a preceding collision check against `config.sources` — `upsertSourceInstance` replaces `[sources.<id>]` wholesale, so an unguarded call is a silent overwrite of another instance's connection AND a silent revocation of its agent grants (07-REVIEW.md CR-01; T-07-23's mitigation text assumed exactly this and was falsified)"
      status: resolved
      verification: test
  artifacts:
    - path: "web/src/lib/instance-id.ts"
      provides: "the single instance-id derivation and collision-check site every new-instance write path calls"
      exports: ["deriveInstanceId", "resolveNewInstanceId", "InstanceIdResult"]
      min_lines: 25
    - path: "web/src/lib/instance-id.test.ts"
      provides: "behavioural unit tests over the guard, including the exact CR-01 regression case"
      min_lines: 30
  key_links:
    - from: "web/src/lib/components/AddSourceModal.svelte"
      to: "web/src/lib/instance-id.ts"
      via: "both handleConnectNext and saveAnyway resolve their candidate id through resolveNewInstanceId before any write"
      pattern: "resolveNewInstanceId"
    - from: "web/src/lib/instance-id.ts"
      to: "web/src/lib/config-edit.ts"
      via: "a not-ok result means upsertSourceInstance is never reached for that id"
      pattern: "upsertSourceInstance"
---

<objective>
**As a** person adding a new source whose connection test just failed, **I want to** `Save anyway` without any risk that the display name I typed quietly replaces a different source I already had working, **so that** the UI write path stays the safe alternative to hand-editing TOML that the whole phase promises.

Purpose: this plan closes the single blocking gap in 07-VERIFICATION.md — 07-REVIEW.md's CR-01 (Critical, unresolved as of `ce620f3`). `saveAnyway()` in `AddSourceModal.svelte` omits the duplicate-instance-id collision guard its sibling `handleConnectNext` enforces, so a display name that derives to an existing instance id silently overwrites that instance's `[sources.<id>]` block — clobbering its `base_url`/token reference and resetting its `agent.read`/`agent.handoff` grants to `false`. It is reachable through ordinary UI interaction, needs no confirmation, and passes D-03's hash lock because nothing else touched the file.

Output: one shared `resolveNewInstanceId` guard in a new pure module, both AddSourceModal write paths routed through it, a behavioural unit suite covering the exact regression, and a structural guard that makes a third unguarded write path fail the suite.

**Scope discipline (gap closure).** This plan touches only what closes the gap. It does not revisit the executed plans 07-01..07-05, does not implement any of 07-REVIEW.md's five WARNING findings (WR-01..WR-05 — all unresolved, all lower severity, none falsifying an enumerated must-have truth), and does not re-author the phase's edge-coverage, UI-Considerations or prohibition predicates: those were authored once into 07-01..07-05 and section-level precedence keeps exactly one producer per section. The `must_haves` above are scoped to the failed truth plus the new truths this fix introduces, per this run's recorded scoping choice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-webspace-builder-ui/07-CONTEXT.md
@.planning/phases/07-webspace-builder-ui/07-REVIEW.md
@.planning/phases/07-webspace-builder-ui/07-VERIFICATION.md
@.planning/phases/07-webspace-builder-ui/07-04-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created or modified by this plan (new — exclude from drift verification):

| Symbol | File | Kind |
|---|---|---|
| `deriveInstanceId`, `resolveNewInstanceId`, `InstanceIdResult` | `web/src/lib/instance-id.ts` (new) | pure function + pure function + exported type |
| `web/src/lib/instance-id.test.ts` | new file | vitest suite |
| `saveAnyway`, `handleConnectNext` | `web/src/lib/components/AddSourceModal.svelte` | modified existing functions (id resolution moves out to the shared helper) |
| `new-instance write paths all route through the one shared guard` | `web/src/lib/components/add-source.test.ts` | new describe block in an existing suite |

## Source coverage audit (gap-closure run)

| Source | Item | Plan | Status |
|---|---|---|---|
| GAP | 07-VERIFICATION.md `gaps[0]` — CR-01 collision guard missing in `saveAnyway()` | 07-06 Task 1 | COVERED |
| GAP | 07-VERIFICATION.md `gaps[0].missing[0]` — reuse the guard, or factor id-derivation + collision-check into one shared helper both paths call | 07-06 Task 1 | COVERED |
| GAP | 07-VERIFICATION.md `gaps[0].missing[1]` — a regression test proving `saveAnyway()` refuses to overwrite an existing instance id | 07-06 Task 1 (behavioural) + Task 2 (recurrence guard) | COVERED |
| GAP | 07-VERIFICATION.md `deferred:` | — | EMPTY — nothing to cover |
| GAP | 07-VERIFICATION.md `behavior_unverified_items` (24 non-backstop + 4 backstop) | — | NOT CODE WORK — closed by the `make dev` walkthrough 07-05-PLAN.md's `<human-check>` already specifies. No item among them names missing or broken code; planning implementation for them would be scope expansion. The one item that intersects this gap (the two-step Describe-failure flow) has its collision scenario added to this plan's own `<human-check>` so the pending pass covers it. |
| GOAL | ROADMAP Phase 7 goal — configure sources and webspaces from the UI instead of hand-editing TOML | 07-01..07-05 (executed) | COVERED — this plan restores the goal's own safety premise, it does not add goal scope |
| REQ | KERN-08 — config editable through the kernel API, secrets environment-only, hand-editing supported | 07-01, 07-02, 07-05 (executed); 07-06 closes the CR-01 caveat | COVERED |
| REQ | UI-12 — webspace builder UI | 07-01, 07-03, 07-04, 07-05 (executed); 07-06 closes the CR-01 caveat | COVERED |
| RESEARCH | 07-RESEARCH.md Package Legitimacy Audit | 07-06 introduces no package | COVERED — no new dependency, `web/package.json` unchanged |
| CONTEXT | D-03 (optimistic hash lock), D-09 (validate-on-save, shared error contract), D-11 (two-step add flow), D-12/D-13 (exactly one deliberate place performs a destructive action) | 07-06 Task 1 + Task 2 | COVERED |
| CONTEXT | every other D-01..D-19 decision | 07-01..07-05 (executed) | OUT OF SCOPE for this gap-closure run — already covered by the executed plans |

No unplanned items. No silent drops.

**Deliberately not planned, with rationale:** 07-REVIEW.md's WR-01 (`item_count` filter divergence), WR-02 (no client-side webspace-name collision check), WR-03 (a new instance retroactively invalidating an unrelated webspace), WR-04 (no directory `fsync` after `os.Rename`) and WR-05 (deleting the last allowlisted instance re-opens participation). 07-VERIFICATION.md carries all five forward for visibility and records that none falsifies an enumerated must-have truth and none blocks the phase goal. They belong in a follow-up pass (`/gsd-code-review 7 --fix` or a later phase), not in a surgical gap-closure run.

<!-- planner-discipline-allow: deriveInstanceId -->
<!-- planner-discipline-allow: describeFailed = false -->

<tasks>

<task type="tracer">
  <name>Task 1: Close CR-01 — one shared id-resolution guard both AddSourceModal write paths call</name>
  <files>web/src/lib/instance-id.ts, web/src/lib/instance-id.test.ts, web/src/lib/components/AddSourceModal.svelte, web/src/lib/components/add-source.test.ts</files>
  <read_first>
    - web/src/lib/components/AddSourceModal.svelte — the whole file. Specifically: the local id-derivation function (~lines 129-135), `handleConnectNext`'s blank-id and collision branches (~lines 204-240), `saveAnyway`'s current unguarded body (~lines 246-269), `submitMatch` (~lines 275-296) and the `newInstanceId`/`describeFailed`/`connectError`/`savingConnectionOnly` state declarations (~lines 95-102) and their reset in `resetFlowState`
    - .planning/phases/07-webspace-builder-ui/07-REVIEW.md — CR-01 in full: the defect, the reachability argument, the suggested fix, and its closing "factor the id-derivation + collision-check into one shared helper" note
    - .planning/phases/07-webspace-builder-ui/07-VERIFICATION.md — the `gaps[0]` entry, whose `missing:` list is the definition of done for this plan
    - web/src/lib/config-edit.ts — `upsertSourceInstance`'s doc comment and body (it does `next.sources[instanceId] = source` unconditionally, which is exactly why the CALLER must guard), and the return-a-new-document discipline every helper follows
    - web/src/lib/api.ts — the `KernelConfig` and `SourceConfig` types, `ApiError`, `CONFIG_CONFLICT_MESSAGE`
    - web/src/lib/last-webspace.ts — the house shape for a small pure `.ts` module: file-header doc comment explaining WHY, then exported functions with their own doc comments. This is the analog the new module follows.
    - web/src/lib/config-edit.test.ts and web/src/lib/plugin-fields.test.ts — the house pure-unit-test style (real fixtures, behavioural assertions, one consequence-describing message per assertion)
    - web/src/lib/components/add-source.test.ts — the comment-stripped source-scan style, the `stripComments`/`extractBetween` helpers, and the existing `submitMatch` single-`putConfig` assertion this task's new assertion sits beside
    - vite.config.ts — the `test.environment: 'node'` setting and its comment, which is why the regression proof is a pure unit test plus a source scan rather than a mounted-component test (no component harness exists and this plan adds no dependency)
  </read_first>
  <action>
Give the two new-instance write paths one shared, tested id-resolution guard, and route both through it.

New module `web/src/lib/instance-id.ts`. Export `deriveInstanceId(displayName: string): string` — move the existing derivation out of AddSourceModal.svelte verbatim, preserving its exact semantics: trim, lowercase, collapse every run of non-`[a-z0-9]` characters to a single hyphen, strip leading and trailing hyphens. Export a discriminated result type `InstanceIdResult` shaped as either `{ ok: true; id: string }` or `{ ok: false; reason: 'blank' | 'collision'; message: string }`. Export `resolveNewInstanceId(cfg: KernelConfig, displayName: string): InstanceIdResult`, which derives the candidate id and returns, in order: the blank result when the derived id is the empty string, carrying the message `Enter a display name so this instance has an id.`; the collision result when `cfg.sources[candidateId]` is present, carrying the message built as `An instance id "` + the candidate id + `" already exists — choose a different display name.` (the exact copy `handleConnectNext` renders today, character for character, including the em dash and the typographic double quotes around the id being plain ASCII `"` as they are today); otherwise the ok result carrying the derived id. It reads `cfg` and never mutates it. Import `KernelConfig` as a type from `./api`.

The module's file-header doc comment must record WHY this module exists rather than the two-line inline check it replaces: 07-REVIEW.md's CR-01 found that `saveAnyway` omitted the check its sibling enforced, and `upsertSourceInstance` replaces a `[sources.<id>]` block wholesale — so an unguarded call silently overwrites another instance's `base_url`/token reference AND resets its `agent.read`/`agent.handoff` grants to `false`, since the new-instance flow always initialises `agent` fresh. State that every path that writes a brand-new instance block must resolve its id through `resolveNewInstanceId` first, and that only what the kernel structurally cannot express is rejected here (a blank id, or one already present) — everything else, including display-name uniqueness, stays with the kernel's own load-time validator so there is one rule set, exactly as the derivation function's original comment said.

New test `web/src/lib/instance-id.test.ts`, pure vitest in the repo's existing node environment. Derivation cases: `Home Email` yields `home-email`; an input padded with whitespace and carrying punctuation such as `  Work / Notes!!  ` yields `work-notes`; an all-caps input lowercases; an input of only hyphens or only punctuation yields the empty string; the empty string yields the empty string. Resolution cases, against a fixture `KernelConfig` whose `sources` carries `home-email` with `agent: { read: true, handoff: true }` and a `token` referencing an environment variable, plus at least one webspace: resolving `Home Email` returns not-ok with reason `collision` and the exact message naming `home-email`; resolving the display name stored on that existing instance likewise collides (the CR-01 scenario the review names — the user reverting an edit or typing an existing working instance's name); resolving an empty, whitespace-only or punctuation-only display name returns not-ok with reason `blank` and the exact blank message; resolving `Work Email` returns ok with id `work-email`; and every call leaves the passed config deep-equal to a structuredClone-free JSON snapshot taken before the call. Add one explicitly named regression case whose message ties the assertion to 07-REVIEW.md CR-01, asserting that resolving the existing instance's own display name never returns an ok result — so no caller can reach `upsertSourceInstance` with that id.

Wire AddSourceModal.svelte. Remove the local derivation function and import `resolveNewInstanceId` from `$lib/instance-id`. In `handleConnectNext`, replace the inline blank-id and collision branches with one call to the shared helper against `config` and the trimmed display name; on a not-ok result, keep today's behaviour exactly — clear the describe-failed flag to `false` (a validation rejection at `Next` is not a connection failure, so the `Save anyway` action must not appear) and set `connectError` to the result's message, then return. On an ok result use the result's `id` everywhere the local candidate id was used, including the assignment to `newInstanceId` in the describe-success branch.

In `saveAnyway`, call the same helper first, before anything else that can write. On a not-ok result set `connectError` to the result's message and return — and leave the describe-failed flag untouched, because the user is still in the failed-connection state and the `Save anyway` action must stay on screen so a corrected display name can be retried without re-running the failed connection test. Only on an ok result does the function clear `connectError`, set the in-flight flag, and proceed to `upsertSourceInstance(config, result.id, connectionValues)` followed by `putConfig({ base_hash: baseHash, config: nextConfig })`. Leave the rest of `saveAnyway` exactly as it is: the `savingConnectionOnly` in-flight guard and its `finally` reset, the `savedAnywayMessage` copy, the `connect-saved` step transition, the `onsaved()` call, and the `ApiError`/`config_changed_on_disk`/`CONFIG_CONFLICT_MESSAGE` error branch — that error branch is 07-05's shared save-state pattern and `save-state.test.ts` scans this file for it.

Extend `web/src/lib/components/add-source.test.ts` with the direct regression proof, in the file's existing comment-stripped style: extract `saveAnyway`'s body with `extractBetween` over the same start/end markers the existing `submitMatch` assertion uses, and assert that the body contains a `resolveNewInstanceId(` call, that the index of that call is strictly lower than the index of the `upsertSourceInstance(` call, and that a `return` appears between the two indices — so the guard cannot be present but bypassed. Give each assertion a consequence-describing message naming CR-01.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/instance-id.ts` exists and exports `deriveInstanceId`, `resolveNewInstanceId` and the `InstanceIdResult` type
    - Behaviour: `resolveNewInstanceId` given a config whose `sources` already contains `home-email`, and the display name `Home Email`, returns `ok: false` with `reason: 'collision'` and the message `An instance id "home-email" already exists — choose a different display name.`
    - Behaviour: `resolveNewInstanceId` given a display name that derives to the empty string returns `ok: false` with `reason: 'blank'` and the message `Enter a display name so this instance has an id.`
    - Behaviour: `resolveNewInstanceId` given a display name free of collisions returns `ok: true` carrying the derived id, and leaves the passed config deep-equal to its pre-call snapshot
    - Behaviour: `deriveInstanceId('  Work / Notes!!  ')` returns `work-notes` and `deriveInstanceId('---')` returns the empty string
    - `web/src/lib/instance-id.test.ts` contains a case whose test name references CR-01 and asserts that resolving an existing instance's own display name never returns an ok result
    - In `add-source.test.ts`, the extracted `saveAnyway` body contains a `resolveNewInstanceId(` call whose index is strictly lower than the index of `upsertSourceInstance(`, with a `return` between the two indices
    - `web/src/lib/components/AddSourceModal.svelte` imports `resolveNewInstanceId` from `$lib/instance-id`, and both `handleConnectNext` and `saveAnyway` call it
    - `cd web && npm run test` exits 0
    - `cd web && npm run check` reports 0 errors
    - `git diff --stat web/package.json` is empty — no dependency added
  </acceptance_criteria>
  <reversibility rating="reversible">An extracted pure helper plus a guard call at two existing call sites — removable in one commit, with no config-file, HTTP-contract or persisted-data change.</reversibility>
  <done>`Save anyway` after a failed connection test can no longer write over an existing source instance: a colliding display name is rejected client-side with the same message the `Next` path uses, no `PUT /api/config` is issued, and the victim instance's connection block and agent grants are untouched. 07-VERIFICATION.md's single failed truth is now satisfiable.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the invariant so a third new-instance write path cannot reintroduce CR-01</name>
  <files>web/src/lib/components/add-source.test.ts</files>
  <read_first>
    - web/src/lib/components/add-source.test.ts — as extended by Task 1, including the `stripComments`/`extractBetween` helpers and the existing describe-block naming style
    - web/src/lib/components/AddSourceModal.svelte — as wired by Task 1: the two `upsertSourceInstance(` call sites (`saveAnyway` and `submitMatch`), the assignments to `newInstanceId` (its declaration, its reset in `resetFlowState`, and the one real assignment in `handleConnectNext`), and the `{#if describeFailed}` gate on the `Save anyway` button in the markup
    - web/src/lib/components/save-state.test.ts — the cross-component scan that also reads `AddSourceModal.svelte` and asserts every `type="submit"` control binds a disabled state, so this task's assertions must not require any markup change
    - .planning/phases/07-webspace-builder-ui/07-VERIFICATION.md — the `gaps[0].missing` list, whose second item this task completes as a durable recurrence guard
  </read_first>
  <action>
Turn Task 1's fix into an invariant the suite enforces, so the review's "this class of bug can't recur when a third save path is added later" concern is mechanically covered rather than trusted.

Add one describe block to `web/src/lib/components/add-source.test.ts`, named for the invariant it pins (every new-instance write path routes through the one shared guard), containing these assertions over the comment-stripped `AddSourceModal.svelte` source:

Single derivation site. Assert the comment-stripped source contains zero occurrences of the token `deriveInstanceId` — derivation now lives only in `web/src/lib/instance-id.ts`, reached through `resolveNewInstanceId`, so no second derivation can drift from the guarded one. The message must say that a local derivation reappearing in this component is how CR-01 became possible.

Enumerated write paths. Assert the comment-stripped source contains exactly two occurrences of `upsertSourceInstance(`, and that they sit inside the extracted `saveAnyway` and `submitMatch` bodies respectively (one each). The message must state that a third write path is not forbidden, but that whoever adds it must extend this guard to cover it — which is the point.

Guarded ids only. Assert that `saveAnyway`'s extracted body reaches `upsertSourceInstance(` only after a `resolveNewInstanceId(` call (the ordering Task 1 established, restated here inside the invariant block for a reader who lands on this describe block first), and that `submitMatch`'s extracted body passes `newInstanceId` rather than deriving anything locally. Then assert that across the whole comment-stripped file there is exactly one assignment to `newInstanceId` that is not the `null` reset — and that it sits inside `handleConnectNext`'s extracted body at an index strictly greater than that body's `resolveNewInstanceId(` call. Together these say: the only id `submitMatch` can write is one the guard already approved.

Rejection keeps the retry affordance. Assert that `saveAnyway`'s extracted body contains zero occurrences of `describeFailed = false`, with a message stating that clearing the describe-failed flag on a rejected name would hide the `Save anyway` button and force the user back through the failed connection test to retry. Assert positively that `handleConnectNext`'s extracted body does contain `describeFailed = false`, preserving the deliberate asymmetry: a validation rejection at `Next` is not a connection failure and must not reveal `Save anyway`.

Add no assertion that requires touching `AddSourceModal.svelte`'s markup — `save-state.test.ts`, `secret-field.test.ts` and `chip-edit-menu.test.ts` all scan this component and every existing assertion in them must keep passing untouched. Follow the file's existing conventions exactly: the found-non-empty-source guard already at the top, `extractBetween` scoping, and one consequence-describing message per assertion.

Finally, run the full web suite and the typecheck and confirm the pre-existing suites are all still green — this plan changes one component and adds one module, and nothing it does should move any other suite.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `add-source.test.ts` asserts the comment-stripped `AddSourceModal.svelte` contains zero occurrences of `deriveInstanceId`
    - `add-source.test.ts` asserts `AddSourceModal.svelte` contains exactly 2 occurrences of `upsertSourceInstance(`, one within the extracted `saveAnyway` body and one within the extracted `submitMatch` body
    - `add-source.test.ts` asserts the extracted `saveAnyway` body contains zero occurrences of `describeFailed = false`
    - `add-source.test.ts` asserts the extracted `handleConnectNext` body contains `describeFailed = false`
    - Behaviour: the sole assignment to `newInstanceId` that is not the `null` reset sits inside `handleConnectNext` at an index strictly greater than that body's `resolveNewInstanceId(` call
    - `cd web && npm run test` exits 0, with `save-state.test.ts`, `secret-field.test.ts`, `chip-edit-menu.test.ts`, `config-edit.test.ts` and `plugin-fields.test.ts` all still passing and unmodified
    - `cd web && npm run check` reports 0 errors
    - `git diff --stat -- web/src/lib/components/AddSourceModal.svelte` shows no change from this task (Task 2 touches only the test file)
  </acceptance_criteria>
  <done>Reintroducing CR-01 — a new-instance write that skips the id guard, a second derivation site, or a rejection that hides the retry affordance — now fails `npm run test` rather than shipping silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input (display name field) → `[sources.<id>]` map key | A free-text field determines which existing config block a write lands on. This is the boundary CR-01 crosses unchecked. |
| browser-held config document → `PUT /api/config` | The whole document is sent; the kernel's D-03 hash lock only proves the file is unchanged on disk, not that the document's own edit was intended. |
| overwritten instance → `agent.read` / `agent.handoff` grants | A wholesale block replacement silently re-derives a security-relevant grant from a fresh default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-27 | Tampering | `AddSourceModal.saveAnyway` → `upsertSourceInstance` → `PUT /api/config` | high | mitigate | `resolveNewInstanceId` runs before any write and returns a collision result for an id already present in `config.sources`; the function returns without calling `upsertSourceInstance` or `putConfig`. Pinned behaviourally by `instance-id.test.ts` and structurally by `add-source.test.ts`'s ordering assertion (Task 1). |
| T-07-28 | Elevation of Privilege | `agent.read` / `agent.handoff` on an overwritten instance | high | mitigate | The same guard. The new-instance flow always initialises `agent` to `{ read: false, handoff: false }`, so an unguarded overwrite silently revoked a granted instance's agent access — a security-relevant state change with no UI trace. With the guard, that write is unreachable. |
| T-07-29 | Tampering | a future third new-instance write path in `AddSourceModal` | medium | mitigate | Task 2 pins a single derivation site, an exact count of `upsertSourceInstance(` call sites bound to named function bodies, and the single guarded assignment to `newInstanceId` — so an unguarded third path fails the suite instead of shipping. |
| T-07-30 | Denial of Service | a user locked out of retrying after a rejected `Save anyway` | low | mitigate | The collision branch deliberately leaves the describe-failed flag set, so `Save anyway` stays on screen and a corrected display name can be retried without re-running the failed connection test. Pinned by Task 2's asymmetry assertions. |
| T-07-31 | Information Disclosure | the new module reading the config document | low | accept | `resolveNewInstanceId` reads only `cfg.sources` map KEYS; it never touches a `${VAR}` token reference or any value, and the document the browser holds is already the raw pre-expansion form (T-07-20, unchanged from 07-01). No new exposure. |
| T-07-SC | Tampering | npm / Go package installs | high | mitigate | No package is introduced by this plan. `web/package.json` is unchanged, the vitest environment stays `node`, and no component-mount harness is added. 07-RESEARCH.md's Package Legitimacy Audit has nothing new to check; the audit table stands. |
</threat_model>

<verification>
- `cd web && npm run check` reports 0 errors and `cd web && npm run test` exits 0
- `git diff web/package.json` is empty — no dependency added, the vitest environment is still `node`
- `git diff --name-only` across this plan's commits lists only `web/src/lib/instance-id.ts`, `web/src/lib/instance-id.test.ts`, `web/src/lib/components/AddSourceModal.svelte` and `web/src/lib/components/add-source.test.ts` — no kernel file, no `docs/plugin-contract.md`, no `proto/` file
- `CGO_ENABLED=0 go build ./...` and `go test ./kernel/... -count=1` still pass (no Go file is touched; run them once as a phase-level sanity check that nothing else regressed)
- 07-VERIFICATION.md's `gaps[0].missing` list is fully satisfied: the collision guard is reused via a shared helper (item 1) and a regression test proves `saveAnyway()` refuses to overwrite an existing instance id (item 2)

<human-check>
Fold this scenario into the pending end-of-phase `make dev` walkthrough that 07-05-PLAN.md's own `<human-check>` block specifies — it is the one item among 07-VERIFICATION.md's 24 behaviour-unverified items that intersects this gap, and the verification report explicitly asks that the pass include the collision case once fixed.

With at least two plugin instances configured and one of them (call it the victim) carrying `agent.read = true` and a working connection: open a webspace, click `+`, choose a `New {plugin type}…` entry, and enter connection details that will fail the trial launch. Click `Next` and confirm the `Couldn't verify this connection.` alert plus the `Save anyway` action. Now edit the display name to the victim instance's own display name and click `Save anyway`. Confirm: the collision message names the derived id, no save occurs, the `Save anyway` button is still on screen, and the browser network tab shows no `PUT /api/config`. Then inspect `config.toml` and confirm the victim's `[sources.<id>]` block — its `base_url`, its `${VAR}` token reference and its `agent` grants — is byte-identical to before. Finally correct the display name to a free one and confirm `Save anyway` then saves normally and the new chip appears.
</human-check>
</verification>

<success_criteria>
- 07-VERIFICATION.md's single failed truth is now satisfiable: no user action in the config write path silently corrupts an unrelated, already-configured source instance (ROADMAP success criterion 3 — the UI write path and the file agree on one persisted form, and nothing is clobbered blindly; KERN-08)
- Hand-editing remains fully supported and secrets stay environment-only: this plan touches no kernel file, no config serialization, and reads no secret value (KERN-08, D-05)
- The webspace builder UI's add-source flow is safe to use without inspecting `config.toml` afterwards (UI-12, D-11/D-12/D-13)
</success_criteria>

<output>
Create `.planning/phases/07-webspace-builder-ui/07-06-SUMMARY.md` when done
</output>
