---
phase: 07.1-browser-e2e-harness
plan: 06
subsystem: testing
tags: [ci, github-actions, makefile, docs, smoke-retirement]

requires:
  - phase: 07.1-browser-e2e-harness
    provides: "07.1-01's Playwright harness (make e2e) and 07.1-03's smoke-served-spa.spec.ts, the hermetic successor to scripts/e2e-smoke.sh's three portable guards, which this plan's Task 2 deletion depends on"
provides:
  - ".github/workflows/ci.yml — first CI workflow: runs test-portable + svelte-check + vitest + e2e typecheck + make e2e (Chromium) on every push/PR to main, no credentials, artifacts on failure (D-13, D-14, D-15)"
  - "Makefile test-portable target — the credential-free, cgo-free half of make test, single-writer module list shared with test (D-13's CI dependency)"
  - "docs/testing.md — the testing map: four gates, harness architecture, how to write a spec, zero-retries policy, two mock plugins, two accepted-risk manual UAT items, and the D-11 standing rule"
  - ".claude/CLAUDE.md Conventions section — the D-11 standing rule recorded where a future planner/executor actually reads it every session"
affects: []

actuals:
  tokens: 7129
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Makefile module-list split (test-portable/test) mirrors the pre-existing plugins/signal cgo split — one canonical module list, never duplicated, so the two definitions cannot drift"
    - "GitHub Actions action versions verified live against each action's own GitHub releases API at execution time (not copied from the research skeleton's low-confidence snapshot) — checkout@v7, setup-go@v7, setup-node@v7, cache@v6, upload-artifact@v7"

key-files:
  created:
    - .github/workflows/ci.yml
    - docs/testing.md
  modified:
    - Makefile
    - README.md
    - .claude/CLAUDE.md
  deleted:
    - scripts/e2e-smoke.sh

key-decisions:
  - "Verified GitHub Actions action major versions live via the GitHub releases API (internet access was available) rather than trusting 07.1-RESEARCH.md's own flagged low-confidence skeleton — actual current majors (checkout@v7, setup-go@v7, setup-node@v7, cache@v6, upload-artifact@v7) differ from the research skeleton's guesses (checkout@v7, setup-go@v7, setup-node@v6, cache@v4, upload-artifact@v4) in setup-node/cache/upload-artifact"
  - "Two doc-comment lines (near the 'uses:' step list and the test-portable rationale) were reworded to avoid containing the literal substrings 'uses:' and 'test-signal' after each tripped its own acceptance-criteria grep — same class of self-referential-grep gotcha 07.1-01-SUMMARY.md recorded for 'webServer'"
  - "Node 26 chosen for actions/setup-node's node-version input, matching this machine's own installed Node (v26.5.1) rather than an arbitrary LTS pin, since no engines field in web/package.json specifies a floor beyond README's documented 'Node 20+'"
  - "E2E_PW_INSTALL_FLAGS=--with-deps set only in the CI workflow's env for the make e2e step, never as a Makefile default — a local `make e2e` must never invoke a privileged system-dependency installer"

patterns-established:
  - "docs/testing.md is the canonical pointer for any future gate question — README.md's Testing section now defers to it rather than re-explaining harness internals inline"

requirements-completed: [D-10, D-11, D-12, D-13, D-14, D-15]

coverage:
  - id: D1
    description: "Every push and pull request to main runs Go tests, svelte-check, vitest and the hermetic Playwright suite automatically (D-13)"
    requirement: D-13
    verification:
      - kind: unit
        ref: "python3 -c \"import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert d['jobs']\" — parses, declares job 'test' with 9 steps covering checkout/setup-go/setup-node/cache/npm ci/check/test/check:e2e/test-portable/e2e/upload-artifact"
        status: pass
      - kind: manual_procedural
        ref: "First actual push to main and its resulting CI run has not yet occurred as of this SUMMARY — cannot be verified locally per the plan's own verification note. Record the observed outcome once available."
        status: pending
    human_judgment: false
  - id: D2
    description: "CI needs no credentials, no .env file, no live source and no C toolchain — runs make test-portable, never make test (D-07, D-13)"
    requirement: D-13
    verification:
      - kind: unit
        ref: "grep -nE 'secrets\\.|PAPERLESS_|PROTON_|SB_AUTH|\\.env' .github/workflows/ci.yml exits 1 (no match); grep -c 'test-signal' .github/workflows/ci.yml is 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "CI gates on Chromium only; Firefox/WebKit remain manually invocable and never enter the automated gate (D-14)"
    requirement: D-14
    verification:
      - kind: unit
        ref: "ci.yml's only E2E_PROJECT reference is a code comment stating it is left at its chromium default; E2E_PROJECT env var is never set in the workflow"
        status: pass
    human_judgment: false
  - id: D4
    description: "A failing CI run uploads Playwright traces/screenshots and retries nothing (D-15)"
    requirement: D-15
    verification:
      - kind: unit
        ref: "grep -c 'if: failure()' .github/workflows/ci.yml is 1 (the upload-artifact step); web/playwright.config.ts's pre-existing retries: 0 is untouched by this plan"
        status: pass
    human_judgment: false
  - id: D5
    description: "scripts/e2e-smoke.sh and the smoke Makefile target no longer exist; make e2e has taken their place as the pre-ship gate (D-10, D-12)"
    requirement: D-10
    verification:
      - kind: unit
        ref: "test ! -e scripts/e2e-smoke.sh; make -n smoke exits non-zero (unknown target); grep -c 'e2e-smoke' Makefile/README.md both 0"
        status: pass
      - kind: manual_procedural
        ref: "Confirmed 07.1-03-SUMMARY.md reported no weakened guard before deletion; scripts/assert-stylesheet.sh and scripts/signal-readonly-smoke.sh both still exist and are untouched"
        status: pass
    human_judgment: false
  - id: D6
    description: "The D-11 standing rule (future UI work extends the e2e suite) is recorded where a future planner/executor will encounter it, not only in a phase context file (D-11)"
    requirement: D-11
    verification:
      - kind: unit
        ref: "grep -q 'docs/testing.md' .claude/CLAUDE.md && grep -q 'make e2e' .claude/CLAUDE.md; grep -c 'Conventions not yet established' .claude/CLAUDE.md is 0; docs/testing.md contains a '## Standing rule' section"
        status: pass
    human_judgment: false

duration: ~55min
completed: 2026-08-09
status: complete
---

# Phase 07.1 Plan 06: First CI, Retiring the Credentialed Smoke Gate Summary

**Adds `.github/workflows/ci.yml` (the repository's first CI workflow — units, svelte-check, vitest, and the hermetic Playwright suite on every push/PR to main, Chromium-only, zero-credential), deletes `scripts/e2e-smoke.sh` and `make smoke` now that `smoke-served-spa.spec.ts` fully replaces its coverage, and writes `docs/testing.md` plus a `.claude/CLAUDE.md` Conventions entry recording the standing rule that future UI work extends the e2e suite.**

## Performance

- **Duration:** ~55min
- **Completed:** 2026-08-09T23:XXZ
- **Tasks:** 3 (all `type="auto"`)
- **Files modified:** 6 (2 new, 3 modified, 1 deleted)

## Accomplishments

- Split `make test` into `test-portable` (every workspace module except the cgo Signal plugin) + `test-signal`, with `test-portable` as the single canonical module list so CI's definition and the desktop developer's `make test` can never drift apart.
- Added `.github/workflows/ci.yml`: checkout, Go setup (version read from `go.work`), Node 26 setup with npm caching, a Playwright-browser cache keyed on the lockfile, `npm ci` + `check`/`test`/`check:e2e`, `make test-portable`, `make e2e` (with `E2E_PW_INSTALL_FLAGS=--with-deps` scoped to that one step only), and a failure-conditioned artifact upload of Playwright traces/screenshots. Every action version was verified live against each action's own GitHub releases at execution time rather than trusting the research skeleton's flagged low-confidence guesses.
- Confirmed `07.1-03-SUMMARY.md` reported no weakened guard, re-read `smoke-served-spa.spec.ts` and verified it covers all three of `scripts/e2e-smoke.sh`'s portable assertions (stylesheet theme token, stream item link shape, typed 404 envelope) before deleting the script and its Makefile target; updated README.md's repository-layout, dotenv, and Testing sections accordingly.
- Wrote `docs/testing.md`: the four gates, single-spec/single-project/cross-engine run instructions, the harness architecture (fresh kernel per file, ephemeral port, generated temp config, closed-set plugins dir, env allowlist) and the failure each isolation property prevents, how to write a new spec, the zero-retries policy, the two mock-shaped plugins, and the two Phase 7 UAT items (SIGKILL timing windows) that remain manual, accepted risk.
- Recorded the D-11 standing rule in `.claude/CLAUDE.md`'s Conventions section, replacing its placeholder text, with a pointer to `docs/testing.md` — the placement that reaches every future phase's planner and executor, not just a phase context file.
- Ran all four gates locally end-to-end after every task: `make test-portable` (17 packages, all pass), `npm run check` (0 errors, 9 pre-existing warnings), `npm run test` (607/607), `npm run check:e2e` (clean), `make e2e` (22/22 Playwright tests pass, twice, after both the Task 1 and Task 2 edits), `make dev-check` (all three cases pass), and finally the full `make test` including the cgo Signal module (all pass).

## Task Commits

1. **Task 1: Split the portable test target, then add the first CI workflow** — `f001823` (feat)
2. **Task 2: Retire the credentialed smoke gate** — `5167485` (feat)
3. **Task 3: Write the testing map and record the standing rule** — `b9b2ce9` (docs)

_Note: all three tasks are `type="auto"` — no TDD RED/GREEN/REFACTOR split applies, no checkpoints in this plan._

## Files Created/Modified

- `.github/workflows/ci.yml` — new; the repository's first CI workflow
- `Makefile` — `test-portable` target added (single module-list owner); `test` now delegates to `test-portable` + `test-signal`; `smoke` target and its `.PHONY` entry removed
- `scripts/e2e-smoke.sh` — deleted (recoverable from git history)
- `README.md` — repository-layout line, dotenv section, and Testing section updated to reflect `make e2e` as the pre-ship gate, CI's existence, and a pointer to `docs/testing.md`
- `docs/testing.md` — new; the project's testing map
- `.claude/CLAUDE.md` — Conventions section now carries the D-11 standing rule instead of its placeholder text

## Decisions Made

See `key-decisions` in frontmatter — live-verified GitHub Actions versions (differing from the research skeleton's own flagged guesses in three of five actions), two self-referential-grep comment rewordings, the Node 26 pin, and scoping `E2E_PW_INSTALL_FLAGS=--with-deps` to CI only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reworded two doc comments that tripped their own literal-string acceptance criteria**
- **Found during:** Task 1, running the task's own acceptance-criteria greps after writing `.github/workflows/ci.yml`
- **Issue:** A comment reading `Every uses: line below is an official actions/* action...` made `grep -c 'uses:' .github/workflows/ci.yml` count 6 instead of 5 (matching `grep -c 'uses: actions/'`); a comment explaining why `make test` isn't used, containing the literal word `test-signal`, made `grep -c 'test-signal' .github/workflows/ci.yml` return 1 instead of the required 0. Both are the same class of self-referential-grep gotcha 07.1-01-SUMMARY.md recorded for the literal word "webServer".
- **Fix:** Reworded both comments to convey the same meaning without the flagged literal substring (`Every step below invokes an official actions/* action...`; described the Signal module's suite without the compound identifier `test-signal`).
- **Files modified:** `.github/workflows/ci.yml`
- **Verification:** both greps now return the exact values the acceptance criteria require
- **Committed in:** `f001823` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (comment wording only; no behavioural change)
**Impact on plan:** None — cosmetic wording fix required purely to satisfy the plan's own literal acceptance-criteria greps against comments, not code.

## Issues Encountered

- The plan's own acceptance criterion `grep -c 'uses: actions/' .github/workflows/ci.yml` implicitly requires every `uses:`-containing line (including comments) to also match `actions/` — resolved per the deviation above rather than adjusting the criterion.
- `make e2e` and `go test ./...` (mockstrict module, and any `go test`/build invocation of a `package main`) leave stray untracked build artifacts (`plugins/mockstrict/mockstrict`, and `kernel/webui/build/.gitkeep` occasionally reported deleted by the SPA build) — removed/restored before each commit per this execution's own worktree instructions; none of these are plan work.

## User Setup Required

None for local development. Once this plan's commits land on `main`, the first actual push-triggered CI run is the one true verification of the workflow the plan itself calls out as impossible to perform locally (see the `pending` coverage row for D1/D13) — record that run's outcome (green/red, and if red, why) once observed; this SUMMARY predates that observation.

## Next Phase Readiness

- The repository now has CI, a single `make e2e` pre-ship gate, and a written testing map — the three artifacts D-13/D-10/D-12/D-11 required.
- `docs/testing.md`'s Standing rule section and the `.claude/CLAUDE.md` Conventions entry mean Phase 8 (WhatsApp) and any future UI-touching phase inherits an explicit, discoverable definition-of-done requirement: extend the e2e suite, or explain in the phase's own docs why a given UAT item cannot be ported.
- No blockers. All four gates (`make test`, `make test-portable`, `make dev-check`, `make e2e`) verified green locally, multiple times, across all three tasks of this plan.

---
*Phase: 07.1-browser-e2e-harness*
*Completed: 2026-08-09*
