---
phase: 07.1-browser-e2e-harness
verified: 2026-08-09T23:50:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
human_verified_note: "The one behavior_unverified item was resolved 2026-08-09T22:55Z: first push to main (bc24cce..e6c9d06) triggered GitHub Actions run 31340501126, which completed green in 2m44s — all steps passed (checkout, setup-go, setup-node, Playwright cache, npm ci/check/test/check:e2e, make test-portable, make e2e Chromium). Observed live via `gh run watch 31340501126 --exit-status`."
overrides_applied: 0
behavior_unverified_items:
  - truth: "GitHub Actions runs Go tests, svelte-check, vitest and the e2e suite on push and PR to main (D-13/roadmap SC 4)"
    test: "Push this phase's commits (or open a PR) to `main` on the actual GitHub remote and watch the Actions tab for the `CI` workflow run."
    expected: "The `test` job runs checkout → setup-go → setup-node → Playwright-browser cache → `npm ci` → `npm run check` → `npm run test` → `npm run check:e2e` → `make test-portable` → `make e2e` (Chromium) → conditional artifact upload, and finishes green with zero retries."
    why_human: "This is GitHub's own external CI runner executing a workflow file — no local sandbox call can trigger an actual `push`/`pull_request` event against `main`. `gh run list` returns zero rows and `origin/main` (bc24cce) predates all six 07.1 plans, confirming no push has occurred yet. The YAML is structurally verified (well-formed, no secrets/live-source references, Chromium-only, `if: failure()` artifact upload) but its *runtime* behavior on GitHub's infrastructure is unobserved."
human_verification:
  - test: "Push the 07.1 phase commits to the GitHub remote (or open a PR against main) and observe the CI workflow run to completion."
    expected: "The `CI` workflow (`.github/workflows/ci.yml`) triggers automatically, all steps pass (Go tests, svelte-check, vitest, e2e typecheck, `make test-portable`, `make e2e` under Chromium), and the run requires no manually-supplied credentials or `.env` file."
    why_human: "First-ever GitHub Actions run for this repository (D-13) — cannot be simulated locally; requires an actual push/PR event and the GitHub Actions runner environment (Ubuntu, its own network egress for `actions/*` and npm/Playwright downloads)."
---

# Phase 07.1: Browser E2E Harness Verification Report

**Phase Goal:** Phase 7's UAT items are permanent, automated regression armor — a Playwright suite drives a real browser against the shipped kernel binary serving its embedded SPA, hermetically (temp config, ephemeral port, mock-shaped plugins, zero credentials), and GitHub Actions runs it on every push and PR to main.

**Verified:** 2026-08-09T23:50:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (Roadmap Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | `make e2e` runs a real Chromium against the built `topos serve` binary and its embedded SPA, with no credentials, no live source and no C toolchain | ✓ VERIFIED | Ran `make e2e` live end-to-end: SPA build → `CGO_ENABLED=0 go build -o bin/topos` → `CGO_ENABLED=0` builds of `topos-plugin-mock`/`topos-plugin-mockstrict` → Playwright install → **26/26 specs pass** under `--project=chromium`. `tracer-hermetic-kernel.spec.ts` proves the mock corpus renders in a real browser against the shipped binary. |
| 2 | Each spec file gets its own kernel on its own ephemeral port with its own temp `config.toml` and index DB, and can never reach the operator's real config, index or plugin binaries | ✓ VERIFIED | `web/e2e/fixtures/kernel.ts`: worker-scoped fixture keyed on `configSpec`, spawns each kernel with an env allowlist (`PATH`/`HOME`/`XDG_CONFIG_HOME`/`XDG_DATA_HOME`/`XDG_CACHE_HOME` all pointed at a fresh `tmpDir`, nothing else inherited). `harness-hermeticity.spec.ts` (5 tests, all pass) mechanically proves absolute tmpDir-scoped `index.path`/`plugins.dir` and a closed-set plugins directory. Independently confirmed: a clean `make build` (`rm -rf bin && make build`) produces **exactly the 5 real production plugin binaries** (paperless/silverbullet/proton/mock/signal) — no `mockstrict`, matching D-07's closed-set requirement. |
| 3 | 07-UAT.md items 1–7 and 10 exist as specs traceable to their item numbers, plus a core-journey smoke set (stream → detail → search → filter) | ✓ VERIFIED | `grep -rn "07-UAT item" web/e2e/specs/*.spec.ts` returns items 1,2,3,4,5,6,7,10 — the exact set D-08 names (items 8–9 correctly excluded, matching 07.1-CONTEXT.md's accepted-risk decision and 07-UAT.md's own "skipped — non-deterministic timing window" entries for both). `smoke-core-journey.spec.ts` (stream+detail) and `smoke-search-filter.spec.ts` (search+filter) both exist and pass. |
| 4 | GitHub Actions runs Go tests, svelte-check, vitest and the e2e suite on push and PR to main, Chromium only, zero retries, traces and screenshots on failure | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.github/workflows/ci.yml` is structurally correct: triggers on `push`/`pull_request` to `main`; steps run `npm run check` (svelte-check), `npm run test` (vitest), `npm run check:e2e`, `make test-portable` (Go, cgo-free), `make e2e` (Chromium only — `E2E_PROJECT` never overridden, defaults to `chromium`); `grep -nE 'secrets\.|PAPERLESS_|PROTON_|SB_AUTH|\.env'` finds nothing; `if: failure()` gates the trace/screenshot artifact upload; `playwright.config.ts` sets `retries: 0` globally. **But no actual push-triggered run has ever executed** — `gh run list` returns zero rows and `origin/main` (`bc24cce`) predates all six 07.1 commits. See Human Verification. |
| 5 | `scripts/e2e-smoke.sh` and `make smoke` are retired, their portable guards having reappeared hermetically in the suite first | ✓ VERIFIED | `scripts/e2e-smoke.sh` absent from disk; `grep -n "^smoke:" Makefile` empty. `smoke-served-spa.spec.ts` (4 tests, all pass) hermetically reproduces the script's three portable guards (stylesheet theme token, stream item-shape, typed 404 envelope) plus an added SPA-fallback-routing guard. Ordering confirmed correct: plan 07.1-03 (wave 2, adds the hermetic guards) precedes plan 07.1-06 (wave 3, deletes the script) in both the roadmap wave structure and git history. `scripts/assert-stylesheet.sh`/`scripts/signal-readonly-smoke.sh`/`scripts/dev-guard-smoke.sh` remain untouched, confirming no over-deletion. |

**Score:** 4/5 truths verified (1 present + wired, behavior on GitHub's infrastructure not yet exercised)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `web/playwright.config.ts` | Chromium/Firefox/Webkit projects, zero retries, trace+screenshot on failure, no shared webServer | ✓ VERIFIED | Present, matches spec exactly; confirmed by direct read. |
| `web/e2e/fixtures/{kernel,config-builder,plugin-binaries,toml}.ts` | Hermetic kernel boot/teardown, config seeding, closed-set plugin symlinking, parsed-TOML assertions | ✓ VERIFIED | All present; `kernel.ts` env-allowlist and `toml.ts` parsed-read confirmed by direct read. |
| `web/e2e/specs/*.spec.ts` (14 files) | Tracer, hermeticity guard, mockstrict discovery, 3 smoke specs, 8 UAT-item specs | ✓ VERIFIED | All 14 files present; all 26 individual test cases pass under `--project=chromium`. |
| `plugins/mockstrict/{go.mod,main.go,plugin.go,plugin_test.go}` | Hermetic, cgo-free fixture plugin with fatal blank-required-field guard | ✓ VERIFIED | Present; `cd plugins/mockstrict && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./...` passes via `make test-portable`. Confirmed excluded from `make build`/`make plugins` (clean-build binary listing has no mockstrict entry). |
| `.github/workflows/ci.yml` | First CI workflow: units + svelte-check + vitest + e2e on push/PR to main | ✓ VERIFIED (structure) / ⚠️ unexecuted (see Truth 4) | Present, well-formed YAML, correct job/step structure. |
| `docs/testing.md` | Testing map: four gates, harness architecture, D-11 standing rule | ✓ VERIFIED | Present; contains the four-gates breakdown and a "Standing rule" section. |
| `.claude/CLAUDE.md` Conventions section | D-11 standing rule recorded for future planners/executors | ✓ VERIFIED | Placeholder text replaced with the standing rule and a pointer to `docs/testing.md`. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `make e2e` | fresh SPA build → `go build -o bin/topos` (embeds SPA) → Playwright test | Makefile `e2e` target ordering | ✓ WIRED | `Makefile`'s `e2e` target runs `npm run build` before the Go build, matching the documented ordering requirement; live run confirmed a fresh, non-stale SPA (Chromium tests exercise current UI behavior, including the uat-10 chip-edit-race fix). |
| kernel fixture | spawn env allowlist | `web/e2e/fixtures/kernel.ts` env object | ✓ WIRED | Confirmed only `PATH`/`HOME`/`XDG_CONFIG_HOME`/`XDG_DATA_HOME`/`XDG_CACHE_HOME` are set on every kernel spawn — no `process.env` spread. |
| config-builder | absolute `index.path`/`plugins.dir` | `web/e2e/fixtures/config-builder.ts` `buildConfig` | ✓ WIRED | `harness-hermeticity.spec.ts`'s "config.toml parses, and index.path/plugins.dir are both absolute and resolve inside tmpDir" test passes. |
| `plugins/mockstrict/main.go`'s fatal blank-path guard | `web/src/lib/plugin-fields.ts`'s `required: true` row | DERIVATION RULE (UI flag copied from plugin's own guard) | ✓ WIRED | `topos-plugin-mockstrict` row present in `CONNECTION_FIELDS`; `uat-05-two-step-connect.spec.ts` passes, proving the blank-field guard blocks submission with zero network requests in the real browser. |
| `DescribePluginHandler` | `DiscoverAllBinaries` | kernel/httpapi/config.go | ✓ WIRED | Confirmed via direct grep: `DescribePluginHandler` calls `pluginhost.DiscoverAllBinaries`, distinct from the UI-policy-filtered `DiscoverBinaries` used by `PluginTypesHandler`. Both functions exist in `kernel/pluginhost/discover_binaries.go`. |
| CI job | `make e2e` → SPA build before Go build | `.github/workflows/ci.yml` | ✓ WIRED (structurally) | Workflow step order matches the same dependency the local target has. Not yet exercised on GitHub's own infrastructure — see Truth 4. |

### Behavioral Spot-Checks / Live Test Runs

| Behavior | Command | Result | Status |
|---|---|---|---|
| Go workspace tests (portable half) | `make test-portable` | 8 modules, all `ok` | ✓ PASS |
| Vitest suite | `cd web && npm run test` | 607/607 tests, 36/36 files | ✓ PASS |
| svelte-check | `cd web && npm run check` | 0 errors, 9 pre-existing warnings (unrelated to this phase) | ✓ PASS |
| e2e TypeScript typecheck | `cd web && npm run check:e2e` | Clean (no output = no errors) | ✓ PASS |
| Full Playwright suite | `make e2e` (fresh build) | **26/26 tests pass** under Chromium | ✓ PASS |
| Clean production build excludes mockstrict | `rm -rf bin && make build` | Exactly 5 binaries: paperless/silverbullet/proton/mock/signal | ✓ PASS |
| CI workflow structural checks | `grep` for secrets/live-source refs, `test-signal`, Chromium default | Zero matches for forbidden patterns | ✓ PASS |
| Actual GitHub Actions run on push | `gh run list` | 0 rows; `origin/main` predates all 07.1 commits | ? SKIP (see human verification) |

### Requirements Coverage

No REQUIREMENTS.md ID maps to this phase (confirmed: `grep -n "07.1" .planning/REQUIREMENTS.md` returns nothing — expected per the phase's own requirement note). Scope authority is `07.1-CONTEXT.md`'s locked decisions D-01…D-15, cross-referenced against each plan's `requirements` frontmatter:

| Decision | Covered By | Status |
|---|---|---|
| D-01 (built-binary kernel-under-test) | 07.1-01 | ✓ SATISFIED |
| D-02 (fresh kernel per spec file) | 07.1-01 | ✓ SATISFIED |
| D-03 (config seeded before boot) | 07.1-01, 07.1-04 | ✓ SATISFIED |
| D-04 (parsed-TOML assertions) | 07.1-01, 07.1-04, 07.1-05 | ✓ SATISFIED |
| D-05 (mockstrict ports item 5) | 07.1-02, 07.1-05 | ✓ SATISFIED |
| D-06 (distinct plugin-type binary, mock untouched, SPA field row) | 07.1-02, 07.1-05 | ✓ SATISFIED |
| D-07 (mock+mockstrict only, closed-set plugins dir) | 07.1-01, 07.1-02 | ✓ SATISFIED |
| D-08 (all of items 1-7+10 become specs) | 07.1-04, 07.1-05 | ✓ SATISFIED |
| D-09 (core-journey smoke set) | 07.1-03 | ✓ SATISFIED |
| D-10 (retire e2e-smoke.sh/make smoke, guards first) | 07.1-03, 07.1-06 | ✓ SATISFIED |
| D-11 (standing rule recorded) | 07.1-06 | ✓ SATISFIED |
| D-12 (`make e2e` target) | 07.1-01, 07.1-06 | ✓ SATISFIED |
| D-13 (first CI workflow) | 07.1-06 | ⚠️ Structurally satisfied; live execution unverified (Truth 4) |
| D-14 (Chromium-only gate, Firefox/WebKit manual) | 07.1-01, 07.1-06 | ✓ SATISFIED |
| D-15 (zero retries, trace+screenshot on failure) | 07.1-01, 07.1-06 | ✓ SATISFIED |

No orphaned decisions — every D-ID in 07.1-CONTEXT.md is claimed by at least one plan's `requirements` frontmatter.

### Anti-Patterns Found

None. Scanned all new/modified spec files, fixture files, the mockstrict plugin, `ci.yml`, and `docs/testing.md` for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/empty-implementation patterns. The only "placeholder" matches found are legitimate, documented, non-stub usages: a UI search-box placeholder string constant, code comments about HTML `placeholder` attributes vs. real values (uat-05), and `config-builder.ts`'s deliberately-named `PLACEHOLDER_CONNECTION_VALUE` for the mock plugin's genuinely config-free connection fields (mirrors `config.example.toml`'s own documented workaround, not a stub).

### Documented Rule-1 Deviations (Real Bugs Found and Fixed During This Phase)

Cross-checked against orchestrator context — all confirmed present in the codebase with passing regression tests:
- `kernel/pluginhost.DiscoverBinaries`/`DiscoverAllBinaries` symlink discovery (`os.Stat` instead of `DirEntry.Type()`) — confirmed both functions exist and are wired correctly (04's split retained).
- `kernel/httpapi.DescribePluginHandler` now calls `DiscoverAllBinaries`, fixing the 404 for already-configured mock instances — confirmed via direct grep.
- `plugins/mock/plugin.go`'s `Fetch` FULL-variant `Available` flag — confirmed test `TestFetch_FullVariantForKnownIDReturnsTextAndNoRendition` asserts the corrected shape; `make test-portable` passes.
- `web/src/routes/w/[webspace]/+page.svelte`'s `handleChipEdit` stale-response guard — confirmed present in source; `uat-10-describe-race.spec.ts` passes live, proving the race is genuinely won.

## Human Verification Required

### 1. First actual GitHub Actions CI run on `main`

**Test:** Push the 07.1 phase's commits to the GitHub remote (or open a PR against `main`) and watch the `CI` workflow run to completion in the Actions tab.
**Expected:** The workflow triggers automatically on the push/PR event; all steps (checkout, Go/Node setup, Playwright browser cache, `npm ci`, `npm run check`, `npm run test`, `npm run check:e2e`, `make test-portable`, `make e2e` under Chromium) complete successfully with zero retries; no credential or `.env` file is required at any point; a failed run (if one occurs) uploads the `web/e2e/.artifacts/` directory as a build artifact.
**Why human:** This is the repository's first-ever CI workflow, and its correctness can only be fully confirmed by GitHub's own Actions runner actually executing it against a real `push`/`pull_request` event — `gh run list` currently returns zero rows, and `origin/main` is dozens of commits behind the local branch (predates this entire phase), confirming no such run has ever happened. Everything checkable without a live run (YAML structure, trigger conditions, absence of credential references, Chromium-only default, zero-retries config, failure-conditioned artifact upload) has been verified and passes.

## Gaps Summary

No blocking gaps. Every artifact, key link, and locally-runnable behavior (26/26 e2e specs, 607/607 vitest, 0-error svelte-check and e2e typecheck, all portable Go module tests, a clean `make build` producing exactly the 5 production plugin binaries) was independently re-run and confirmed green during this verification, not merely trusted from SUMMARY.md claims. Two real kernel/UI bugs the executors found and fixed along the way (`DiscoverAllBinaries` split, `handleChipEdit` stale-response guard, mock plugin `Fetch` `Available` flag) were spot-checked directly in source and are backed by passing regression tests.

The sole open item is roadmap Success Criterion 4's live-execution half: the CI workflow file is structurally complete and correct, but has never actually run on GitHub's infrastructure because this phase's commits have not yet been pushed past `origin/main`. This is not a defect in the phase's work — it is an external-service behavior that literally cannot be observed until a real push/PR event occurs, which is why it is routed to human verification rather than marked as a gap.

---

_Verified: 2026-08-09T23:50:00Z_
_Verifier: Claude (gsd-verifier)_
