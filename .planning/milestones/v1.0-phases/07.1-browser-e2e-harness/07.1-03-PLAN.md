---
phase: 07.1-browser-e2e-harness
plan: 03
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - web/e2e/specs/smoke-core-journey.spec.ts
  - web/e2e/specs/smoke-search-filter.spec.ts
  - web/e2e/specs/smoke-served-spa.spec.ts
autonomous: true
requirements: [D-09, D-10]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A regression that stops the stream rendering, stops the detail pane loading content, stops search returning or highlighting, or stops source filter chips filtering, fails the suite (D-09)."
    - "The portable guards `scripts/e2e-smoke.sh` carried — the served SPA links a real stylesheet containing the theme token, stream items carry a well-formed link shape, and an unknown webspace answers a typed 404 envelope — exist hermetically, with no credentials and no live source, BEFORE that script is deleted (D-09, D-10)."
  artifacts:
    - web/e2e/specs/smoke-core-journey.spec.ts
    - web/e2e/specs/smoke-search-filter.spec.ts
    - web/e2e/specs/smoke-served-spa.spec.ts
  key_links:
    - "This plan is D-10's precondition: `scripts/e2e-smoke.sh` may not be deleted (plan 07.1-06) until its stylesheet, stream-shape and 404-envelope assertions live here."
    - "Stream assertions -> waitForFirstSync: the boot-time refresh has to land before any item count is meaningful, or the spec is a coin flip."
---

<objective>
Ship the core-journey smoke set: the armor Phase 8's stream and detail-pane churn actually needs, plus hermetic replacements for the portable guards the retiring `scripts/e2e-smoke.sh` carried.

Purpose: the UAT ports in plans 07.1-04 and 07.1-05 pin specific Phase 7 defects. This plan pins the four things that must never break at all — stream renders, detail opens, search finds and highlights, filters filter — and clears D-10's blocker by rehoming the three assertions that were the only reason to keep a credentialed smoke script.

Output: three spec files, all green under Chromium, all credential-free.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.1-browser-e2e-harness/07.1-CONTEXT.md
@.planning/phases/07.1-browser-e2e-harness/07.1-PATTERNS.md
@.planning/phases/07.1-browser-e2e-harness/07.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `web/e2e/specs/smoke-core-journey.spec.ts` | new file | stream + detail pane |
| `web/e2e/specs/smoke-search-filter.spec.ts` | new file | search + source filter chips |
| `web/e2e/specs/smoke-served-spa.spec.ts` | new file | absorbs `scripts/e2e-smoke.sh`'s portable guards |

<tasks>

<task type="auto">
  <name>Task 1: Core journey — the stream renders seeded items and the detail pane opens with content</name>
  <files>web/e2e/specs/smoke-core-journey.spec.ts</files>
  <read_first>
    - `web/e2e/fixtures/kernel.ts`, `web/e2e/fixtures/config-builder.ts`, `web/e2e/fixtures/toml.ts` — the fixture API 07.1-01 shipped; read the current signatures rather than assuming them.
    - `web/e2e/specs/tracer-hermetic-kernel.spec.ts` — the established spec shape and locator conventions to follow.
    - `plugins/mock/plugin.go` — the fixed four-item corpus this spec asserts against: source ids `1`-`4`, titles `Welcome to the mock source`, `Standup: Tuesday`, `Standup: Wednesday`, `Shopping list`; labels `demo` (all four), `meeting` (items 2 and 3), `errands` (item 4); `mockFullText` per id; `Fetch` returns `available: false` with `noRenditionReason` for every variant; `matchVocabulary` is `["labels"]`.
    - `web/src/lib/components/StreamRow.svelte` — each row is a `<button>` carrying `aria-pressed`, with the title in a `<p>` and a metadata line; this is the click target.
    - `web/src/lib/components/StreamList.svelte` — the branch order: sync-failure is checked strictly BEFORE empty, so a failed sync never renders as `Nothing here yet`.
    - `web/src/lib/components/DetailPane.svelte` — the selected item's title renders in an `<h2>`; extracted text renders in a `whitespace-pre-wrap` paragraph; a missing rendition renders `No preview available`.
    - `web/src/routes/w/[webspace]/+page.svelte` — the two-pane layout, `selectedId` state, the document title format `{webspace} — webspaces`, and how a row click drives the detail fetch.
    - `docs/api.md` `GET /api/items/{id}` and `GET /api/items/{id}/content` — the detail-fetch contract behind the pane.
  </read_first>
  <action>
Write `web/e2e/specs/smoke-core-journey.spec.ts` seeded with two `topos-plugin-mock` instances (ids `mock-01` and `mock-02`, display names `Mock One` and `Mock Two`) and one webspace `smoke` whose `keywords` is `["demo"]`, so every corpus item participates through both instances.

Wait for the boot-time sync of both instances to land before asserting anything about stream contents.

Assert, in this order:
  1. Navigating to `/w/smoke` renders the webspace name in the switcher trigger and sets the document title to `smoke — webspaces`.
  2. The stream renders eight rows — the mock corpus is four items and both instances contribute independently under the kernel's `{source}:{source_id}` stable-ID scheme, so a regression that silently merges two instances into one shows up here as four. Assert the count, not merely "at least one": a bare non-empty check is exactly the vacuous shape this repo's `e2e-smoke.sh` header comment warns about.
  3. All four mock titles are visible.
  4. Clicking the row titled `Welcome to the mock source` selects it (`aria-pressed` becomes true) and the detail pane heading shows that same title.
  5. The detail pane renders the item's extracted text — assert on a distinctive substring of `mockFullText["1"]`, read from `plugins/mock/plugin.go`, not a paraphrase.
  6. Because the mock offers no rendition for any variant, the detail pane shows its no-preview treatment alongside the extracted text rather than an error state. Assert the no-preview copy is present AND that no error alert is rendered — proving the "available: false with a reason" path is treated as the normal outcome the plugin contract says it is, not as a failure.
  7. Selecting a second row moves the detail pane to that row's title and clears the first row's pressed state.

Use user-facing locators throughout (`getByRole('button', { name })`, `getByRole('heading')`, `getByText`). The SPA carries no `data-testid` attributes and this phase adds none — if a locator is genuinely ambiguous, prefer scoping with `page.getByRole('main')` or a `locator(...).filter(...)` chain over introducing a test id.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/specs/smoke-core-journey.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx playwright test e2e/specs/smoke-core-journey.spec.ts --project=chromium` exits 0.
    - Behaviour: the spec asserts an exact stream row count of 8, not a `toBeGreaterThan` bound.
    - Behaviour: the spec asserts the detail heading text equals the clicked row's title, for two different rows in sequence.
    - Behaviour: the spec asserts a distinctive substring of the mock plugin's own `mockFullText` entry for item `1` is visible in the detail pane.
    - `grep -c 'getByTestId' web/e2e/specs/smoke-core-journey.spec.ts` is 0.
    - `cd web && npm run check:e2e` exits 0.
    - `git diff --stat HEAD -- web/src` is empty (no shipped UI file was touched to make a locator work).
  </acceptance_criteria>
  <done>A browser proves the stream renders both instances' items and the detail pane loads real content for a clicked row, with an exact expected count rather than a non-empty check.</done>
</task>

<task type="auto">
  <name>Task 2: Core journey — search returns and highlights, source filter chips filter</name>
  <files>web/e2e/specs/smoke-search-filter.spec.ts</files>
  <read_first>
    - `web/e2e/specs/smoke-core-journey.spec.ts` — the fixture seeding and conventions Task 1 just established; reuse the same two-instance shape.
    - `web/src/lib/components/SearchBox.svelte` — the input's placeholder comes from `searchCopy.placeholder` and the clear control's accessible name from `searchCopy.clearLabel`; the input is debounced in the route, never disabled here.
    - `web/src/lib/format.ts` — `searchCopy` (`placeholder`: `Search this webspace`, `clearLabel`: `Clear search`), `noMatchesHeading`, `searchVariant`, and `highlightText`'s matching semantics.
    - `web/src/lib/components/SearchResults.svelte` — the idle/loading/error/empty/results variants and which copy each renders.
    - `web/src/app.css` — the single `.search-highlight` declaration (06-05 promoted it out of a component-scoped style block precisely so stream and detail share one vocabulary; a spec asserting on this class is asserting on that shared contract).
    - `web/src/lib/components/SourceChip.svelte` — the filter toggle is the chip's own `<button>` carrying `aria-pressed`, whose accessible name is the instance's `display_name`; the refresh and menu controls are separate buttons named `Refresh {name}` and `Edit {name}`.
    - `web/src/lib/components/WebspaceHeader.svelte` — the chip row renders only when at least one instance is configured, chips render in config-declared order and are scoped to this webspace's participants, and `Clear filters` appears only while a selection is active.
    - `web/src/lib/components/StreamEmpty.svelte` — the filtered-empty variant's distinct copy versus the unfiltered `Nothing here yet` copy.
    - `docs/api.md` `GET /api/webspaces/{webspace}/search` — the search contract behind the box.
  </read_first>
  <action>
Write `web/e2e/specs/smoke-search-filter.spec.ts` with the same two-mock-instance, one-webspace seeding as Task 1.

Search assertions:
  1. Typing `standup` into the search box (located by its placeholder copy) returns exactly the rows for mock corpus items `2` and `3`, once per instance — four results — and none of items `1` or `4`.
  2. At least one rendered element carries the shared highlight class and its text, case-insensitively, equals the typed term. Assert on the class the app actually ships (`search-highlight`), so the 06-05 shared-vocabulary contract is what breaks if a component reintroduces its own scoped style.
  3. Activating the clear control (accessible name from `searchCopy.clearLabel`) restores the full unfiltered stream to its original row count.
  4. A query that matches nothing (`zzzznomatch`) renders the no-matches copy the app derives from the query, not an error state and not the plain empty-stream copy — the two are deliberately different states and a regression that collapses them is exactly what this assertion catches.

Filter-chip assertions:
  5. Both chips render, named `Mock One` and `Mock Two`, in config-declared order.
  6. Clicking the `Mock One` chip sets its `aria-pressed` to true and narrows the stream to that instance's four rows; `Mock Two`'s chip stays unpressed.
  7. `Clear filters` appears while a chip is selected, and activating it restores both the unpressed state and the full row count.
  8. Selecting a chip whose instance contributes no matching items is not reachable with this corpus, so instead assert the complementary property: with `Mock One` selected, none of the visible rows attribute themselves to `Mock Two`. Read `StreamRow.svelte` for where the source display name is rendered and assert against that, so the assertion is about attribution rather than about a count that two identical corpora make ambiguous.

Do not drive the UI to build any precondition — the config is seeded before boot (D-03).
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/specs/smoke-search-filter.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx playwright test e2e/specs/smoke-search-filter.spec.ts --project=chromium` exits 0.
    - Behaviour: searching `standup` yields exactly 4 result rows and 0 rows titled `Shopping list`.
    - Behaviour: at least one element with the `search-highlight` class is present after searching, with text matching the query case-insensitively.
    - Behaviour: clicking a source chip flips its `aria-pressed` to `true` and the visible rows all attribute to that instance.
    - Behaviour: `Clear filters` restores the pre-filter row count exactly.
    - `grep -c 'search-highlight' web/e2e/specs/smoke-search-filter.spec.ts` is at least 1.
    - `grep -c 'getByTestId' web/e2e/specs/smoke-search-filter.spec.ts` is 0.
    - `cd web && npm run check:e2e` exits 0.
    - `git diff --stat HEAD -- web/src` is empty.
  </acceptance_criteria>
  <done>Search finds, highlights, clears and reports no-matches distinctly; chips filter by instance and clear cleanly — all proven in a browser against seeded data.</done>
</task>

<task type="auto">
  <name>Task 3: Rehome the retiring smoke script's portable guards, hermetically</name>
  <files>web/e2e/specs/smoke-served-spa.spec.ts</files>
  <read_first>
    - `scripts/e2e-smoke.sh` — in full. The three assertions this task must reproduce without credentials: the served SPA links a stylesheet that fetches non-empty and contains the theme token; every stream item has a well-formed `link` shape; and an unknown webspace answers `404` with a typed error envelope. Also read its own comment explaining WHY it pre-checks the port — that reasoning is what the fixture's readiness gate already carries.
    - `scripts/assert-stylesheet.sh` — the exact theme token it greps for (`#020617`) and the other stylesheet properties it checks; this task reproduces the theme-token check, which is the one guarding G-01-2 (the SPA shipping zero real CSS).
    - `docs/api.md` — the envelope convention and the error-code table, specifically `webspace_not_found`.
    - `plugins/mock/plugin.go` — the per-item `Fidelity` values (`EXACT` for items 1 and 2, `CONVERSATION_ONLY` for item 3, `ANCHORED` for item 4) and the `http://localhost/...` deep links, so the item-shape assertion matches the corpus rather than paperless's old all-`exact` expectation.
    - `web/e2e/fixtures/kernel.ts` — how a spec reaches the fixture's `baseURL` for direct API requests, and Playwright's `request` fixture for non-browser HTTP calls.
  </read_first>
  <action>
Write `web/e2e/specs/smoke-served-spa.spec.ts`, seeded with one `topos-plugin-mock` instance and one webspace whose keywords match the whole corpus. This spec is the reason `scripts/e2e-smoke.sh` becomes deletable, so every assertion below must be genuinely equivalent to its bash ancestor, not a weaker gesture at it.

  1. Served-SPA stylesheet guard (the G-01-2 recurrence guard): fetch `/`, parse the returned HTML for a `<link rel="stylesheet">` href, normalise the adapter-static relative href (`./_app/...`) to an absolute path exactly as the bash script does, fetch that stylesheet, and assert the response is 200, the body length is greater than zero, and the body contains the `#020617` theme token. Fail with a message naming G-01-2 if the link is absent, matching the bash script's own failure copy in intent.
  2. Stream item-shape guard: request `GET /api/webspaces/<name>/stream` directly and assert the payload has a non-empty `items` array, that every item's `link.url` starts with `http://localhost/`, and that every item's `link.fidelity` is one of the corpus's three declared values. Do NOT assert every item is `exact` — that was true only of the paperless corpus the old script ran against; asserting it here would encode a false invariant.
  3. Typed 404 envelope guard: request `GET /api/webspaces/does-not-exist/stream` and assert the status is 404 and the parsed body matches an error envelope whose `error.code` is `webspace_not_found`.
  4. SPA fallback routing guard (new, and cheap): assert that a deep client-side route such as `/w/<name>` served directly by the kernel returns 200 HTML rather than a 404, proving the embedded SPA's `200.html` fallback is wired — the mechanism every other spec's `page.goto` silently depends on.

Add a file-level comment recording that this spec is the hermetic successor to `scripts/e2e-smoke.sh`, naming the three assertions it inherited, so a future reader can trace why the script was removed rather than rediscovering it from git history.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/specs/smoke-served-spa.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx playwright test e2e/specs/smoke-served-spa.spec.ts --project=chromium` exits 0.
    - Behaviour: the spec fetches the linked stylesheet over HTTP and asserts its body contains `#020617`; deleting that token from `web/src/app.css` and rebuilding would fail this spec.
    - Behaviour: `GET /api/webspaces/does-not-exist/stream` asserted as status 404 with `error.code` equal to `webspace_not_found`.
    - Behaviour: every stream item's `link.url` asserted to start with `http://localhost/`.
    - Behaviour: a direct request to `/w/<seeded-webspace>` asserted to return 200.
    - `grep -c '020617' web/e2e/specs/smoke-served-spa.spec.ts` is at least 1.
    - `grep -c 'webspace_not_found' web/e2e/specs/smoke-served-spa.spec.ts` is at least 1.
    - `cd web && npm run check:e2e` exits 0.
    - `scripts/e2e-smoke.sh` still exists and is unmodified by this plan (its removal belongs to plan 07.1-06).
  </acceptance_criteria>
  <done>Every portable guard the credentialed smoke script carried now runs hermetically in the Playwright suite, unblocking D-10's deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec assertions -> shipped behaviour | The suite's verdict is the only automated signal a Phase 8 change did not break Phase 7's UI; a weak assertion is a silently missing gate. |
| Test kernel HTTP surface -> spec `request` calls | Specs read the same loopback API the browser does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-04 | Repudiation | smoke assertions | high | mitigate | Exact expected counts (8 rows, 4 search results) rather than non-empty bounds; every stream assertion gated behind `waitForFirstSync`; the no-matches state asserted as distinct from the empty-stream state so a collapse of the two cannot pass. This is the specific defect class `scripts/e2e-smoke.sh`'s own stale-listener comment records burning a session. |
| T-07.1-12 | Repudiation | D-10 retirement precondition | high | mitigate | The three inherited guards are individually named in acceptance criteria, and `scripts/e2e-smoke.sh` must still exist and be unmodified when this plan completes — the deletion is deferred to 07.1-06 so the replacement is provably green first. |
| T-07.1-13 | Tampering | shipped SPA source | medium | mitigate | `git diff --stat HEAD -- web/src` must be empty in Tasks 1 and 2 — a spec may not reach for a locator by editing the application, which is how a test suite quietly becomes the reason the app has markup it does not need. |
</threat_model>

<verification>
- All three spec files pass under Chromium.
- `make e2e` still exits 0 with the full suite (tracer, hermeticity, discovery if 07.1-02 has landed, and these three).
- No file under `web/src/` or `plugins/` was modified by this plan.
- Every spec runs with no `.env`, no credentials, and no outbound network access.
</verification>

<success_criteria>
- Stream, detail, search, highlight and source filtering each have a browser-level regression gate (D-09).
- The stylesheet, item-shape and 404-envelope guards run hermetically, clearing D-10's precondition.
- Assertions are exact where the fixture makes exactness possible; no assertion passes on an empty or partially-synced index.
</success_criteria>

<output>
Create `.planning/phases/07.1-browser-e2e-harness/07.1-03-SUMMARY.md` when done. Record any assertion that had to be weakened relative to its `scripts/e2e-smoke.sh` ancestor and exactly why, since plan 07.1-06 deletes that script on the strength of this plan's coverage.
</output>
