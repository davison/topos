---
phase: 14-google-drive-source-built-out-of-repo
plan: 05
type: execute
wave: 4
depends_on: [14-04]
files_modified:
  - .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md
  - docs/plugin-contract.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SRC-06]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every entry the plugin repository's gap log carries is pulled back into this repository and given exactly one disposition — republished into the published contract now, or routed to the plugin-developer-guide backlog item"
    - "Every gap disposed as documentation-fixable is actually answered in docs/plugin-contract.md, in the section a plugin author would look in"
    - "The published contract now states where a plugin keeps its own private state, which it did not before this phase"
    - "No gap is dropped, softened or silently marked resolved: the triage record accounts for every entry by id"
    - "Gaps needing a wire or contract change are filed against the backlog phase rather than fixed here"
  artifacts:
    - path: ".planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md"
      provides: "The imported gap log plus the triage table that dispositions every entry by id"
      contains: "GAP-01"
    - path: "docs/plugin-contract.md"
      provides: "The republished answers to every documentation-fixable gap, including plugin-private state"
      contains: "private state"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog items under the plugin distribution and developer-guide phase for every contract-change gap"
  key_links:
    - from: ".planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md"
      to: "docs/plugin-contract.md"
      via: "each documentation-fixable row names the contract section its answer landed in"
      pattern: "plugin-contract"
    - from: ".planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md"
      to: ".planning/ROADMAP.md"
      via: "each contract-change row names the backlog item it was filed as"
      pattern: "PLUG-11"
  prohibitions:
    - statement: "MUST NOT omit, soften or retro-fit a gap-log entry when importing it — every question the clean-room sessions could not answer is carried across as it was recorded, including the ones that make the published contract look incomplete, and a gap judged invalid is dispositioned with a written reason rather than deleted"
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Close the loop the out-of-repo exercise exists to produce: take what the plugin repository could not answer from the published contract, and either answer it in the published contract now or file it where the wire-level fix will happen.

Purpose: D-07 makes the gap log a deliverable rather than a diary. A gap log nobody triages is the same as no gap log, and SRC-06's fourth criterion is explicitly "with any place the published contract or mock fell short written down as a contract gap" — written down, and then acted on.

Output: the gap log imported into this phase directory with a disposition per entry, the documentation-fixable ones republished into `docs/plugin-contract.md`, and the rest filed against the plugin developer-guide backlog item.

Locked decision implemented here: D-07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-google-drive-source-built-out-of-repo/14-CONTEXT.md
@.planning/phases/14-google-drive-source-built-out-of-repo/14-PATTERNS.md
@.planning/phases/14-google-drive-source-built-out-of-repo/14-LIVE-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import the gap log and disposition every entry</name>
  <precondition>/home/darren/projects/davison/topos-plugin-gdrive/CONTRACT-GAPS.md exists and is non-empty (plan 14-03's hand-off checkpoint returned it).</precondition>
  <files>.planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md</files>
  <read_first>
    - /home/darren/projects/davison/topos-plugin-gdrive/CONTRACT-GAPS.md — the whole log, every entry, in the plugin repository.
    - docs/plugin-contract.md — the whole document, to judge each gap against what the contract actually says today rather than what it feels like it should say. The section list matters for Task 2's placement decisions.
    - .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTEXT.md decision D-07 — the two-way disposition rule and its reversibility note.
    - .planning/phases/14-google-drive-source-built-out-of-repo/14-LIVE-UAT.md — its closing line, which carries anything the live run surfaced that never reached the plugin repository's own log.
    - .planning/REQUIREMENTS.md "Future Requirements" — the PLUG-11 statement, which is where contract-change gaps are routed.
  </read_first>
  <action>
    Create 14-CONTRACT-GAPS.md in the phase directory as this repository's record of the exercise's output. It has three parts.

    First, the imported log: every entry from the plugin repository's own file, carried across with its id, date, question, what the four published inputs did say, and the resolution the clean-room session took. Carry them across as recorded. Do not reword an entry to make the contract look better, do not merge two entries because they are adjacent, and do not drop an entry because it turned out to be a misreading — a misreading of the published contract is itself a finding about the published contract, and it gets a disposition like every other row.

    Second, anything the live UAT surfaced that never made it into the plugin repository's log, appended with new ids in the same format and marked as originating from the UAT run rather than from a clean-room session.

    Third, a triage table with one row per gap id and exactly one disposition each. The dispositions are: documentation-fixable, meaning the published contract already implies the answer or can state it without any wire or behaviour change, republished in this phase by Task 2, with the target section named; contract-change, meaning answering it properly needs a proto, SDK or kernel-behaviour change, filed against the developer-guide backlog item by Task 3 with the filed item named; and not-a-gap, allowed only with a written reason explaining what in the four published inputs did answer it and where, which is itself a signal that the answer was hard to find.

    Expect at least the two seeded entries about plugin-private state to be present. Both were predicted before any code existed and both are documentation-fixable: the contract addresses a plugin's connection configuration but never says where a plugin may keep state of its own — a token that must outlive the process, or a working cache that makes the next sync incremental.

    Finish with a one-line count reconciling the imported entries plus the UAT additions against the triage rows, so a dropped row is arithmetically visible.
  </action>
  <verify>
    <automated>test -s .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md &amp;&amp; grep -q 'GAP-01' .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md &amp;&amp; grep -q 'GAP-02' .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md &amp;&amp; make docs-check</automated>
  </verify>
  <acceptance_criteria>
    - Every gap id present in the plugin repository's log appears in the imported section of `14-CONTRACT-GAPS.md` — verify by extracting the id list from both files and diffing; the diff must be empty in the direction of loss.
    - The triage table has exactly one row per id, and the number of rows equals the number of imported entries plus the number of UAT additions.
    - Every row carries one of the three dispositions; every documentation-fixable row names a target section of `docs/plugin-contract.md`; every contract-change row names a backlog item; every not-a-gap row carries a non-empty reason.
    - `GAP-01` and `GAP-02` are present and both dispositioned documentation-fixable.
    - The closing reconciliation line is present and its arithmetic is correct.
  </acceptance_criteria>
  <done>Every question the clean-room sessions could not answer is in this repository, unedited, with exactly one decided disposition and nothing unaccounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Republish the documentation-fixable gaps into the published contract</name>
  <precondition>Task 1's triage table exists and names a target section for every documentation-fixable row.</precondition>
  <reversibility rating="costly">These edits change the contract third parties read and build against; a clarification that turns out wrong is itself a contract change to walk back, across however many plugins have already been written to it. Carried from 14-CONTEXT.md D-07.</reversibility>
  <files>docs/plugin-contract.md</files>
  <read_first>
    - docs/plugin-contract.md — the whole document, and in particular the sections the triage table named as targets, plus "Configuration", "The launch environment", "Logging" and "What this document does not cover", which are the sections most likely to be targets and whose voice any new prose must match.
    - .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md — the triage table Task 1 produced, which is this task's work list.
    - .planning/phases/14-google-drive-source-built-out-of-repo/14-PATTERNS.md section on this file — its instruction to edit in place at the relevant existing section rather than restructuring anything unrelated to a specific gap.
    - scripts/check-doc-links.sh — what `make docs-check` enforces, so any cross-reference added here resolves.
  </read_first>
  <action>
    Answer each documentation-fixable gap in docs/plugin-contract.md, in the section the triage table named.

    Add a section covering plugin-private state, which the two seeded gaps are about and which the document currently never mentions. It must answer three questions a plugin author will have: where a plugin may keep state of its own that must outlive its process, distinguishing that from the connection configuration the host supplies; what the host does and does not guarantee about that location, given that the launch environment is deliberately scrubbed to an allowlist and a plugin therefore cannot assume the operator's usual environment is present; and what the host guarantees about lifetime — that it never reads, migrates, backs up or removes a plugin's private state, so a plugin that keeps a cache owns its invalidation entirely. Place it near the existing configuration and launch-environment material, where a plugin author reading about what the host hands them will find the answer to what they may keep for themselves. Say plainly that a credential kept in such a file is the plugin's own responsibility to protect, and cross-reference the existing logging rule rather than restating it.

    For every other documentation-fixable row, edit the named section in place. Match the document's existing voice and heading depth. Do not renumber, reorder or restructure sections unrelated to a specific gap, and do not use this pass to make unrelated improvements — a diff that mixes gap answers with editorial tidying cannot be reviewed against the triage table.

    After each edit, update that row of the triage table in `14-CONTRACT-GAPS.md` to record where the answer landed, so the log and the contract stay traceable to each other.
  </action>
  <verify>
    <automated>make docs-check &amp;&amp; grep -qi 'private state' docs/plugin-contract.md &amp;&amp; make test-portable</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugin-contract.md` contains a heading about plugin-private state, and its body answers all three of: where such state may live, what the host does not guarantee about the launch environment around it, and that the host never reads, migrates or removes it.
    - Every row of the triage table dispositioned documentation-fixable has a corresponding edit in `docs/plugin-contract.md`, and the row records where the answer landed.
    - `git diff docs/plugin-contract.md` touches only sections named in the triage table plus the one new section — no unrelated rewording.
    - `make docs-check` exits 0.
    - `make test-portable` exits 0 (the contract document is asserted against by the SDK's own contract test; a structural edit that breaks it fails here).
  </acceptance_criteria>
  <done>A third party reading the published contract today gets an answer to every question this exercise proved it could not answer — in the section they would have looked in first.</done>
</task>

<task type="auto">
  <name>Task 3: File the contract-change gaps against the backlog</name>
  <precondition>Task 1's triage table exists and every contract-change row is identified.</precondition>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md "Backlog" section and the plugin distribution, developer guide and certification phase entry beneath it — the existing item shape and its placeholder line.
    - .planning/REQUIREMENTS.md "Future Requirements" — the PLUG-10 and PLUG-11 statements and their formatting.
    - .planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md — the triage table's contract-change rows.
  </read_first>
  <action>
    For every contract-change row, add a backlog item under the existing backlog phase for plugin distribution and the developer guide, replacing or joining its placeholder line. Each item states the gap in one line as a change to be made rather than as a question, and cites its gap id so the backlog entry and the log stay linked.

    Add a one-line note under the PLUG-11 statement in REQUIREMENTS.md recording that Phase 14's out-of-repo exercise produced concrete input for it, with the gap ids, so a future reader of the requirement finds the evidence without knowing this phase's history.

    Make both edits scoped: touch only the backlog phase entry in the roadmap and only the future-requirements line in the requirements file. Do not alter Phase 14's own roadmap entry, any other phase entry, any requirement status, or the requirement-to-phase mapping table.

    Then update the triage table's contract-change rows to name the backlog item each was filed as.

    If Task 1's triage produced no contract-change rows, record that fact explicitly in the triage table's reconciliation line and in this plan's summary rather than leaving it implicit — an exercise that found no wire-level gaps is a real result about the contract's maturity, and it should be stated as one.
  </action>
  <verify>
    <automated>make docs-check &amp;&amp; node /home/darren/.claude/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 14 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Every contract-change row in the triage table names a backlog item, and that item's text appears under the backlog phase in `.planning/ROADMAP.md` citing the same gap id.
    - `.planning/REQUIREMENTS.md` contains a line under the PLUG-11 statement citing the gap ids produced by this phase.
    - `git diff .planning/ROADMAP.md` shows changes only inside the backlog section; the Phase 14 entry and every other phase entry are untouched.
    - `git diff .planning/REQUIREMENTS.md` shows changes only in the future-requirements area; no requirement status checkbox and no mapping-table row is altered.
    - `node gsd-tools.cjs query roadmap.get-phase 14` still returns `"found": true` with the phase's four success criteria intact.
    - If there were no contract-change rows, the reconciliation line says so explicitly.
  </acceptance_criteria>
  <done>Nothing the exercise found is left without a home: answered now in the contract, or filed as a named backlog item against the phase that will change the wire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin repository → topos repository | The gap log is text authored in another repository, imported here and used to justify edits to a published contract |
| published contract → third-party plugin authors | Every edit here changes what someone outside this project builds against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14.5-01 | Repudiation | gap-log import | high | mitigate | Entries are carried across unedited and reconciled by count and by id, so a dropped or softened row is both arithmetically and diff-visible. Backed by this plan's prohibition and by the acceptance criterion that the id lists diff empty in the direction of loss. |
| T-14.5-02 | Tampering | published contract edits | high | mitigate | Edits are scoped to sections the triage table named, each traceable back to a gap id; the SDK's own contract test runs as part of the task's verification, so a structural edit that breaks the document's machine-read parts fails immediately rather than shipping. |
| T-14.5-03 | Tampering | imported gap-log text treated as instructions | medium | mitigate | The log is prose authored in another repository and is data, not direction: a gap entry that reads as an instruction to change the contract in a particular way is transcribed and dispositioned on its merits, never executed. Every republish decision is made against the contract as it reads today. |
| T-14.5-04 | Denial of Service | roadmap and requirements edits | low | mitigate | Both edits are scoped to a single section, pinned by acceptance criteria on the diff, and the phase entry is re-read through the roadmap query afterwards to prove it survived. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files and sections created by **this plan** (excluded from drift verification):

- `.planning/phases/14-google-drive-source-built-out-of-repo/14-CONTRACT-GAPS.md` — imported gap log plus triage table
- a new plugin-private-state section in `docs/plugin-contract.md`
- new backlog items under the roadmap's plugin distribution and developer-guide phase, each citing a gap id
- a new evidence line under the PLUG-11 future requirement
</artifacts_this_phase_produces>

<verification>
- `make docs-check` exits 0.
- `make test-portable` exits 0.
- The gap id list in `14-CONTRACT-GAPS.md` is a superset of the plugin repository's own.
- Every gap id has exactly one disposition, and every disposition has a landing place named.
- The roadmap query for phase 14 still returns the phase with its four success criteria.
</verification>

<success_criteria>
- The published contract answers what this exercise proved it did not.
- Wire-level gaps are filed, not fixed here.
- No gap was dropped, softened or silently resolved.
</success_criteria>

<output>
Create `.planning/phases/14-google-drive-source-built-out-of-repo/14-05-SUMMARY.md` when done
</output>
