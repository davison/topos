---
phase: 09-ui-polish-and-source-management-rework
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - web/src/lib/components/SourceChip.svelte
  - web/src/lib/components/chip-edit-menu.test.ts
  - web/src/lib/components/source-chip-tooltip.test.ts
  - web/e2e/specs/09-chip-menu.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reachable source chip's tooltip reads \"{name} — synced 5 minutes ago\" and \"{name} — synced yesterday\" — the relative phrase is used verbatim with no literal suffix appended, so no variant can read doubled."
    - "E9 populated: the de-duplicated copy is correct for every Intl.RelativeTimeFormat numeric-auto output — \"5 minutes ago\", \"yesterday\", \"last week\", \"now\" — because the formatter's own phrase is used verbatim with no literal suffix."
    - "A source chip reveals exactly one trailing hover/focus-visible control: the overflow trigger. The standalone refresh button is gone."
    - "E1 populated: the chip menu reads Refresh now, separator, Edit connection…, Edit match settings…, optional Re-link…, separator, Remove from this webspace."
    - "E1 loading: Refresh now is disabled and its icon spins while the source is syncing — refreshing something already mid-refresh is a no-op the standalone button never guarded."
    - "E1 zero-one-many: the Re-link… item's existing WhatsApp gate is unchanged — still present only for the one plugin type that can re-link."
    - "SourceChip's public prop shape is unchanged — onrefresh and onedit keep the exact signatures WebspaceHeader already threads through, including the measurement clone's no-op handlers."
  artifacts:
    - web/src/lib/components/source-chip-tooltip.test.ts
    - web/e2e/specs/09-chip-menu.spec.ts
  key_links:
    - "formatRelativeTime's numeric-auto output -> tooltipText branches (used verbatim, never suffixed)"
    - "the removed standalone refresh Button's onrefresh(source.name) call -> the new first DropdownMenuItem's onSelect"
    - "WebspaceHeader's measurement clone -> SourceChip's unchanged prop shape (the chip gets narrower; the clone re-measures automatically)"
---

<objective>
Fix the two shipped defects on the source chip: a literal " ago" appended to a
phrase that already carries its own word, and a second hover-revealed control
where the design calls for one.

Both are corrections to an already-shipped component with no contract change —
`SourceChip`'s props are identical before and after, so `WebspaceHeader` and
its overflow measurement clone need no edit at all.

Dependency on 09-01 is a real file dependency: the tracer adds the plugin
identity icon to this same component.

Purpose: ROADMAP success criterion 1, two of its eight named fixes.
Output: correct tooltip copy across every relative-time variant, and a chip
with a single trailing affordance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md
@.planning/phases/09-ui-polish-and-source-management-rework/09-01-SUMMARY.md
@docs/testing.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Location |
|---|---|---|
| `web/src/lib/components/source-chip-tooltip.test.ts` | vitest guard over every relative-time tooltip variant | `web/src/lib/components/` |
| `web/e2e/specs/09-chip-menu.spec.ts` | Playwright spec | `web/e2e/specs/` |

<precondition>
09-01 has landed its identity-icon change to `SourceChip.svelte`. Assert with
`grep -c 'PluginIcon' web/src/lib/components/SourceChip.svelte` returning at
least 2; halt if absent — this plan edits the same file and must build on that
state rather than reverting it.
</precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stop appending a literal suffix to an already-complete relative phrase</name>
  <files>web/src/lib/components/SourceChip.svelte, web/src/lib/components/source-chip-tooltip.test.ts</files>
  <read_first>
    - web/src/lib/components/SourceChip.svelte — the `tooltipText` derived block and its five branches (syncing, success, warning, destructive, unknown/default)
    - web/src/lib/format.ts's formatRelativeTime — confirm it builds on Intl.RelativeTimeFormat with numeric 'auto', which is what makes the special-cased outputs ("yesterday", "last week", "now") complete phrases in their own right
    - web/src/lib/format.test.ts — the existing coverage of formatRelativeTime, so the new test complements it rather than re-testing the formatter
    - .planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md Fix 3's before/after table and the Copywriting Contract's first three rows — the exact target strings per branch
  </read_first>
  <behavior>
    - The success branch renders the display name, an em-dash separator, the word synced, and the formatter's phrase verbatim — with no additional word appended after it
    - The warning branch renders the display name, an em-dash separator, the words last error, the formatter's phrase verbatim, a colon, and the source's last error text
    - The destructive branch is unchanged — it reads "unreachable since {relative}" and never had the defect
    - The unknown/default branch is unchanged — it reads "not yet synced" and uses no relative phrase at all
    - The syncing branch is unchanged and is still evaluated before all four last-known-state branches
    - Every Intl.RelativeTimeFormat numeric-auto output shape produces a well-formed sentence in the success branch: a numeric delta such as "5 minutes ago", and the special-cased "yesterday", "last week" and "now"
  </behavior>
  <action>
Remove the trailing literal word appended after `${relative}` in
`SourceChip.svelte`'s `tooltipText` success and warning branches. The
formatter already returns a complete phrase — `Intl.RelativeTimeFormat` with
`numeric: 'auto'` yields "5 minutes ago" for a numeric delta but also
"yesterday", "last week" and "now" for its special-cased deltas, so the
appended word was wrong in *every* case, not just the visible ones. "synced
yesterday" and "synced now" were latent instances of the identical bug, hit
less often only because those exact deltas are hit less often.

Target strings, per 09-UI-SPEC.md's Copywriting Contract: the success branch
becomes the display name, an em dash, the word `synced`, and the formatter's
phrase; the warning branch becomes the display name, an em dash, the words
`last error`, the formatter's phrase, a colon, and `source.last_error`. Leave
the destructive, unknown and syncing branches byte-identical — none of them
had the defect.

`formatRelativeTime` itself needs no change. The bug is purely the extra
literal at the call site; changing the formatter would break every other caller.

Write `web/src/lib/components/source-chip-tooltip.test.ts` covering the
`<behavior>` list. Drive it by exercising the branch logic against real
`formatRelativeTime` outputs for a numeric delta and for each of the
special-cased deltas, so the test asserts the *sentences a user sees* rather
than merely that a substring was deleted from the source. That distinction is
what makes this test catch a future regression that reintroduces the suffix in
only one branch.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos/web && npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` exits 0 with every `<behavior>` case present in source-chip-tooltip.test.ts, including the yesterday, last-week and now variants
    - `grep -c 'unreachable since' web/src/lib/components/SourceChip.svelte` equals 1 — the destructive branch is untouched
    - `grep -c 'not yet synced' web/src/lib/components/SourceChip.svelte` equals 1 — the unknown branch is untouched
    - `git diff web/src/lib/format.ts` is empty — the formatter was not changed
    - `cd web && npm run check` reports 0 errors
  </acceptance_criteria>
  <reversibility rating="reversible">
    Two template strings.
  </reversibility>
  <done>
    Every tooltip variant reads as a correct sentence, proven against the
    formatter's real special-cased outputs rather than against the source text.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold refresh into the overflow menu, leaving one trailing control</name>
  <files>web/src/lib/components/SourceChip.svelte, web/src/lib/components/chip-edit-menu.test.ts</files>
  <read_first>
    - web/src/lib/components/SourceChip.svelte — the whole markup block: the filter button, the standalone RefreshCw Button with its handleRefreshClick stopPropagation handler, and the DropdownMenu with its trigger and four items
    - web/src/lib/components/chip-edit-menu.test.ts — the whole file. Three of its existing assertions encode the pre-fix state and must be updated as part of this change, not treated as regressions: the aria-label prefix assertion, the exactly-one-DropdownMenuSeparator assertion, and the exact menu-item label set equality
    - web/src/lib/components/WebspaceHeader.svelte lines 110-290 — the chip row, the invisible measurement clone that passes no-op handlers, and the visibleChipCount measurement effect. This file is deliberately NOT in this plan's files_modified: the chip's props are unchanged and the clone re-measures the narrower chip automatically
    - .planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md Fix 5 — the new menu order, the syncing guard, the aria-label change, and the sub-44px exception the single remaining trigger continues to carry
  </read_first>
  <behavior>
    - The standalone refresh Button is gone; the chip reveals exactly one trailing hover/focus-visible control
    - Refresh now is the first item in the dropdown menu, with a leading refresh icon
    - Refresh now is disabled while the source is syncing, and its icon spins while syncing
    - Refresh now calls the component's existing onrefresh with the source name — the same call the removed button made
    - The menu order is: Refresh now, separator, Edit connection…, Edit match settings…, optionally Re-link…, separator, Remove from this webspace
    - The menu contains exactly two separators
    - The Re-link… item stays gated on the existing WhatsApp source-type check
    - The overflow trigger's aria-label reads "{display_name} actions"
    - The trigger keeps its size-8 sub-44px sizing and its hover/focus-visible reveal treatment
    - SourceChip's props are unchanged — onrefresh and onedit keep their existing signatures
  </behavior>
  <action>
Remove the standalone `RefreshCw` `Button` from `SourceChip.svelte` entirely,
along with its now-unused `handleRefreshClick` handler. Keep the `RefreshCw`
icon import — it moves into the menu.

Add `Refresh now` as the **first** `DropdownMenuItem` in the existing menu,
with a leading `RefreshCw` icon that carries the spin animation class while
`source.syncing`, and with the item itself disabled while `source.syncing`.
That guard is new: refreshing something already mid-refresh is a no-op, and the
removed standalone button never had it. The item calls the component's existing
`onrefresh(source.name)` — no prop-shape change, so `WebspaceHeader` threads
`onrefresh`/`onedit` through exactly as it does today and needs no edit.

Insert a separator after `Refresh now`, producing the final order: `Refresh
now`, separator, `Edit connection…`, `Edit match settings…`, `Re-link…` when
the existing WhatsApp gate is satisfied, separator, `Remove from this
webspace`. The menu now carries two separators, not one.

Broaden the trigger's `aria-label` from the edit-only phrasing to
`{display_name} actions` — it is no longer edit-only. Keep everything else
about the trigger identical: the `size-8` sizing (the nested-inside-a-44px-pill
sub-floor exception 06-UI-SPEC.md G-06-3b and 07-UI-SPEC.md already establish,
now the chip's only sub-floor control), the hover and focus-visible reveal
classes, the selected-state colour overrides, and `handleEditClick`'s
stopPropagation-before-forwarding order — that ordering is the D-12 versus
Phase 6 D-01 collision guard and must not be disturbed.

Update `chip-edit-menu.test.ts`'s three now-stale assertions to the new state:
the aria-label assertion asserts the actions phrasing; the separator-count
assertion asserts two; the menu-item label set equality gains `Refresh now` as
its first member. Update them in place with a comment naming this plan and Fix
5 as the reason, following the precedent 07-11 set when it updated two config
tests that encoded a superseded rule. Add assertions for the syncing disable and
the spin class, which are new behaviour with no existing guard.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos/web && npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` exits 0 with every `<behavior>` case present across chip-edit-menu.test.ts
    - The menu-item label set assertion in chip-edit-menu.test.ts asserts set equality against exactly five labels with Refresh now first
    - The separator-count assertion in chip-edit-menu.test.ts asserts 2
    - `grep -c 'aria-label' web/src/lib/components/SourceChip.svelte` equals 1 — only the overflow trigger carries one now, since the refresh button that carried the second is gone
    - `grep -c 'onrefresh(source.name)' web/src/lib/components/SourceChip.svelte` equals 1 — the refresh call survived the move
    - `grep -c 'animate-spin' web/src/lib/components/SourceChip.svelte` equals 1
    - `git diff --stat web/src/lib/components/WebspaceHeader.svelte` is empty — the prop shape really was unchanged
    - `cd web && npm run check` reports 0 errors
  </acceptance_criteria>
  <reversibility rating="reversible">
    Markup relocation inside one component, with no contract change.
  </reversibility>
  <done>
    The chip has one trailing control, refresh lives at the top of its menu with
    a syncing guard it never had, and the component's public interface is
    provably unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Browser proof for the chip's single control, menu order and tooltip copy</name>
  <files>web/e2e/specs/09-chip-menu.spec.ts</files>
  <read_first>
    - web/e2e/specs/uat-06-remove-chip-roundtrip.spec.ts — the closest precedent: it seeds mock instances, opens the chip's overflow menu, and drives menu items
    - web/e2e/specs/uat-02-remove-source-items.spec.ts — chip interaction and post-action assertions against a hermetic kernel
    - web/e2e/fixtures/config-builder.ts — attachedWebspace and FixtureSourceSpec
    - docs/testing.md `## Writing a new spec` and `## Standing rule`
  </read_first>
  <action>
Write one spec covering the browser-drivable half of both fixes, per the
standing rule that a browser-drivable check becomes a spec.

Cover, as separate cases:

1. **One trailing control** — seed a mock instance, hover its chip, and assert
   exactly one trailing icon button is revealed inside the chip. Count the
   revealed controls rather than asserting a specific one is absent, so the
   assertion states the design rule ("exactly one") rather than the removal.
2. **Menu order** — open the chip's overflow menu and assert the visible menu
   item text sequence equals the expected order for a non-WhatsApp source:
   Refresh now, Edit connection…, Edit match settings…, Remove from this
   webspace. Assert the sequence, not merely membership — the ordering is the
   part Fix 5 specifies and the part a careless edit breaks.
3. **Refresh works from its new home** — click Refresh now and assert the
   source actually refreshes (the chip enters and leaves its syncing state, or
   a refresh request is observed), proving the relocated control is wired and
   not merely rendered.
4. **Tooltip copy** — after the first sync completes, hover the chip and assert
   its tooltip text contains the word synced followed by a relative phrase, and
   that no word appears twice in immediate succession in that string. A
   duplicate-adjacent-word assertion is the general form of this defect and
   catches the "yesterday ago" variant too, not just the common one.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos && make e2e E2E_ARGS="specs/09-chip-menu.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `make e2e E2E_ARGS="specs/09-chip-menu.spec.ts"` exits 0
    - The spec contains four distinct test cases, provable by `grep -c "test(" web/e2e/specs/09-chip-menu.spec.ts` being at least 4
    - The menu-order case asserts an ordered sequence of item texts, not a membership check
    - The trailing-control case asserts a count of revealed controls
    - The refresh case observes a real state change or request, not merely that the item is clickable
  </acceptance_criteria>
  <reversibility rating="reversible">
    Test-only.
  </reversibility>
  <done>
    A real browser proves the chip exposes one control, the menu is in the
    specified order, refresh still works from its new location, and no tooltip
    variant reads doubled.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → kernel | Refresh remains a POST that mutates sync state; relocating its trigger must not change who can invoke it or when. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-17 | Denial of Service | chip refresh trigger | low | mitigate | Relocating refresh into the menu could have removed its guard against repeated invocation. Task 2 adds a syncing-disabled guard the standalone button never had, so the new location is strictly harder to spam than the old one, and Task 3's spec exercises the real request path. |
| T-09-18 | Information Disclosure | chip tooltip | low | accept | The warning branch renders `source.last_error` verbatim into a tooltip. Unchanged from shipped behaviour; the kernel's own error text is already surfaced in the chip today and this fix only removes a redundant word. |
| T-09-19 | Tampering | shared file with 09-01 | low | mitigate | `SourceChip.svelte` is edited by both 09-01 and this plan. The precondition asserts 09-01's identity icon is present before this plan starts, so a revert cannot pass silently. |
</threat_model>

<verification>
- `cd web && npm run check && npm test` — 0 type errors, tooltip variants and menu structure guarded
- `make e2e E2E_ARGS="specs/09-chip-menu.spec.ts"` — one control, correct order, working refresh, non-doubled tooltip
- `git diff --stat web/src/lib/components/WebspaceHeader.svelte` empty — the chip contract really did not change
</verification>

<success_criteria>
- No tooltip variant reads "ago ago" or "yesterday ago"
- The chip reveals exactly one trailing control and refresh is the menu's first item, guarded against re-entry while syncing
- Both are permanent Playwright gates
</success_criteria>

<output>
Create `.planning/phases/09-ui-polish-and-source-management-rework/09-05-SUMMARY.md` when done
</output>
