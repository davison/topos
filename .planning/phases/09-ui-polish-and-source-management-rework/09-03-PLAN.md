---
phase: 09-ui-polish-and-source-management-rework
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - web/src/routes/+layout.svelte
  - web/src/lib/assets/favicon.svg
  - web/static/robots.txt
  - web/src/app.css
  - web/src/lib/components/ui/dropdown-menu/dropdown-menu-content.svelte
  - web/src/lib/components/ui/popover/popover-content.svelte
  - web/src/lib/popover-surface.test.ts
  - web/e2e/specs/09-static-assets-and-surfaces.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A browser tab showing topos displays the topos app icon as its favicon, not the SvelteKit-scaffold Svelte wordmark."
    - "robots.txt served by the kernel disallows all crawling, matching topos's local-personal-data premise even when an instance is LAN/tailnet-reachable."
    - "An open dropdown menu or popover renders on a visibly lighter surface than the pane behind it, and its own border frame stays legible against that new surface."
    - "Dialog and AlertDialog keep bg-card — they are already separated from the pane by their own full-screen scrim and are explicitly out of scope for the surface change."
  artifacts:
    - web/static/robots.txt with a full disallow
    - "--popover: #172033 declared in web/src/app.css"
    - web/src/lib/popover-surface.test.ts
    - web/e2e/specs/09-static-assets-and-surfaces.spec.ts
  key_links:
    - "web/static/app-icon.png -> served at /app-icon.png -> +layout.svelte's icon link"
    - "app.css --popover token -> Tailwind v4 @theme inline --color-popover -> bg-popover on dropdown-menu-content and popover-content"
    - "web/static/robots.txt -> npm run build -> kernel/webui/build/robots.txt -> go:embed -> the kernel's own served robots.txt"
---

<objective>
Land the three fixes that are pure static assets and one palette value: the
favicon becomes topos's own icon, robots.txt stops inviting crawlers into a
personal-data viewer, and floating menus stop being camouflaged against the
panes they open over.

Dependency on 09-01 is tracer-first sequencing (TRACER_MODE), not a code
dependency — this plan shares no file with 09-01 and imports nothing it
produces.

Purpose: ROADMAP success criterion 1, three of its eight named fixes.
Output: a topos-branded tab, a correct robots.txt, and one new real step on
the dark-elevation staircase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md
@docs/testing.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Location |
|---|---|---|
| `--popover: #172033` | CSS custom property value (token existed, value is new) | `web/src/app.css` |
| `web/src/lib/popover-surface.test.ts` | vitest source-scan guard | `web/src/lib/` |
| `web/e2e/specs/09-static-assets-and-surfaces.spec.ts` | Playwright spec | `web/e2e/specs/` |

Removed by this plan: `web/src/lib/assets/favicon.svg` (the SvelteKit-scaffold
Svelte wordmark; no other reference to it exists in `web/src`).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: topos favicon and a correct robots.txt</name>
  <files>web/src/routes/+layout.svelte, web/src/lib/assets/favicon.svg, web/static/robots.txt</files>
  <read_first>
    - web/src/routes/+layout.svelte — the whole file; it currently imports `$lib/assets/favicon.svg` and uses the imported URL in a `<link rel="icon">`
    - web/static/robots.txt — the three shipped lines, which are syntactically valid but semantically backwards for this app
    - .gitignore line 10 (`kernel/webui/build/*`) — establishing that the embedded build tree is a generated artifact, not a second source of truth to hand-edit
    - .planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md Fix 1 and Fix 6, plus the Copywriting Contract's robots.txt row for the exact comment wording
  </read_first>
  <action>
In `web/src/routes/+layout.svelte`, drop the `$lib/assets/favicon.svg` import
and point the icon link at the already-present `web/static/app-icon.png`, which
SvelteKit serves root-relative at `/app-icon.png`. Declare the link's type as
`image/png` and use the literal href `/app-icon.png` — a static-directory asset
is referenced by its served path, never imported.

Delete `web/src/lib/assets/favicon.svg`. It is the stock SvelteKit-scaffold
Svelte wordmark and nothing else in `web/src` references it; confirm that with
a repo-wide search before deleting rather than assuming it.

Replace `web/static/robots.txt` with the disallow-everything form locked in
09-UI-SPEC.md's Copywriting Contract: a two-line comment explaining that topos
is a local, personal-data tool and nothing served here is meant for public
indexing even if the instance is reachable on a LAN or tailnet, then
`User-agent: *` and `Disallow: /`.

Do **not** hand-edit `kernel/webui/build/robots.txt`. 09-UI-SPEC.md Fix 6 asks
for both copies to change together, and they do — but the mechanism is the
build, not a second edit: `kernel/webui/build/*` is gitignored, `npm run build`
regenerates it from `web/static/`, and `go build` then embeds the regenerated
tree. Hand-editing the build copy would be overwritten by the next build and
would create a second, divergent source of truth. Task 3's Playwright spec
proves the embedded copy is correct by fetching it from a real built kernel,
which is the assertion that actually matters.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos/web && npm run check && npm run build && grep -q '^Disallow: /$' ../kernel/webui/build/robots.txt && grep -q 'app-icon.png' ../kernel/webui/build/index.html ../kernel/webui/build/200.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -e web/src/lib/assets/favicon.svg` fails (the file is gone)
    - `grep -rn "favicon" web/src/` produces no output
    - `grep -c 'app-icon.png' web/src/routes/+layout.svelte` equals 1
    - `grep -c 'image/png' web/src/routes/+layout.svelte` equals 1
    - `grep -c '^Disallow: /$' web/static/robots.txt` equals 1
    - `grep -c '^User-agent: \*$' web/static/robots.txt` equals 1
    - After `npm run build`, `grep -c '^Disallow: /$' kernel/webui/build/robots.txt` equals 1 — the embedded twin tracks the source automatically
    - `cd web && npm run check` reports 0 errors
  </acceptance_criteria>
  <reversibility rating="reversible">
    Two static assets and one link tag.
  </reversibility>
  <done>
    The tab shows topos's own icon, the scaffold asset is gone, and both the
    source-tree and build-tree robots.txt disallow all crawling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give floating menus their own surface token</name>
  <files>web/src/app.css, web/src/lib/components/ui/dropdown-menu/dropdown-menu-content.svelte, web/src/lib/components/ui/popover/popover-content.svelte, web/src/lib/popover-surface.test.ts</files>
  <read_first>
    - web/src/app.css — the whole palette region: the `@theme inline` block mapping `--color-popover` to `--popover`, and BOTH declaration sites of `--popover` (around lines 70 and 155 — the token is declared twice and both must change together, or the two blocks disagree)
    - web/src/lib/scrollbar-theme.test.ts — the house comment-stripped source-scan guard over app.css, including its rationale for stripping comments before asserting; this task's new test follows it exactly
    - web/src/lib/components/ui/dropdown-menu/dropdown-menu-content.svelte and web/src/lib/components/ui/popover/popover-content.svelte — the single long class string in each, currently carrying `bg-card border-border`
    - web/src/lib/components/ui/dialog/dialog-content.svelte — its `bg-background/80` scrim and `bg-card` surface, which are deliberately NOT changed by this task
    - .planning/phases/09-ui-polish-and-source-management-rework/09-UI-SPEC.md Fix 8 and the Color table's "New — Popover/menu surface" row — the elevation-staircase argument for the specific value chosen
  </read_first>
  <behavior>
    - app.css declares --popover with a value distinct from --card at every declaration site
    - app.css's --popover value sits strictly between --card and --border in lightness, so the menu reads lighter than any pane behind it while its own border frame stays legible against it
    - dropdown-menu-content.svelte's surface class is bg-popover, not bg-card
    - popover-content.svelte's surface class is bg-popover, not bg-card
    - dialog-content.svelte still uses bg-card — the dialog carve-out is explicit, not accidental
    - border-border is unchanged in both menu components
  </behavior>
  <action>
Change `--popover` in `web/src/app.css` from its current value (byte-identical
to `--card`'s `#0f172a`, which is why the token's whole purpose was never
realised) to `#172033`. Change it at **both** declaration sites in the file —
the token appears twice and leaving one stale would make the surface depend on
cascade order.

This value is chosen to extend the palette's existing dark-elevation staircase
by one real step rather than introducing an unrelated hue: `--background`
`#020617`, then `--card` `#0f172a` for every pane surface, then the new
`--popover` `#172033`, then `--secondary`/`--border`/`--muted`/`--accent`
`#1e293b`. It sits strictly between `--card` and `--border` in lightness, which
is what makes both required properties true at once — the menu reads lighter
than the pane behind it, and `--border` stays lighter still so the menu's own
frame does not vanish into its fill. Do not "simplify" this to `#1e293b`: that
would collide with `--border` and erase the frame instead of fixing the
pane-collision problem.

In `dropdown-menu-content.svelte` and `popover-content.svelte`, swap the
surface utility from `bg-card` to `bg-popover` in each component's class
string. Leave `border-border` and every other utility in those strings
untouched.

Leave `dialog-content.svelte` and the AlertDialog primitives alone. Both
already render behind their own full-screen `bg-background/80` scrim, which
separates them from whatever pane sits behind them, so they correctly keep
`bg-card`. Guard that carve-out in the test rather than leaving it as a
comment.

Write `web/src/lib/popover-surface.test.ts` covering the whole `<behavior>`
list, following `scrollbar-theme.test.ts`'s comment-stripped source-scan
pattern — app.css is heavily commented in house style and a raw-text assertion
can be satisfied or tripped by prose. For the lightness-ordering assertion,
parse the four hex values out of the stripped source and compare computed
relative luminance rather than asserting the literal string `#172033`, so the
test encodes the *invariant* (card < popover < border) and not just today's
chosen value.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos/web && npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` exits 0 with every `<behavior>` case present in popover-surface.test.ts, including the card-less-than-popover-less-than-border luminance ordering
    - `grep -c '172033' web/src/app.css` equals 2 (both declaration sites changed)
    - `grep -c 'bg-popover' web/src/lib/components/ui/dropdown-menu/dropdown-menu-content.svelte` equals 1
    - `grep -c 'bg-popover' web/src/lib/components/ui/popover/popover-content.svelte` equals 1
    - `grep -c 'bg-card' web/src/lib/components/ui/dialog/dialog-content.svelte` equals 1 — the dialog carve-out is intact
    - `grep -c 'border-border' web/src/lib/components/ui/dropdown-menu/dropdown-menu-content.svelte` equals 1 — the frame utility is unchanged
    - `cd web && npm run check` reports 0 errors
  </acceptance_criteria>
  <reversibility rating="reversible">
    One palette value and two utility swaps.
  </reversibility>
  <done>
    Menus render on their own elevation step, dialogs are untouched, and the
    ordering invariant is guarded by a test that survives a future palette
    re-tune.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Browser proof for the favicon, robots.txt and menu surface</name>
  <files>web/e2e/specs/09-static-assets-and-surfaces.spec.ts</files>
  <read_first>
    - web/e2e/specs/smoke-served-spa.spec.ts — the existing spec that asserts on the kernel-served SPA shell; the closest precedent for asserting on document head and static routes
    - web/e2e/specs/uat-06-remove-chip-roundtrip.spec.ts — the FixtureConfigSpec / attachedWebspace / test.use idiom for seeding a mock instance
    - web/e2e/fixtures/kernel.ts — the KernelFixture surface (baseURL, the per-file kernel) and the exported test/expect
    - docs/testing.md `## Writing a new spec` and `## Standing rule` — the conventions and the project rule that any browser-drivable check becomes a spec rather than a manual note
  </read_first>
  <action>
Write one spec file covering all three of this plan's fixes against a real
built-and-embedded kernel, per the standing Phase 07.1 rule that a
browser-drivable check becomes a spec rather than a manual check.

Cover, as separate test cases:

1. **Favicon** — the served document's icon link resolves to `/app-icon.png`,
   and a direct request for that path returns 200 with an `image/png`
   Content-Type. Assert on the resolved link element in the served document,
   not on the SvelteKit source, so this proves the *embedded build* is correct.
2. **robots.txt** — a direct request to `/robots.txt` returns 200 and its body
   contains a `Disallow: /` line and a `User-agent: *` line. This is the
   assertion that makes hand-editing the build copy unnecessary: it proves the
   embedded twin tracks `web/static/`.
3. **Menu surface distinctness** — seed one mock instance, open the webspace
   switcher dropdown, and compare the computed `background-color` of the open
   menu's content element against the computed `background-color` of the header
   pane behind it. Assert they are different, and assert the menu's computed
   `border-color` differs from the menu's own computed background — the two
   properties Fix 8 exists to make simultaneously true. Compare resolved
   computed values, never class strings, so this catches a token that is
   declared but not actually applied.
  </action>
  <verify>
    <automated>cd /home/darren/projects/davison/topos && make e2e E2E_ARGS="specs/09-static-assets-and-surfaces.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `make e2e E2E_ARGS="specs/09-static-assets-and-surfaces.spec.ts"` exits 0
    - The spec contains three distinct test cases (favicon, robots.txt, menu surface), provable by `grep -c "test(" web/e2e/specs/09-static-assets-and-surfaces.spec.ts` being at least 3
    - The menu-surface case asserts on values read via getComputedStyle, provable by `grep -c 'getComputedStyle' web/e2e/specs/09-static-assets-and-surfaces.spec.ts` being at least 1
    - The robots.txt case asserts a 200 response body containing a full disallow, not merely that the route exists
  </acceptance_criteria>
  <reversibility rating="reversible">
    Test-only.
  </reversibility>
  <done>
    A real browser against a real embedded build proves the favicon, the
    robots.txt and the menu-versus-pane contrast, and each becomes a permanent
    regression gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → kernel HTTP surface | An instance may be reachable beyond loopback (the repo's own `make dev` exposes the Vite dev server to the tailnet), so what the app advertises to automated clients matters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Information Disclosure | `web/static/robots.txt` | medium | mitigate | The shipped file explicitly invites full crawling of a personal-data viewer (email, chats, documents) that may be LAN/tailnet-reachable. Task 1 replaces it with `Disallow: /` and Task 3 proves the embedded copy the kernel actually serves carries that line. Note honestly that robots.txt is advisory only and is defence in depth, not access control — topos's real access control remains its loopback-only default (`docs/api.md`), which this change does not weaken or substitute for. |
| T-09-12 | Information Disclosure | favicon asset | low | accept | `/app-icon.png` becomes fetchable by anything that can reach the instance. It is a project logo containing no user data. Accepted. |
| T-09-13 | Tampering | `web/src/app.css` palette | low | accept | A palette value change cannot cross a trust boundary. Guarded against silent regression by the luminance-ordering test rather than by a security control. |
</threat_model>

<verification>
- `cd web && npm run check && npm test` — 0 type errors, popover-surface invariants held
- `cd web && npm run build` then `grep '^Disallow: /$' kernel/webui/build/robots.txt` — the embedded twin regenerates correctly
- `make e2e E2E_ARGS="specs/09-static-assets-and-surfaces.spec.ts"` — all three fixes proven in a real browser against the embedded build
</verification>

<success_criteria>
- The browser tab shows topos, not Svelte
- The served robots.txt disallows everything
- An open menu is visibly distinct from the pane behind it, and its frame is still visible
- All three are permanent Playwright gates
</success_criteria>

<output>
Create `.planning/phases/09-ui-polish-and-source-management-rework/09-03-SUMMARY.md` when done
</output>
