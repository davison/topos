---
phase: 08-whatsapp-conversations-managed-risk
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/plugin-fields.ts
  - web/src/lib/components/QRPanel.svelte
  - web/src/lib/components/AddSourceModal.svelte
  - web/src/lib/components/RelinkModal.svelte
  - web/src/lib/components/SourceChip.svelte
  - web/src/routes/w/[webspace]/+page.svelte
  - web/src/lib/components/qr-panel.test.ts
  - web/src/lib/components/add-source.test.ts
  - web/src/lib/components/chip-edit-menu.test.ts
  - web/src/lib/components/relink.test.ts
  - web/src/lib/plugin-fields.test.ts
  - web/e2e/specs/uat-08-whatsapp-qr-link.spec.ts
  - docs/testing.md
autonomous: true
requirements: [SRC-03]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user adding a WhatsApp source sees the QR panel inline in the Add-Source Step 1 dialog when the trial launch reports the instance is not linked, and can configure, scan, and reach match settings in one continuous flow (D-02)"
    - "The source chip's menu offers a Re-link… entry for a WhatsApp instance, opening the SAME QR component in a small dialog — one component, two entry points (D-03)"
    - "The QR panel renders the rotating pairing image with the instruction line 'Scan with your phone to link' and a countdown line, driven by the kernel's own reported validity rather than a hardcoded interval"
    - "The QR panel covers loading, populated, error, expired and success states, and cancelling it never destroys work already typed into the Step 1 connection fields"
    - "A WhatsApp instance can be fully saved through Step 1 and Step 2 while still unpaired — the not-linked condition is offered as a QR opportunity, never treated as a trial-launch failure and never forced down the Save-anyway path (UI-SPEC E5 partial)"
    - "The Add-Source picker offers WhatsApp as a connectable plugin type with a Display Name field and a required Local Path field whose default matches config.example.toml"
    - "The Playwright e2e suite covers the WhatsApp QR flow hermetically — the link endpoint is intercepted at the route layer, so the suite never reaches WhatsApp's servers, a real account, or a real credential (Phase 07.1 standing rule, docs/testing.md)"
    - "UI-SPEC E5 (Add-Source Step 1, Connect WhatsApp) holds for its explicit state categories: empty (an unfilled form shows the Local Path placeholder and required-field validation blocks an empty submit, existing Phase 7 machinery unchanged), loading (the trial-launch in-progress state is inherited from the two-step modal unchanged), error (validation, trial-launch failure, hash-conflict and Save-anyway machinery are all inherited from Phase 7 unchanged), overflow (the same field and modal overflow rules as every other plugin type, no new rules), long-text (long paths and display names follow the existing text-input behaviour — scroll within the input, no reflow)"
    - statement: "UI-SPEC E5 partial — a WhatsApp instance can be fully saved (Step 1 trial-launch and Step 2 both succeed, since Describe carries only static vocabulary) while the device is still unpaired: fully configured yet functionally inert until linked, resolving into the existing not-linked health-dot state with no modal-level branching. Evidence: an explicit test confirming the Add-Source flow does not treat the not-linked condition as a trial-launch failure and does not force the Save-anyway path for an unpaired instance."
      verification: backstop
  artifacts:
    - path: "web/src/lib/components/QRPanel.svelte"
      provides: "The single QR pairing panel used by both entry points — poll loop, countdown, and the five states"
      contains: "Scan with your phone to link"
    - path: "web/src/lib/components/RelinkModal.svelte"
      provides: "The small dialog wrapping QRPanel for the chip menu's Re-link… entry"
      contains: "QRPanel"
    - path: "web/e2e/specs/uat-08-whatsapp-qr-link.spec.ts"
      provides: "Hermetic Playwright coverage of the QR flow with the link endpoint intercepted at the route layer"
      contains: "page.route"
  key_links:
    - from: "web/src/lib/components/QRPanel.svelte"
      to: "web/src/lib/api.ts"
      via: "the panel drives the link session through the typed start/poll/cancel client functions"
      pattern: "startWhatsAppLink|pollWhatsAppLink|cancelWhatsAppLink"
    - from: "web/src/lib/components/AddSourceModal.svelte"
      to: "web/src/lib/components/QRPanel.svelte"
      via: "Step 1's not-linked branch renders the panel inline beside ConnectionForm (D-02)"
      pattern: "QRPanel"
    - from: "web/src/lib/components/SourceChip.svelte"
      to: "web/src/routes/w/[webspace]/+page.svelte"
      via: "the Re-link… menu entry raises a new onedit kind the route handles by opening RelinkModal (D-03)"
      pattern: "relink"
---

<objective>
Build the in-app pairing surface the phase's users actually touch: one QR panel component, offered inline while adding a WhatsApp source and again from the source chip's Re-link… entry, plus the hermetic Playwright coverage Phase 07.1's standing rule requires of any UI work.

Purpose: D-01 chose the polished in-app flow. Plan 08-03 built the kernel side and wrote the UI contract; this plan renders it, and it is the last plan before the phase's user-visible promise ("link WhatsApp by scanning a QR code") is true.

Output: `QRPanel.svelte`, its two entry points, the WhatsApp row in the Add-Source picker, and a route-mocked e2e spec.
</objective>

<artifacts_this_phase_produces>
New symbols and files created by this plan (excluded from source-grounding drift verification):

- **New files:** `web/src/lib/components/QRPanel.svelte`, `web/src/lib/components/RelinkModal.svelte`, `web/src/lib/components/qr-panel.test.ts`, `web/src/lib/components/relink.test.ts`, `web/src/lib/plugin-fields.test.ts`, `web/e2e/specs/uat-08-whatsapp-qr-link.spec.ts`
- **New TypeScript identifiers:** `WhatsAppLinkState`, `WhatsAppLinkSession`, `StartWhatsAppLinkRequest`, `startWhatsAppLink`, `pollWhatsAppLink`, `cancelWhatsAppLink`
- **New plugin-fields entries:** a `topos-plugin-whatsapp` row in `CONNECTION_FIELDS` and a `topos-plugin-whatsapp` entry in `PLUGIN_TYPE_LABELS`
- **New chip-menu kind:** `'relink'` added to the `onedit` kind union in `SourceChip.svelte`, `WebspaceHeader.svelte` and the webspace route
- **New AddSourceModal state:** a `link` branch within the existing `step` machine
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-whatsapp-conversations-managed-risk/08-CONTEXT.md
@.planning/phases/08-whatsapp-conversations-managed-risk/08-UI-SPEC.md
@.planning/phases/08-whatsapp-conversations-managed-risk/08-PATTERNS.md
@.planning/phases/08-whatsapp-conversations-managed-risk/08-03-SUMMARY.md
@docs/testing.md
@docs/api.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The QR panel, and WhatsApp inline in the Add-Source flow (D-01/D-02)</name>
  <precondition>The three link routes exist and behave as Plan 08-03 Task 3's SUMMARY documents — confirm the request/response shapes recorded there before writing the API client, since this task's poll loop is written directly against them.</precondition>
  <files>web/src/lib/api.ts, web/src/lib/plugin-fields.ts, web/src/lib/components/QRPanel.svelte, web/src/lib/components/AddSourceModal.svelte, web/src/lib/components/qr-panel.test.ts, web/src/lib/components/add-source.test.ts, web/src/lib/plugin-fields.test.ts</files>
  <read_first>
    - `.planning/phases/08-whatsapp-conversations-managed-risk/08-UI-SPEC.md` — the whole "Amendment — In-App QR Panel" section Plan 08-03 Task 1 wrote: QR sizing, the instruction line, the countdown copy, the five states, and the per-entry-point success transition. This is the binding contract for this task
    - `.planning/phases/08-whatsapp-conversations-managed-risk/08-03-SUMMARY.md` — the exact request/response shapes and error codes of the three link routes
    - `web/src/lib/api.ts` — the `ApiError` class, `CONFIG_CONFLICT_MESSAGE`, and the shape of an existing typed client function such as `describePlugin`, including how it unwraps the error envelope
    - `web/src/lib/components/AddSourceModal.svelte` — the whole file: the `step` state union, `selectPluginType`, `handleConnectNext`, `saveAnyway`, `submitMatch`, `resetFlowState`, and the two Dialog blocks, so the QR branch is added alongside them rather than replacing any
    - `web/src/lib/plugin-fields.ts` — `CONNECTION_FIELDS`, its DERIVATION RULE comment, `ConnectionField.defaultValue` versus `placeholder`, `PLUGIN_TYPE_LABELS`, and `missingRequiredFields`
    - `plugins/whatsapp/main.go` — the pre-`goplugin.Serve` fatal guard that determines which fields are required, per the derivation rule (never copy a sibling row's shape)
    - `config.example.toml`'s `[sources.whatsapp]` block — the path default the field row must match
    - `web/src/lib/components/add-source.test.ts` — the house test pattern this task extends: comment-stripped source scanning, `extractBetween` scoping, a found-non-empty-source guard first, one consequence-describing message per assertion
    - `web/src/lib/components/ui/alert/index.js` and `ui/dialog/index.js` — the primitives the panel reuses; no new primitive is introduced
  </read_first>
  <action>
    **`api.ts`.** Add typed clients for the three link routes, following `describePlugin`'s shape exactly (same fetch helper, same `ApiError` unwrapping, same exported request/response interfaces): `startWhatsAppLink({plugin, path, instance?})`, `pollWhatsAppLink(sessionId)`, `cancelWhatsAppLink(sessionId)`. Export a `WhatsAppLinkState` union covering the states Plan 08-03 documented and a `WhatsAppLinkSession` interface carrying the session id, the state, the QR data URI and its remaining validity when present, and the error code and message when present. Mirror the kernel's field names verbatim rather than a sketch — the shipped handler and `docs/api.md` are authoritative, the same discipline every prior phase's client followed.

    **`plugin-fields.ts`.** Add the `topos-plugin-whatsapp` row to `CONNECTION_FIELDS`: `DISPLAY_NAME_FIELD`, then a Local Path field marked required with both a placeholder and a seeded `defaultValue` matching `config.example.toml`'s `[sources.whatsapp]` path, then `SYNC_INTERVAL_FIELD`. Derive the required flag by reading `plugins/whatsapp/main.go`'s own fatal guard as the DERIVATION RULE comment demands, and record that derivation in a comment on the row. Seed a `defaultValue` rather than a placeholder alone, because the standard install path is genuinely correct and a placeholder is not submittable — the same reasoning the Signal row's comment records. Add a `topos-plugin-whatsapp` entry to `PLUGIN_TYPE_LABELS` mapping to "WhatsApp". Create `plugin-fields.test.ts` if it does not already exist and assert the new row's shape and the label.

    **`QRPanel.svelte` (new).** One component, two entry points. Props: the plugin binary name, the store path, an optional instance id (present for Re-link, absent for Add-Source), and callbacks for paired and for cancelled. On mount it calls `startWhatsAppLink` and then polls `pollWhatsAppLink` on an interval derived from the session's own reported remaining validity — clamped to a sane floor so a short validity cannot produce a request storm — until a terminal state arrives. It calls `cancelWhatsAppLink` on unmount and on an explicit cancel, so navigating away never leaves a subprocess alive.

    Render exactly the five states the UI-SPEC amendment defines: loading before the first QR arrives; populated showing the square QR image plus the instruction line "Scan with your phone to link" and a countdown line in the amendment's "Refreshes in M:SS" shape; error rendering the kernel's own message through the existing destructive `Alert` with a retry control; expired saying the session expired and offering a restart without closing the surrounding modal; and success invoking the paired callback. Use only existing spacing, typography and colour tokens — the amendment introduces none, and the QR image is a plain raster, not a themed surface. Give the image a meaningful accessible name rather than leaving it decorative, since it is the only content of the populated state.

    **`AddSourceModal.svelte`.** Extend the existing `step` union with a `link` value; do not restructure the two Dialog blocks. In `handleConnectNext`, when the selected plugin type is WhatsApp and the trial launch succeeds, branch on whether the described instance reports itself linked: when it does not, move to the `link` step and render `QRPanel` inline inside the SAME Step 1 dialog beside the already-entered connection values, so the user configures, scans and reaches match settings in one continuous flow (D-02). On the panel's paired callback, advance to the existing `match` step with the vocabulary the trial launch already returned. On the panel's cancelled callback, return to `connect` with every typed value intact — cancelling out mid-link is a supported path and the existing "saved but not yet linked" outcome remains valid.

    The load-bearing constraint, and the one this task's own test pins: an unpaired WhatsApp instance is NOT a trial-launch failure. `Describe` carries only static match vocabulary and succeeds identically whether or not a device is paired, so `describeFailed` must stay false and the Save-anyway control must stay hidden on this path. The QR panel is an opportunity offered at that moment, never a gate — a user who cancels still reaches Step 2 and still saves a fully configured, functionally inert instance that resolves into the existing not-linked health-dot state.

    **Tests.** Extend `add-source.test.ts` in the house pattern with assertions scoped via `extractBetween` to the new `link` branch, and add `qr-panel.test.ts` in the same pattern over `QRPanel.svelte`. Cover: the panel is rendered from within the Step 1 dialog block rather than a new dialog; the instruction and countdown copy match the amendment; all five states have a rendering branch; the cancel path calls the cancel client; the unmount path calls the cancel client; and — the E5 evidence — the WhatsApp not-linked branch is reached without setting the describe-failure flag and without revealing the Save-anyway control, asserted positively by scoping to the branch and checking which state assignments it contains.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run check &amp;&amp; npm run test -- --run qr-panel add-source plugin-fields</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run check` reports 0 errors
    - `npm run test -- --run qr-panel` passes, including assertions that `QRPanel.svelte` renders branches for all five states and contains the exact instruction line "Scan with your phone to link"
    - `npm run test -- --run qr-panel` passes an assertion that both the explicit cancel path and the unmount path invoke `cancelWhatsAppLink`
    - `npm run test -- --run add-source` passes an assertion, scoped to the WhatsApp not-linked branch, that the branch sets the step to the link value and assigns neither the describe-failure flag nor the Save-anyway reveal
    - `npm run test -- --run add-source` passes an assertion that `QRPanel` is rendered inside the existing Step 1 dialog block, not a newly added Dialog
    - `npm run test -- --run plugin-fields` passes assertions that the `topos-plugin-whatsapp` row marks Local Path required, seeds a `defaultValue` equal to `config.example.toml`'s `[sources.whatsapp]` path, and that the label resolves to "WhatsApp"
    - `web/src/lib/api.ts` exports `startWhatsAppLink`, `pollWhatsAppLink` and `cancelWhatsAppLink` with field names matching `docs/api.md`
    - The Add-Source picker offers WhatsApp — provable by running `make dev` with the whatsapp binary present and opening the "+" picker
  </acceptance_criteria>
  <done>A user picks "New WhatsApp…", fills in a path, and — when the device is unpaired — scans a rotating QR right there in Step 1 before moving on to match settings; cancelling still saves a configured-but-unlinked instance.</done>
</task>

<task type="auto">
  <name>Task 2: Re-link… from the source chip's menu (D-03)</name>
  <files>web/src/lib/components/SourceChip.svelte, web/src/lib/components/RelinkModal.svelte, web/src/routes/w/[webspace]/+page.svelte, web/src/lib/components/chip-edit-menu.test.ts, web/src/lib/components/relink.test.ts</files>
  <read_first>
    - `web/src/lib/components/SourceChip.svelte` — the whole file: the `onedit` prop's kind union, the three existing `DropdownMenuItem` entries and the separator, `handleEditClick`'s stopPropagation-before-callback discipline, and the `busy` prop's deliberately narrow scope
    - `web/src/lib/components/WebspaceHeader.svelte` — the `onedit` prop declaration and its type, both real chip render sites, and the measurement-clone site that passes a no-op `onedit`
    - `web/src/routes/w/[webspace]/+page.svelte` — `handleChipEdit` and its stale-response guard, the `editInstance`/`editMode`/`editVocabulary` state, and the keyed `EditSourceModal` render block
    - `web/src/lib/components/EditSourceModal.svelte` — the dialog composition, prop shape, and `onclose`/`onsaved` convention `RelinkModal` should match
    - `web/src/lib/components/QRPanel.svelte` — the component Task 1 built, and its props and callbacks
    - `web/src/lib/api.ts` — `SourceStatus`, specifically its `source_type` field, which is the discriminator gating the new menu entry
    - `.planning/phases/08-whatsapp-conversations-managed-risk/08-UI-SPEC.md`'s amendment — the Re-link entry-point behaviour and its success transition
    - `web/src/lib/components/chip-edit-menu.test.ts` — the house pattern and the existing exact-menu-contents assertions this task must update rather than bypass
  </read_first>
  <action>
    **`SourceChip.svelte`.** Widen the `onedit` kind union to include a relink value and add a "Re-link…" `DropdownMenuItem`. Render it only when `source.source_type` is the WhatsApp source type — every other plugin has nothing to re-link, and an inert menu entry is worse than an absent one. Place it above the separator alongside the two existing edit entries; the separator and the destructive remove entry keep their positions and the remove entry keeps its exclusive claim on the `busy` gate. Route the new item through the identical `onedit` callback the other entries use, so the stopPropagation-before-callback discipline that keeps a menu open from toggling the chip's filter continues to apply unchanged.

    **`WebspaceHeader.svelte`.** Widen the `onedit` prop type to match. The measurement clone's no-op `onedit` needs no change beyond satisfying the widened type.

    **`RelinkModal.svelte` (new).** A small Dialog wrapping `QRPanel`, following `EditSourceModal`'s prop and callback convention: `open`, the instance id, the config (to resolve the instance's plugin binary, path and display name), `onclose`, and `onrelinked`. The title names the instance being re-linked. On the panel's paired callback it shows a brief confirmation line and closes, then fires `onrelinked` so the caller refreshes source health — the amendment's Re-link success transition. On close it lets the panel's own cancel-on-unmount terminate the session. Keep it deliberately smaller than the Add-Source dialog: it carries the panel and nothing else, no connection fields and no match fields.

    **`web/src/routes/w/[webspace]/+page.svelte`.** Handle the new kind in `handleChipEdit`: branch on it before the existing connection/match path, since it opens a different modal and needs no `describePlugin` call and therefore no stale-response guard. Track the relinking instance in its own state value rather than overloading `editInstance`/`editMode`, so a Re-link session and an edit session can never collide over the same shared state — the exact class of bug the existing stale-response guard exists to prevent. Render `RelinkModal` keyed on the relinking instance so a different chip always mounts a fresh session, matching the discipline the keyed `EditSourceModal` block already uses. Wire `onrelinked` to the same source-health refresh the route already performs after a config change, so the chip's dot updates without a page reload.

    **Tests.** Update `chip-edit-menu.test.ts`'s exact-menu-contents assertions to the new four-entry-plus-separator shape and add an assertion that the Re-link entry's render is guarded on the WhatsApp source type. Add `relink.test.ts` in the house pattern covering: `RelinkModal` renders `QRPanel` and no connection or match form; the route's `handleChipEdit` branches on the new kind before the describe path; the route tracks the relinking instance in state distinct from `editInstance`; and the route's `RelinkModal` render block is keyed on the relinking instance.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run check &amp;&amp; npm run test -- --run chip-edit-menu relink</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run check` reports 0 errors
    - `npm run test -- --run chip-edit-menu` passes with the menu-contents assertion updated to the four-entry-plus-separator shape and a new assertion that the Re-link entry's render is guarded on the WhatsApp source type
    - `npm run test -- --run relink` passes an assertion that `RelinkModal.svelte` renders `QRPanel` and contains neither `ConnectionForm` nor `MatchFieldsForm`
    - `npm run test -- --run relink` passes an assertion that the route's relinking-instance state identifier is distinct from `editInstance`
    - `npm run test -- --run relink` passes an assertion that the route's `RelinkModal` render block is keyed on the relinking instance
    - The `onedit` kind union is identical in `SourceChip.svelte`, `WebspaceHeader.svelte` and the webspace route — provable by `npm run check` compiling all three against one another
  </acceptance_criteria>
  <done>A WhatsApp chip's menu offers Re-link…, opening the same QR component in a small dialog; the chip's health dot updates when the pairing succeeds; and no other plugin type shows the entry.</done>
</task>

<task type="auto">
  <name>Task 3: Hermetic Playwright coverage of the QR flow (Phase 07.1 standing rule)</name>
  <files>web/e2e/specs/uat-08-whatsapp-qr-link.spec.ts, docs/testing.md</files>
  <read_first>
    - `docs/testing.md` — the whole "Harness architecture", "Writing a new spec", "Zero-retries policy", "The two mock-shaped plugins" and "Standing rule" sections; the standing rule is what makes this task mandatory rather than optional
    - `web/e2e/specs/uat-05-two-step-connect.spec.ts` — the closest existing spec: it already drives the Add-Source two-step modal, so it is the shape this spec extends rather than reinvents
    - `web/e2e/specs/uat-10-describe-race.spec.ts` — the existing precedent for a spec that intercepts a kernel route with `page.route` rather than relying on a real backend response
    - `web/e2e/specs/uat-06-remove-chip-roundtrip.spec.ts` — the existing precedent for driving the source chip's menu
    - `web/e2e/fixtures/config-builder.ts` — `buildConfig`/`writeConfig` and the four ready-made spec shapes, and `kernel.ts`'s `waitForFirstSync` and worker-scoped `configSpec` option
    - `web/src/lib/components/QRPanel.svelte` and `RelinkModal.svelte` — the accessible names and roles the spec's locators will target
    - `.planning/phases/08-whatsapp-conversations-managed-risk/08-03-SUMMARY.md` — the link routes' exact URL shapes and response bodies, which the route interception must reproduce faithfully
  </read_first>
  <action>
    Write one new spec file covering the QR flow end to end in the browser, hermetically.

    **Hermeticity is the constraint that shapes everything else.** The harness's plugins directory is a closed set containing only the mock fixture binaries — `topos-plugin-whatsapp` is deliberately not built by `make e2e` and must not be added to it, exactly as the real paperless/signal/proton plugins are structurally excluded. So this spec does not exercise a real WhatsApp plugin at all: it seeds config through `FixtureConfigSpec` and intercepts the three link routes with `page.route`, serving scripted responses. Register every route handler before the click that provokes it, per the standing guidance. The result is a spec that proves the SPA's own behaviour — the poll loop, the countdown, the state transitions, the two entry points — without a real account, a real credential, or a network call.

    Because the harness cannot offer WhatsApp in the picker (no whatsapp binary is discovered), drive the Add-Source assertions against a mock-shaped instance where the flow's mechanics are identical, and drive the WhatsApp-specific assertions — the picker row's fields, the chip's Re-link entry's source-type gate — through route-intercepted `GET /api/config/plugin-types` and `GET /api/sources` responses that report a WhatsApp plugin type and a WhatsApp-typed instance. State that reasoning in the spec file's header comment so a future reader does not mistake the interception for laziness.

    Cover these cases, each as its own `test`:
    1. **Populated QR.** With the start route returning a QR session, the panel shows the QR image with its accessible name, the instruction line, and a countdown line.
    2. **Rotation.** With the poll route returning a second, different QR payload, the rendered image updates without the surrounding dialog closing or the entered connection values being lost.
    3. **Scan success.** With the poll route transitioning to the paired state, the Add-Source flow advances to the match step and the QR panel is gone.
    4. **Expiry.** With the poll route transitioning to the timeout state, the panel shows the expired message and offers a restart, and the dialog stays open.
    5. **Error.** With the start route returning the store-in-use error, the panel renders the kernel's own message through the destructive alert.
    6. **Cancel releases the session.** Closing the dialog mid-session issues the cancel request — assert on the intercepted request having been made, not merely on the UI closing.
    7. **Re-link entry point.** With a WhatsApp-typed instance in the intercepted sources response, the chip menu shows Re-link…, opening it shows the same panel, and a non-WhatsApp chip's menu does not show the entry.
    8. **Not linked is not a failure (the E5 evidence).** With the trial launch succeeding and the instance unpaired, the flow reaches the QR step with no failure alert and no Save-anyway control, and cancelling out still reaches the match step and saves — assert the written config through `readConfigToml`, not a text match.

    Prefer user-facing locators throughout — `getByRole`, `getByText`, `getByLabel` — and do not add test-only attributes to the shipped components; if a locator is awkward to write, fix the accessible name in the component instead. Honour the zero-retries policy: no `waitForTimeout` sleeps, no retry loops — drive the countdown assertions off the scripted response values rather than real elapsed time.

    **`docs/testing.md`.** Add the new spec to whatever inventory or map that document keeps of the suite, and extend the "What stays manual, and why" section with the one thing this spec genuinely cannot cover: a real pairing against a real WhatsApp account, which needs a phone and is covered instead by Plan 08-01 Task 3's hands-on spike.
  </action>
  <verify>
    <automated>make e2e E2E_ARGS='specs/uat-08-whatsapp-qr-link.spec.ts' &amp;&amp; make e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make e2e E2E_ARGS='specs/uat-08-whatsapp-qr-link.spec.ts'` passes with all eight tests green
    - `make e2e` passes the whole suite — no pre-existing spec regresses
    - The spec file registers its `page.route` handlers before the actions that provoke them, and asserts config outcomes through `readConfigToml` rather than text matching
    - The spec contains no `waitForTimeout` sleep and no retry loop — countdown and rotation assertions are driven by scripted response values
    - `make e2e`'s target is unchanged — no whatsapp plugin binary is added to the harness's closed plugin set
    - The spec's header comment states why the link routes are intercepted rather than exercised against a real plugin
    - `docs/testing.md` lists the new spec and records the real-account pairing as the one thing that stays manual, pointing at Plan 08-01 Task 3
  </acceptance_criteria>
  <done>The QR pairing flow — both entry points, all five panel states, rotation, cancellation, and the not-linked-is-not-a-failure rule — is regression-armoured by a hermetic browser spec that never touches a real WhatsApp account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel → browser | The rotating pairing image and the kernel's own error text cross into the SPA |
| user → SPA → kernel | Instance ids, plugin binary names and store paths typed by the user reach the link start endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Information Disclosure | `QRPanel.svelte`'s rendered pairing image | medium | mitigate | The panel only ever renders the image the kernel returns and never persists it — no local storage, no history entry, no URL parameter. The kernel's loopback-only default binding remains the network boundary, unchanged by this plan |
| T-08-10 | Denial of Service | orphaned link sessions from an abandoned browser tab | medium | mitigate | Task 1: the panel cancels its session on explicit cancel AND on unmount; Task 3 case 6 asserts the cancel request actually fires rather than only that the dialog closed. The kernel's own deadline reaper is the independent second layer |
| T-08-12 | Elevation of Privilege | the store path submitted to the link start endpoint | medium | mitigate | The SPA submits only the path already stored on, or being entered for, the instance; the kernel refuses any plugin binary outside its own discovered set regardless of what the SPA sends, so the browser cannot widen what may be launched |
| T-08-13 | Information Disclosure | kernel error text rendered verbatim in the panel | low | accept | Error text is rendered as text through the existing `Alert` component, never as HTML; Svelte escapes interpolated text by default, so a hostile message cannot inject markup. Accepted because the message's author is the local kernel, not a remote party |
</threat_model>

<verification>
- `cd web && npm run check` reports 0 errors
- `cd web && npm run test` passes, including the new `qr-panel`, `relink` and `plugin-fields` suites and the updated `add-source` and `chip-edit-menu` suites
- `make e2e` passes the whole suite including the new WhatsApp spec
- `make test-portable` still passes — this plan changes no Go code
- The Add-Source picker offers WhatsApp with a required, pre-seeded Local Path
- A WhatsApp chip's menu shows Re-link… and a non-WhatsApp chip's does not
</verification>

<success_criteria>
A user adds WhatsApp from the "+" picker, scans the QR that appears right there in the modal, and lands on match settings — and can re-scan later from the chip's Re-link… entry when the session breaks. Every part of that flow a browser can drive is covered by a hermetic spec.
</success_criteria>

<output>
Create `.planning/phases/08-whatsapp-conversations-managed-risk/08-04-SUMMARY.md` when done.
</output>
