---
phase: 16-provenance-based-plugin-trust
plan: 07
type: execute
wave: 6
depends_on: ["16-05"]
files_modified:
  - docs/plugin-contract.md
autonomous: true
gap_closure: true
requirements: [TRUST-01, TRUST-02, TRUST-03, TRUST-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/plugin-contract.md's 'Trust tiers' section states that both plugin directories are pure search paths and that tier is decided per binary from provenance evidence, wherever the binary sits — matching the shipped D-11 model, Dirs's own doc comment, and docs/plugin-trust.md (16-VERIFICATION.md gap 2, CR-02)."
    - "No sentence anywhere in docs/plugin-contract.md still asserts that a binary earns a tier from the directory it resolved from, that tier is derived from directory location, or that one directory unconditionally wins a name collision."
    - "The collision paragraph states the shipped rule: whichever candidate carries valid evidence wins, and if neither or both do, the trusted-first search order decides — matching resolveBinaryDetailed's actual precedence, TestResolveBinary_CollisionResolvesToWhicheverCopyCarriesEvidence, and TestEscalation_ShadowingCannotInheritTrust (TRUST-04)."
    - "The section defers to docs/plugin-trust.md as the authoritative trust model rather than restating it, honouring that document's own stated intent that every other trust-touching document links back rather than restating (D-11 intent)."
    - "The honest-limits paragraphs survive the rewrite intact: a link-time manifest match is an integrity control and not publisher authentication, a verified binary is not thereby a safe binary, and signed provenance is what supplies publisher authentication (TRUST-02)."
    - "The external-tier consent-and-pin description still reads as the fully supported way to run an unsigned binary, not as a punishment or a degraded path (TRUST-03, unchanged fallback)."
    - "Every existing cross-reference into this section still resolves: the '## Trust tiers' heading is preserved so docs/plugins/signal.md's #trust-tiers anchor and docs/api.md's two 'Trust tiers' references keep landing on a section that is now correct, and make docs-check passes."
    - statement: "A reader who lands mid-document (via the #trust-tiers anchor, without reading from the top) still gets a correct model in the first paragraph they encounter, rather than a correction that only arrives later in the section."
      verification: backstop
  artifacts:
    - path: "docs/plugin-contract.md"
      provides: "A Trust tiers section describing the shipped evidence-based model and deferring to docs/plugin-trust.md"
      contains: "pure search paths"
  key_links:
    - from: "docs/plugin-contract.md"
      to: "docs/plugin-trust.md"
      via: "the Trust tiers section points the reader at the canonical trust document instead of restating the model"
      pattern: "plugin-trust.md"
    - from: "docs/plugin-contract.md"
      to: "kernel/pluginhost/discover_binaries.go"
      via: "the section's collision and search-path statements match Dirs's own doc comment and resolveBinaryDetailed's actual precedence"
      pattern: "EvaluateTrust"
  prohibitions:
    - requirement_id: TRUST-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT leave any sentence in the maintained doc set telling an operator that placing a binary in a particular directory grants it trust. A document that still says so is an instruction to create the exact escalation path TRUST-04 closes by test, and an operator following it would build a deployment they wrongly believe is vetted."
    - requirement_id: TRUST-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT overclaim what verification proves. The rewrite must not drop or soften the 'integrity control, not publisher authentication' caveat or the warning against reading 'verified' as 'safe', and must not imply that a signature attests to a plugin's behaviour rather than its origin."
    - requirement_id: TRUST-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT describe the unsigned external-tier path as second-class, discouraged, or a failure state. It is the fully supported way to run code topos did not sign, and the corrected document must not nudge an operator away from it or toward disabling a gate."
---

<objective>
Close 16-VERIFICATION.md gap 2: `docs/plugin-contract.md`'s "Trust tiers"
section still describes the pre-Phase-16 directory-derived trust model almost
verbatim — contradicting the shipped D-11 code, this phase's own tests, and
`docs/plugin-trust.md`, which this phase established as the one canonical trust
document every other document links back to.

Purpose: an operator reading only `docs/plugin-contract.md` would form an
actively incorrect mental model — that the trusted directory confers trust, and
that a name collision always resolves to it. Both are false and both point
toward over-trusting a deployment. 16-05-SUMMARY.md self-flagged this as known
but out of that plan's narrowly-scoped edit.

Output: a "Trust tiers" section that states the shipped evidence-based model,
defers to `docs/plugin-trust.md` for the authoritative statement, and leaves no
residual directory-derived claim anywhere else in the same file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-provenance-based-plugin-trust/16-VERIFICATION.md
@.planning/phases/16-provenance-based-plugin-trust/16-REVIEW.md
@.planning/phases/16-provenance-based-plugin-trust/16-CONTEXT.md
</context>

<assumptions>
## Flagged assumptions (spec-less probe fallback — no SPEC exists for this phase)

The deterministic edge probe returned all four phase requirement rows
`unresolved`. None is auto-resolved and none is dropped; each is recorded here
as an explicit assumption the executor works under and the re-verifier may
challenge.

| Requirement | Probe category | Assumption this plan proceeds under |
|---|---|---|
| TRUST-01 | unclassified | `docs/plugin-trust.md` (123 lines, verified ✓ by 16-VERIFICATION.md) is CORRECT and canonical; this plan aligns `docs/plugin-contract.md` to it and never the reverse. If the two disagree on a detail, `docs/plugin-trust.md` wins. |
| TRUST-02 | unclassified | The publisher-authentication paragraphs 16-05 added (~lines 278-300) are correct as shipped and are preserved, not rewritten. |
| TRUST-03 | unclassified | The "Pinning" section (from line 321) accurately describes the unchanged consent-and-pin path; this plan does not edit it, and assumes any edit there would itself risk a TRUST-03 regression. |
| TRUST-04 | concurrency ("If interrupted or run in parallel, what is guaranteed?") | Documentation carries no runtime concurrency guarantee. The doc-level analogue this plan honours: the collision text must describe a rule that is deterministic regardless of scan order, matching `resolveBinaryDetailed`'s own precedence. Carried as a `verification: backstop` truth above (mid-document entry via the `#trust-tiers` anchor). |

**Canon-referral breadcrumb (not minted as a prohibition):** supply-chain
integrity and signing-key handling are canon security concerns owned by
`/gsd-secure-phase` and this phase's existing `T-16-*` register — not minted
here.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite the "Trust tiers" section to the shipped evidence-based model</name>
  <reversibility rating="reversible">A documentation section rewrite in one file; revertible with a single `git revert` and no code, format, or artifact consequences.</reversibility>
  <files>docs/plugin-contract.md</files>
  <read_first>
    - `docs/plugin-contract.md` lines 206-320 — the whole "Trust tiers" section as it currently stands, including the paragraphs 16-05 already corrected (~278-300) which must survive untouched.
    - `docs/plugin-trust.md` — the entire 123-line canonical document. Its "The model, in one paragraph", "The two evidence sources today (D-10)", and "What earns trust, and what does not" table are the authority this rewrite defers to; its opening claim that every other trust-touching document "links back here rather than restating the model" is the intent being honoured.
    - `kernel/pluginhost/discover_binaries.go` — the `Dirs` type's doc comment (~lines 186-207, "Neither field grants anything by itself… tier is decided per binary by `pluginhost.EvaluateTrust`… wherever the binary sits") and `resolveBinaryDetailed`'s collision branch (~lines 495-515), which is the ACTUAL precedence the collision paragraph must describe.
    - `kernel/pluginhost/tier_test.go` — `TestResolveBinary_ExternalWithSignedManifestResolvesTrustedTier`, `TestResolveBinary_LocationSymmetric`, `TestResolveBinary_TrustedWithNoEvidenceResolvesExternalTier`, `TestResolveBinary_CollisionResolvesToWhicheverCopyCarriesEvidence` — the committed behaviour the prose must agree with.
    - `.planning/phases/16-provenance-based-plugin-trust/16-REVIEW.md` — finding CR-02, which enumerates the three false claims and prescribes the fix.
    - `docs/api.md` around lines 715-730 — the `tier` field's documented two-value vocabulary and its cross-reference into this very section, so the rewrite stays consistent with what the API contract promises.
  </read_first>
  <action>
    Rewrite `docs/plugin-contract.md` lines 206-244 — the section heading through the end of the collision paragraph. Keep the heading text exactly `## Trust tiers` (unchanged, so `docs/plugins/signal.md`'s `#trust-tiers` anchor and `docs/api.md`'s two references keep resolving).

    Target state, paragraph by paragraph:

    (1) Opening. State that the kernel scans two plugin directories and that both are **pure search paths** — places the kernel looks for a binary named by `[sources.<id>] plugin`, and nothing more. Neither directory grants a tier. Say so in the first sentence a reader landing on the `#trust-tiers` anchor encounters, not later in the section.

    (2) The two directories. Keep the concrete, still-true facts about each: `[plugins] dir` defaults to `plugins`, resolved relative to the running `topos` executable, and is what this repository's `make build`/`make dev` populate; `[plugins] external_dir` defaults to a per-OS platform data directory with no config required — `$XDG_DATA_HOME/topos/plugins-external` (falling back to `~/.local/share/topos/plugins-external`) on Linux, `~/Library/Application Support/topos/plugins-external` on macOS, `%LOCALAPPDATA%\topos\plugins-external` on Windows — the kernel never creates it, and an empty external tier is a legitimate, unremarkable state. Delete the per-directory tier assertions attached to each bullet; a directory bullet must no longer name `pluginhost.TierTrusted` or `pluginhost.TierExternal` as its consequence.

    (3) Where tier actually comes from. Replace the paragraph at lines 225-232 with the shipped rule: tier is decided per binary by `pluginhost.EvaluateTrust` from the provenance evidence the artifact itself carries, wherever that binary sits — a signed release manifest, or (during the Phase 17 transition) the kernel's link-time build manifest. Keep the still-true half of the old paragraph verbatim in substance: there is no `Describe`-reported "I am trusted" field and no allowlist of known-good `source_type` values, tier is set once at launch and never re-derived from a live RPC, and a plugin process is not a trustworthy witness to its own trustworthiness. Add the two consequences this phase's tests pin: a binary in the external directory CAN evaluate to trusted when it carries valid evidence, and a binary in the trusted directory evaluates to external when it carries none.

    (4) The collision paragraph at lines 234-244. Replace the fixed-winner rule with the shipped one: on a name collision across both directories, whichever candidate carries valid evidence wins; if neither carries evidence, or both do, the trusted-first search order decides; and a candidate that a manifest positively names with a digest that no longer matches is a tamper refusal that resolves to the refusal and never falls back to the other copy. Preserve the still-true `launch_advisory: "shadowed"` fact — `GET /api/sources` carries it on that instance's entry so the operator can see that the plugin they consented to pin is not the one running — and preserve its `13-05-PLAN.md`/`D-14` citation.

    (5) Deferral. Close the section by pointing the reader at `docs/plugin-trust.md` for the authoritative model — what earns trust, what does not, the on-disk manifest format, and key rotation — stating explicitly that this document does not restate it. Use a relative markdown link of the form `[docs/plugin-trust.md](plugin-trust.md)` so `scripts/check-doc-links.sh` resolves it.

    Constraints on the rewrite:
    - Do NOT quote, restate, or "correct in place" any superseded claim. Delete it. A negative grep gate in this task's acceptance criteria scans this file for the old wording, and a sentence that reproduces it — even to disavow it — fails that gate and, worse, leaves the wrong sentence findable by a reader skimming.
    - Do NOT touch lines 246 onward in this task. The build-provenance paragraph, the honest-limits paragraph, the publisher-authentication paragraph, the bare-filename paragraph, and the two-instances paragraph are Task 2's scope or are deliberately preserved.
    - Do NOT edit `docs/plugin-trust.md`, `docs/api.md`, `docs/install.md`, or `docs/plugins/signal.md` — this plan modifies exactly one file.
  </action>
  <verify>
    <automated>test "$(grep -c '^## Trust tiers$' docs/plugin-contract.md)" -eq 1 && test "$(grep -c 'pure search paths' docs/plugin-contract.md)" -ge 1 && test "$(grep -c 'plugin-trust.md' docs/plugin-contract.md)" -ge 2 && test "$(grep -cE 'derived exclusively from which directory|the trusted directory always wins|A binary resolved from here is' docs/plugin-contract.md)" -eq 0 && ./scripts/check-doc-links.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Trust tiers$' docs/plugin-contract.md` outputs `1` — the heading text is byte-identical, so the `#trust-tiers` anchor still resolves.
    - `grep -c 'derived exclusively from which directory' docs/plugin-contract.md` outputs `0`.
    - `grep -c 'the trusted directory always wins' docs/plugin-contract.md` outputs `0`.
    - `grep -c 'A binary resolved from here is' docs/plugin-contract.md` outputs `0`.
    - `grep -c 'pure search paths' docs/plugin-contract.md` outputs at least `1`.
    - `grep -c 'EvaluateTrust' docs/plugin-contract.md` outputs at least `1` — the section names the function that actually decides tier.
    - `grep -c 'plugin-trust.md' docs/plugin-contract.md` outputs at least `2` — the new deferral link plus the pre-existing reference at ~line 298.
    - `grep -c 'launch_advisory' docs/plugin-contract.md` outputs at least `1` — the shadowed-advisory fact survived the collision rewrite.
    - `grep -c 'plugins-external' docs/plugin-contract.md` outputs at least `1` — the per-OS default-directory detail survived.
    - `./scripts/check-doc-links.sh` exits 0.
    - The rewritten collision paragraph names all three outcomes — evidence-carrying candidate wins, trusted-first order breaks the neither/both case, tamper refusal never falls back — verifiable by reading the diff; the executor quotes the rewritten paragraph in the SUMMARY.
    - `git diff -- docs/plugin-contract.md` shows changes confined to the region from the `## Trust tiers` heading through the collision paragraph; lines 246 onward are untouched by this task.
  </acceptance_criteria>
  <done>The "Trust tiers" section states the shipped evidence-based model, names `EvaluateTrust` as the decider, describes the real collision rule, defers to `docs/plugin-trust.md`, and retains its anchor and every still-true operational detail.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the residual directory-derived claims elsewhere in the same file</name>
  <files>docs/plugin-contract.md</files>
  <read_first>
    - `docs/plugin-contract.md` lines 156-168 (the "Discovery and launch" preamble), 246-276 (the build-provenance paragraph), 302-311 (the bare-filename paragraph), and 313-319 (the two-instances paragraph). These four regions carry the same superseded premise as the section Task 1 rewrote, and leaving them is what would let 16-VERIFICATION.md gap 2's truth — "every other document agrees with the canonical model" — stay open inside a single file.
    - `docs/plugin-contract.md` lines 278-300 — the honest-limits and publisher-authentication paragraphs 16-05 added. These are CORRECT and must survive byte-intact; read them so you can confirm you have not disturbed them.
    - `docs/plugin-trust.md` — "The two evidence sources today (D-10)" and "Phase 17 retires the link-time arm", which the build-provenance paragraph must not contradict.
    - `kernel/config/types.go` — `PluginsConfig`, whose "search path" wording 16-02 already established; the doc's language should agree with the config type's own comment.
  </read_first>
  <action>
    Correct the four residual regions, each with a minimal, surgical edit. Do not restructure the document.

    (a) Lines 156-168, "Discovery and launch" preamble. Two problems. First, the sentence spanning ~161-163 forward-references a merged section title that no document heading matches — this file has two separate headings, `## Trust tiers` and `## Pinning`; re-point that cross-reference at the "Trust tiers" heading alone. Second, the clause ending that same sentence (~166-168) attributes the kernel's and the UI's degree of trust to where the binary was found; replace it with the shipped fact — both directories are search paths, and a binary's tier is decided from the provenance evidence it carries, not from which of the two it was found in.

    (b) Lines 246-276, the build-provenance paragraph. Its thesis — trusted status is a build-provenance fact rather than a filesystem-location proxy — is correct and stays. Its one stale clause is the concession at ~247-249 granting each directory a per-tier candidacy role; under D-11 no such per-directory candidacy exists, so delete that clause and say instead that the two directories remain real install conveniences (`D-16`) and nothing more. Also generalise the paragraph's launch-time description: the kernel re-hashes the binary and checks it against its evidence — the link-time table OR a validly-signed release manifest — rather than describing the link-time table as the only gate. Keep every still-true operational detail: `cmd/topos-manifest` hashing at build time, the `-ldflags -X` linkage, the `manifest_unverified` launch failure, the add-source picker's trial "describe" launch being gated identically, a kernel built with no manifest trusting nothing through that arm, verification never demoting-and-running, and the pointer to `docs/plugins/signal.md`'s local-builds section.

    (c) Lines 302-311, the bare-filename paragraph. The RULE is unchanged and correct — `[sources.<id>] plugin` must be a bare binary filename with no path separator and no `.`/`..` segment, failing config load and `PUT /api/config` by name. Only its rationale is stale: the sentence at ~307-311 grounds the rule in a location-based trust premise. Replace that rationale with the shipped one — the rule confines resolution to the two configured search directories, so a caller-supplied path can never point the kernel at a file outside them; trust is then decided separately, from evidence.

    (d) Lines 313-319, the two-instances paragraph. Its conclusion is still true — two `[sources.<id>]` entries naming the same plugin binary always resolve to the same tier — but the parenthetical at ~315 grounds that conclusion in the resolved filesystem path. Replace the parenthetical: tier belongs to the binary's own evidence, not to the instance, so the same bytes evaluate identically for every instance that names them. Keep the closing statement that different binaries can freely sit in different tiers and that a mixed deployment is the expected shape.

    Finally, re-read the whole file once end to end and confirm no other sentence still attributes trust to directory location. Record in the SUMMARY the line numbers of every region you changed and a one-line statement that the full-file re-read found no further stale claim.
  </action>
  <verify>
    <automated>test "$(grep -cE 'directory of origin determining|which tier it.s a CANDIDATE for|property of the binary.s resolved path|trust is decided by the kernel from where the binary|keeps the premise|Trust tiers and pinning' docs/plugin-contract.md)" -eq 0 && test "$(grep -c 'an integrity control, not publisher' docs/plugin-contract.md)" -eq 1 && test "$(grep -c 'must not contain a path separator' docs/plugin-contract.md)" -ge 1 && ./scripts/check-doc-links.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'directory of origin determining how much' docs/plugin-contract.md` outputs `0`.
    - `grep -c "which tier it's a CANDIDATE for" docs/plugin-contract.md` outputs `0`.
    - `grep -c "property of the binary's resolved path" docs/plugin-contract.md` outputs `0`.
    - `grep -c 'trust is decided by the kernel from where the binary' docs/plugin-contract.md` outputs `0`. (The literal is line-wrapped in the source before the word that completes it, so the gate deliberately stops at the last word on that line.)
    - `grep -c 'keeps the premise' docs/plugin-contract.md` outputs `0` — the sentence that grounded the bare-filename rule in a location-based trust premise is gone.
    - `grep -c 'Trust tiers and pinning' docs/plugin-contract.md` outputs `0` — the dangling forward-reference to a non-existent section is gone.
    - `grep -c 'an integrity control, not publisher' docs/plugin-contract.md` outputs `1` — the honest-limits caveat survived (TRUST-02 prohibition). (The caveat is line-wrapped after `publisher`, so the gate stops there rather than spanning the line break.)
    - `grep -c 'verified safe' docs/plugin-contract.md` outputs at least `1` and `grep -c 'Do not read' docs/plugin-contract.md` outputs at least `1` — the do-not-read-verified-as-safe warning survived.
    - `grep -c 'manifest_unverified' docs/plugin-contract.md` outputs at least `1` and `grep -c 'topos-manifest' docs/plugin-contract.md` outputs at least `1` — the still-true operational detail survived the (b) edit.
    - `grep -c 'must not contain a path separator' docs/plugin-contract.md` outputs at least `1` — the bare-filename RULE survived while only its rationale changed.
    - Repo-wide sweep is clean: `grep -rn 'trusted directory always wins\|derived exclusively from which directory\|resolved from here is' docs/ README.md web/README.md` produces no output.
    - `./scripts/check-doc-links.sh` exits 0 and `make docs-check` exits 0.
    - `git diff -- docs/plugin-contract.md` shows the region 278-300 (as numbered before this plan) unchanged — the paragraphs 16-05 added are byte-intact.
    - `git status --porcelain` lists `docs/plugin-contract.md` and no other file as modified by this plan.
  </acceptance_criteria>
  <done>No sentence anywhere in `docs/plugin-contract.md` still attributes trust to directory location, every still-true rule and operational detail is preserved, the honest-limits paragraphs are byte-intact, and the doc-link gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator's mental model | The document is the operator's only account of how the kernel decides trust; a false claim here becomes a deployment decision |
| documentation → third-party plugin author | `docs/plugin-contract.md` is the published contract a third-party author builds against; a false trust claim shapes how they ship and where they tell users to place binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07-01 | Spoofing | `docs/plugin-contract.md` "Trust tiers" (lines 206-241) | high | mitigate | Rewrite so the section states directories are pure search paths and tier comes only from evidence; negative grep gates on the three superseded claims fail the task if any survives, and a repo-wide sweep confirms no other maintained doc carries them. |
| T-16-07-02 | Elevation of Privilege | The collision / shadow-rule paragraph | high | mitigate | State the shipped rule (evidence wins; trusted-first breaks ties; a tamper refusal never falls back) so no reader concludes that placing a colliding filename in a particular directory wins by construction — the exact belief `TestEscalation_ShadowingCannotInheritTrust` disproves. |
| T-16-07-03 | Repudiation | Overclaiming what verification proves | medium | mitigate | The honest-limits and publisher-authentication paragraphs (~278-300) are preserved byte-intact and gated by positive greps for both the "integrity control, not publisher authentication" caveat and the do-not-read-verified-as-safe warning. |
| T-16-07-04 | Tampering | Cross-document reference integrity | medium | mitigate | The `## Trust tiers` heading text is preserved so `docs/plugins/signal.md`'s `#trust-tiers` anchor and `docs/api.md`'s two references keep resolving; `make docs-check` / `scripts/check-doc-links.sh` gate every relative link. |
| T-16-07-SC | Tampering | npm/pip/cargo installs | low | accept | Documentation-only plan: `files_modified` is one markdown file, no package-manager install task exists, and no dependency manifest or lockfile is touched. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `make docs-check` exits 0.
- `grep -rn 'trusted directory always wins\|derived exclusively from which directory\|resolved from here is\|directory of origin determining' docs/ README.md web/README.md` produces no output.
- `docs/plugin-contract.md` and `docs/plugin-trust.md` agree: neither asserts a per-directory tier, both describe the same two evidence arms, and only `docs/plugin-trust.md` states the model in full.
- `git status --porcelain` shows `docs/plugin-contract.md` as the only file this plan modified.
- No source file, test, or build recipe was touched — `git diff --stat -- kernel/ web/src/ web/e2e/ scripts/ Makefile` produces no output.
</verification>

<success_criteria>
- 16-VERIFICATION.md gap 2's single `missing` item is satisfied in full: both directories described as pure search paths, per-directory tier assertions removed, the collision paragraph corrected to the evidence-based rule, and the reader pointed at `docs/plugin-trust.md` as the authoritative model.
- The sweep goes beyond the recorded line range (206-241) to the three further regions in the same file carrying the same superseded premise, so the gap's truth — every other document agrees with the canonical one — actually holds rather than holding only within one section.
- Everything still true is preserved: default directory paths, the manifest generator and its link-time linkage, the `manifest_unverified` refusal, the trial-launch gate, the shadowed advisory, the bare-filename rule, the honest-limits caveat, and the consent-and-pin path.
- Every cross-reference into the section still resolves and the doc-link gate passes.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and document regions this gap-closure plan creates or modifies. The
plan-review-convergence source-grounding pass should treat every entry below as
authored-by-this-plan rather than as drift against the current tree.

**Modified (existing document regions, all in `docs/plugin-contract.md`):**
- `## Trust tiers` section body (lines 206-244) — rewritten: the two directories described as pure search paths, per-directory `pluginhost.TierTrusted`/`pluginhost.TierExternal` assertions removed, the tier-derivation paragraph re-pointed at `pluginhost.EvaluateTrust` and artifact-carried evidence, the collision paragraph replaced with the evidence-wins / trusted-first-tiebreak / tamper-refusal-never-falls-back rule, and a closing deferral link to `docs/plugin-trust.md`. The `## Trust tiers` heading TEXT is deliberately unchanged.
- "Discovery and launch" preamble (lines 156-168) — the dangling "Trust tiers and pinning" forward-reference corrected to "Trust tiers", and the trailing directory-of-origin trust clause replaced with the evidence-based statement.
- Build-provenance paragraph (lines 246-276) — the per-directory candidacy clause removed; the launch-time check generalised to both evidence arms; every operational detail retained.
- Bare-filename paragraph (lines 302-311) — rule unchanged, rationale re-grounded on search-path confinement instead of directory-derived trust.
- Two-instances paragraph (lines 313-319) — the parenthetical re-grounded on the binary's evidence rather than its resolved path.

**New (created by this plan):** none — this plan creates no new file, heading, or symbol. It adds one relative markdown link target (`plugin-trust.md`) inside an existing section.

**Explicitly NOT touched:** `docs/plugin-trust.md`, `docs/api.md`, `docs/install.md`, `docs/plugins/signal.md`, `docs/testing.md`, the honest-limits and publisher-authentication paragraphs at lines 278-300, the `## Pinning` section from line 321, and all source, test, script, and build files.

**Out of scope (recorded in 16-REVIEW.md, not part of the recorded gaps):** WR-02 (`scripts/install.sh` marking provenance data files executable) and IN-01 (collision-fallback log wording). Both are advisory findings, not verification gaps; close them via `/gsd-code-review 16 --fix`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/16-provenance-based-plugin-trust/16-07-SUMMARY.md` when done
</output>
