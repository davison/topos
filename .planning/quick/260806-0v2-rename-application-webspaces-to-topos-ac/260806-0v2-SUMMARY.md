---
phase: quick-260806-0v2
plan: 01
subsystem: infra
tags: [go-workspace, protobuf, go-plugin, sveltekit, rename]

requires: []
provides:
  - "github.com/davison/topos module path across all seven Go workspace modules"
  - "topos.v1 proto package under proto/topos/v1/, regenerated stubs under sdk/gen/topos/v1/"
  - "TOPOS_PLUGIN / topos-source-plugin-v1 handshake magic cookie"
  - "bin/topos + bin/plugins/topos-plugin-{paperless,silverbullet,proton,mock,signal} build outputs"
  - "kernel default config/data paths at ~/.config/topos and ~/.local/share/topos"
  - "live install migrated onto the new paths, config.toml plugin/cert/index values rewritten"
affects: ["05-source-instances-per-type-matching"]

actuals:
  tokens: 53320
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Product-name rename kept strictly separate from the 'webspace' concept term via an explicit ordered replacement table, most-specific-pattern-first, to avoid double-touching substrings"

key-files:
  created:
    - proto/topos/v1/plugin.proto
    - sdk/gen/topos/v1/plugin.pb.go
    - sdk/gen/topos/v1/plugin_grpc.pb.go
    - cmd/topos/main.go
  modified:
    - go.mod, sdk/go.mod, plugins/{mock,paperless,proton,signal,silverbullet}/go.mod
    - sdk/shared.go (handshake cookie), all five plugins/*/plugin.go (contract_version)
    - kernel/config/types.go (DefaultIndexPath, doc comments)
    - Makefile, .gitignore, .claude/settings.local.json (untracked, gitignored — edited but not committed)
    - config.example.toml, README.md, docs/api.md, docs/plugin-contract.md
    - web/src/routes/+page.svelte, web/src/lib/components/StreamError.svelte
    - .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md
    - .planning/phases/05-source-instances-per-type-matching/05-CONTEXT.md (forward-looking path refs only)
    - ~/.config/topos/config.toml (live install, outside repo)

key-decisions:
  - "Task 1 is type=tracer with a fully-automated <verify> (make test) that already passed; per the dispatching agent's explicit instruction to complete Tasks 1-2 before stopping, the generic interactive tracer-feedback checkpoint was not raised as a separate stop — Task 3 (the plan's own blocking human-verify) is the single checkpoint for this run."
  - ".claude/settings.local.json is gitignored (not tracked) — edited in place for consistency but not staged/committed; the plan's own files_modified list included it as a real file to update, and it now reflects bin/topos and topos-plugin-* patterns"
  - ".planning/config.json has no project_title field to update (that value in the init query was derived live from PROJECT.md's heading, not a stored field) — no action needed beyond the PROJECT.md title update already made"

requirements-completed: [QUICK-260806-0v2]

coverage: []

duration: 11min
completed: 2026-08-06
status: complete
---

# Quick Task 260806-0v2: Rename Application (Webspaces -> Topos) Summary

**Renamed the plugin contract spine, build surface, default config/data paths, and living docs from webspaces to topos across all seven Go modules; migrated the user's live `~/.config` and `~/.local/share` install onto the new paths — checkpoint (Task 3, live `make dev` verification) intentionally left open for the user.**

## Performance

- **Duration:** 11 min (Tasks 1-2 only; Task 3 checkpoint not run)
- **Started:** 2026-08-05T23:45:47Z
- **Tasks:** 2 of 3 completed (Task 3 is a blocking human-verify checkpoint, deliberately not executed per run instructions)
- **Files modified:** 85 unique tracked files across both commits, plus 4 live files outside the repo (`~/.config/topos/config.toml` and the three files that moved with it, plus the whole `~/.local/share/topos/` directory)

## Accomplishments

- **Task 1 — contract spine rename (commit `21cf179`):** `proto/webspaces/v1/` → `proto/topos/v1/`, `sdk/gen/webspaces/v1/` → `sdk/gen/topos/v1/` (both via `git mv`, history preserved); all 7 `go.mod` module paths `github.com/davison/webspaces` → `github.com/davison/topos`; protobuf stubs regenerated via `make proto` (buf, found at `/opt/go/bin/buf` after adding it to PATH) against the new `topos.v1` package; `sdk.Handshake` magic cookie `WEBSPACES_PLUGIN`/`webspaces-source-plugin-v1` → `TOPOS_PLUGIN`/`topos-source-plugin-v1`; `contract_version` wire value `topos.v1` in all five plugins, contract test assertions updated (not weakened). All seven workspace modules build and test green, including `plugins/signal` under `CGO_ENABLED=1 -tags libsqlcipher`.
- **Task 2 — build surface, default paths, docs, live migration (commit `1117b71`):** `cmd/webspaces/` → `cmd/topos/` (git mv); usage string, error prefix, logger `Name`, "serving" log line, and XDG config-path join segment in `cmd/topos/main.go` updated to bare `topos`; `Makefile`, `.gitignore` (`/webspaces` → `/topos` stray-binary entry), `.claude/settings.local.json` (gitignored, edited not committed) all updated to `bin/topos`/`topos-plugin-*`; `kernel/config/types.go`'s `DefaultIndexPath` and doc comments now point at `~/.config/topos`/`~/.local/share/topos`; `scripts/signal-readonly-smoke.sh`'s throwaway `$TMP_XDG` child directory renamed from `webspaces` to `topos` so it still matches the kernel's real XDG lookup; product-vs-concept judgment sites in `web/src/routes/+page.svelte`, `StreamError.svelte`, `README.md`, `.claude/CLAUDE.md`, `docs/api.md`, `docs/plugin-contract.md` updated by hand-reading each line (every `webspace` concept use — `[webspaces.*]` config tables, `/api/webspaces` routes, `webspace_not_found`, `web/src/routes/w/[webspace]/`, `ReplaceWebspaceSourceItems` — confirmed untouched). Living planning docs (`PROJECT.md`, `ROADMAP.md`, `REQUIREMENTS.md`) retitled; `05-CONTEXT.md`'s forward-looking `proto/webspaces/v1/` references and D-06's live-config-path mention updated for Phase 5 agents, nothing else in that file touched.
- **Live install migrated:** `mv ~/.config/webspaces ~/.config/topos` and `mv ~/.local/share/webspaces ~/.local/share/topos` — each a single atomic directory move (confirmed neither destination existed beforehand, confirmed no kernel process was running via `pgrep`). `index.db`/`index.db-shm`/`index.db-wal` moved together; `config.toml`, `config.toml.bak`, `proton-bridge-cert.pem`, `silverbullet-ca.pem` moved together via `mv` (never copy-then-delete). `~/.config/topos/config.toml` edited: `[index] path` → `~/.local/share/topos/index.db`; all four `plugin = ` values → `topos-plugin-*`; both `ca_cert` paths → `~/.config/topos/*.pem`. **Deliberately left unchanged:** `[plugins] dir = "/home/darren/projects/davison/webspaces/bin/plugins"` (the checkout directory itself is not renamed in this task) and the `[webspaces.house-move]` / `[webspaces.test]` table headers (concept term).
- **All verification gates passed:** `rm -rf bin && make build` → `bin/topos` + all five `bin/plugins/topos-plugin-*`, no old-named binary; `make test` (all 7 modules green, signal under cgo); `make dev-check` (dev-guard-smoke.sh, all 3 cases pass); `./scripts/assert-stylesheet.sh` (OK); `npm --prefix web run check` (0 errors, 1 pre-existing unrelated warning in SearchBox.svelte); `npm --prefix web run test` (97/97 passing).

## Task Commits

1. **Task 1: Rename the contract spine** - `21cf179` (feat) - 69 files changed
2. **Task 2: Rename the build surface, default paths, docs; migrate the live install** - `1117b71` (feat) - 38 files changed

**Plan metadata:** not yet committed — orchestrator handles the docs commit per run constraints.

## Files Created/Modified

See `key-files` in frontmatter for the representative set; full lists are in each commit (`git show --stat 21cf179`, `git show --stat 1117b71`).

**Outside the repo (live install, not committed):**
- `~/.config/webspaces/` → `~/.config/topos/` (config.toml, config.toml.bak, proton-bridge-cert.pem, silverbullet-ca.pem)
- `~/.local/share/webspaces/` → `~/.local/share/topos/` (index.db, index.db-shm, index.db-wal)

## Decisions Made

- Task 1's tracer-feedback gate (interactive-run checkpoint after a `type="tracer"` task) was subsumed by the dispatching agent's explicit instruction to complete Tasks 1-2 before stopping. Task 1's own `<verify>` block is fully automated (`make test`) and passed cleanly, so no separate manual checkpoint was raised between Task 1 and Task 2 — Task 3 remains the single blocking checkpoint for this run.
- `.claude/settings.local.json` is gitignored globally (`~/.gitignore` has `**/.claude/settings.local.json`), so it was edited for functional consistency (permission entries now reference `bin/topos`/`topos-plugin-*`) but was never staged or committed — this is expected, not an omission.
- `.planning/config.json` has no `project_title` key; the "Webspaces" value seen in the init query output is derived live from `.planning/PROJECT.md`'s `# ` heading, which was already updated to `# topos`. No config.json edit was needed or made.

## Deviations from Plan

None - plan executed exactly as written for Tasks 1 and 2. Task 3 (blocking human-verify) intentionally not executed per explicit run constraints.

## Issues Encountered

- `buf`, `protoc-gen-go`, and `protoc-gen-go-grpc` were not on `PATH` by default (only `protoc` itself was) — found at `/opt/go/bin/` (`$(go env GOPATH)/bin`) and added to `PATH` for the `make proto` regeneration step. This is an environment quirk, not a plan deviation; the plan's own fallback logic in the Makefile (`buf` preferred, `protoc` fallback) was unaffected.

## User Setup Required

**This plan has an open checkpoint requiring your action.** Task 3 of the plan (`.planning/quick/260806-0v2-rename-application-webspaces-to-topos-ac/260806-0v2-PLAN.md`) is a blocking human-verify step that was deliberately not run:

1. Run `make dev` from the repo root (`/home/darren/projects/davison/webspaces`). It rebuilds the five `topos-plugin-*` binaries, starts the kernel, and opens the UI.
2. Confirm the browser title and home-page heading read `topos`.
3. Confirm your configured webspaces are listed; open `house-move`.
4. Confirm every source (paperless, silverbullet, proton, signal) shows green/healthy in the header.
5. Spot-check one item per source: open it, confirm the detail pane renders and the "open in source" deep link is present.
6. Confirm the old directories are gone: `ls ~/.config/webspaces ~/.local/share/webspaces` should report "no such file" for both (already true as of this run — the migration in Task 2 already moved them).

If a source shows an error, it most likely means its `plugin = ` name in `~/.config/topos/config.toml` didn't get rewritten correctly, or a stale binary is being picked up — re-run `rm -rf bin && make build` (already verified clean once during this run) and retry.

## Next Phase Readiness

- The repo can now be pushed to `github.com/davison/topos` with a matching module path.
- Phase 5's plugin-contract republication will carry the `topos.v1` package name from the start — no rework needed there.
- Nothing in the repo depends on the checkout's on-disk directory name (`~/projects/davison/webspaces`); the live config's `[plugins] dir` still points there deliberately and can be updated later in one place if the checkout itself is ever renamed.
- ~~**Blocker:** this quick task is not complete until Task 3's live verification is confirmed by the user (see "User Setup Required" above).~~ **Closed 2026-08-12 (v1.0 milestone sweep):** Task 3's checkpoint is satisfied by sustained live use of the renamed app — the checkout itself was renamed to `~/projects/davison/topos`, quick tasks 260811-rqc (topos header branding) and the page-title fix built on the rename, and Phases 7–10 all ran live UAT sessions against the running `topos` kernel with all sources healthy.

## Self-Check: PASSED

- FOUND: `proto/topos/v1/plugin.proto`
- FOUND: `sdk/gen/topos/v1/plugin.pb.go`
- FOUND: `cmd/topos/main.go`
- FOUND: `bin/topos` (executable)
- FOUND: `~/.config/topos/config.toml`
- FOUND: `~/.local/share/topos/index.db`
- FOUND: commit `21cf179`
- FOUND: commit `1117b71`

---
*Phase: quick-260806-0v2*
*Completed: 2026-08-06 (Tasks 1-2); Task 3 checkpoint closed 2026-08-12 via sustained live use (see Next Phase Readiness)*
