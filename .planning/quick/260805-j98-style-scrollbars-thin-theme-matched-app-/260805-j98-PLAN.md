---
phase: 260805-j98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app.css
  - web/src/lib/scrollbar-theme.test.ts
  - scripts/assert-stylesheet.sh
autonomous: false
requirements: [QUICK-260805-j98]

estimate:
  tokens: 36000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every scrollable region in the app — stream pane, detail pane body, detail pane alert branch, search results (which share the stream pane's region), and the index route — renders a thin scrollbar, not the browser default width, in both Firefox and Chromium."
    - "The scrollbar thumb reads as part of the app's dark palette against both the page background surface and the card surface; it is visible enough to grab but never louder than the content."
    - "No new literal color value enters the codebase — every scrollbar color resolves, through var(), from a palette token already defined in web/src/app.css."
    - "The styling is declared once, globally; no Svelte component gains scrollbar CSS of its own."
    - "The pre-existing G-01-2 (real CSS shipped) and G-02-1 (container-scale shadowing) stylesheet guards still pass unchanged."
  artifacts:
    - "web/src/app.css — derived scrollbar custom properties in :root, plus a root-element thin-scrollbar rule and a WebKit pseudo-element fallback in @layer base"
    - "web/src/lib/scrollbar-theme.test.ts — comment-stripped source-scan guard over app.css"
    - "scripts/assert-stylesheet.sh — extended with a built-stylesheet scrollbar assertion alongside the existing G-01-2 / G-02-1 checks"
  key_links:
    - "scrollbar-width and scrollbar-color are INHERITED CSS properties — one declaration on the root element is what reaches every scroll container in the app. This inheritance is the entire delivery mechanism; if a component ever re-declares either property locally, coverage silently fragments."
    - "The derived scrollbar tokens must live in the unlayered :root block, NOT inside the @theme inline block. A key declared inside @theme joins a Tailwind v4 theme namespace, which is the exact mechanism of the recorded G-02-1 regression (named spacing keys shadowing the container scale and collapsing every max-w-* utility)."
    - "scripts/assert-stylesheet.sh runs in two modes: standalone against a globbed built stylesheet, and from scripts/e2e-smoke.sh (line 78) against a temp file fetched from the running kernel. Any assertion added must hold under both invocations."
---

<objective>
Replace the browser-default scrollbars with thin, theme-matched ones across every scrollable region of the SPA, declared once in `web/src/app.css`.

Purpose: The app is a dark, dense reading surface (`#020617` page / `#0f172a` card). A stock light-grey OS scrollbar sitting inside it is the single loudest piece of unstyled chrome left in the UI, and it appears in the two panes the user looks at constantly.

Output: Derived scrollbar tokens plus a root-element rule (standards-track properties) and a WebKit pseudo-element fallback in `app.css`; a source-scan regression test; and a scrollbar assertion added to the existing built-stylesheet guard script.

**Explicitly OUT OF SCOPE (do not implement, do not stub, do not leave a hook for):** date markers rendered in the stream scrollbar. That is Phase 6 work (UI-11). This plan is the thin, theme-matched restyle and nothing else.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@web/src/app.css
@scripts/assert-stylesheet.sh
@web/src/lib/pane-layout.test.ts
</context>

<interfaces>
**Palette tokens already defined in `web/src/app.css`** (unlayered `:root`, mirrored identically in `.dark`; the app is dark-only and `.dark` mirrors `:root` deliberately):

`--background`, `--foreground`, `--card`, `--card-foreground`, `--popover`, `--popover-foreground`, `--secondary`, `--secondary-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--primary`, `--primary-foreground`, `--ring`, `--destructive`, `--destructive-foreground`, `--success`, `--warning`, `--border`, `--input`, `--radius`.

`--muted-foreground` is the palette's mid-tone foreground (used for de-emphasised body copy). It is the only token that is legible against BOTH the page background and the card surface, which is why it is the correct base to derive a thumb color from. `--border` / `--secondary` / `--muted` are all the same near-background surface tone and would produce a thumb that vanishes against the card.

**Existing structure of `web/src/app.css`, in file order:**
1. `@import 'tailwindcss'` / `@import '@fontsource-variable/inter'`
2. `@custom-variant dark`
3. `@theme inline { ... }` — Tailwind theme namespace bindings only. Carries a standing prohibition comment (G-02-1) against re-adding named spacing keys.
4. `:root { ... }` — unlayered palette definitions, the file's home for raw values.
5. `.dark { ... }` — identical mirror of `:root`.
6. `@layer base { * {...} body {...} }`
7. `@layer components { .stream-row-surface {...} .stream-row-meta {...} }`

**Every scroll container in the app today** (found by grepping `overflow` across `web/src`):
- `web/src/routes/w/[webspace]/+page.svelte:220` — stream pane wrapper, `overflow-y-auto` (`SearchResults` renders inside this same region, so search adds no second scroll container)
- `web/src/lib/components/DetailPane.svelte:88` — loaded extracted-text block, `overflow-y-auto`
- `web/src/lib/components/DetailPane.svelte:108` — unreachable/deleted alert branch, `overflow-y-auto`
- All other `overflow-*` occurrences are `overflow-hidden` clips, not scroll regions.

None of these declares `scrollbar-width` or `scrollbar-color`, so all three inherit from the root element. **No component file is modified by this plan.**

`web/src/lib/components/ui/scroll-area/` (shadcn `ScrollArea`) exists but is imported by **zero** files. Leave it untouched; it is not on any rendered path.

**Browser support (user is on Linux; Firefox and Chromium both in scope):**
- `scrollbar-width` / `scrollbar-color`: Firefox 64+, Chromium 121+.
- `::-webkit-scrollbar` pseudo-elements: all Chromium, never Firefox.
- In Chromium 121+ the standards-track properties take precedence and the `::-webkit-scrollbar` pseudo-elements are ignored on that element. That is the intended precedence, not a conflict — the pseudo-element block exists solely as the pre-121 Chromium fallback.
- Tailwind v4's Lightning CSS pipeline targets Chrome 111+ / Firefox 128+ / Safari 16.4+, so `color-mix()` passes through to the built stylesheet rather than being downlevelled.

**Test harness facts:**
- vitest runs with `environment: 'node'`, `include: ['src/**/*.test.ts']` (`web/vite.config.ts`). There is no jsdom / component-mount harness, and jsdom performs no layout or scrollbar rendering, so it would not help here regardless.
- The established pattern for asserting on non-TS source is a Node source-scan test: `web/src/lib/pane-layout.test.ts` and `web/src/lib/components/date-format.test.ts` both `readFileSync` a file off disk (path resolved from `import.meta.url` via `fileURLToPath`/`dirname`/`join`) and assert on its text.
- Node built-in types come from the hand-written `web/src/lib/node-builtins.d.ts` ambient declarations — `readFileSync`, `dirname`, `join`, `fileURLToPath` are already declared. **Add no new dependency.** If a needed built-in is not declared there, extend that file rather than installing `@types/node`.
- Gates: `npm --prefix web run test` (vitest), `npm --prefix web run check` (svelte-check, must stay at 0 errors), `npm --prefix web run build` (writes the built SPA into `kernel/webui/build/`).

**`scripts/assert-stylesheet.sh` contract:** takes an optional CSS file argument; with no argument it globs `kernel/webui/build/_app/immutable/assets/*.css` and picks the first file containing `#020617`. It currently runs four checks (non-empty file; `#020617` present; three named container widths resolve to their rem values; no named `max-w-*` utility resolves to a raw pixel value) and prints an `OK:` line on success. `scripts/e2e-smoke.sh` line 78 invokes it against a temp file fetched from the live kernel. Every existing check must keep working verbatim.
</interfaces>

<source_audit>
| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Thin, theme-matched scrollbars app-wide | COVERED — Task 1 |
| REQ | QUICK-260805-j98 | COVERED — Tasks 1-3 |
| RESEARCH | *(no research phase for quick tasks)* | n/a |
| CONTEXT | Derive colors from existing `app.css` tokens; never hardcode new hex | COVERED — Task 1 action + Task 1 guard assertion |
| CONTEXT | Do not introduce theme keys that shadow Tailwind namespaces (G-02-1) | COVERED — Task 1 action + Task 1 `@theme`-region guard assertion |
| CONTEXT | Prefer standards-track `scrollbar-width: thin` + `scrollbar-color`, WebKit fallback for the browser mix | COVERED — Task 1 |
| CONTEXT | Cover Firefox and Chromium (Linux) | COVERED — Task 1 (both code paths) + Task 3 (both browsers exercised) |
| CONTEXT | Applied consistently to every scrollable region (stream, detail, search results, any overflow container) | COVERED — Task 1 via root-element inheritance; regions enumerated in `<interfaces>` and walked in Task 3 |
| CONTEXT | Global CSS in `app.css` is the natural home — avoid per-component duplication | COVERED — Task 1 modifies only `app.css`; no component file is in `files_modified` |
| CONTEXT | Check whether the built-stylesheet guard needs a scrollbar assertion; keep existing guards passing | COVERED — Task 2 |
| CONTEXT | Verification: test / check / build gates plus a short documented manual browser check | COVERED — Task 1 + Task 2 automated gates, Task 3 manual walkthrough |
| CONTEXT | Date markers in the stream scrollbar are OUT OF SCOPE (Phase 6, UI-11) | EXCLUDED — stated as a non-goal in `<objective>`; no task touches it |
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thin theme-matched scrollbars, declared once at the root and inherited everywhere</name>
  <files>web/src/lib/scrollbar-theme.test.ts, web/src/app.css</files>
  <behavior>
    `web/src/lib/scrollbar-theme.test.ts` reads `web/src/app.css` off disk, strips CSS comments from it FIRST, and asserts only against the comment-stripped text (never the raw file). Assertions:

    - Test 1 (loud-failure guard): the comment-stripped source is non-empty — a wrong path resolution must fail here rather than make every later assertion vacuously pass over an empty string.
    - Test 2: the comment-stripped source declares a thin scrollbar width and declares a scrollbar color pair.
    - Test 3: the scrollbar color declaration's value is composed exclusively of `var(--...)` references — it contains no hex literal and no bare color keyword other than what a referenced custom property resolves to.
    - Test 4: each derived scrollbar custom-property definition references at least one `var(--...)` and contains no six-digit hex literal, so the scrollbar palette can never drift from the app palette.
    - Test 5 (region-scoped negative gate, G-02-1 discipline): the `@theme inline { ... }` block — sliced out of the COMMENT-STRIPPED source, from the block opener to the first following closing brace — contains no scrollbar custom-property declaration. Derived scrollbar tokens belong in the unlayered palette block, not in a Tailwind theme namespace.
    - Test 6: the WebKit pseudo-element fallback rules are present, so pre-121 Chromium is covered.

    Comment stripping is load-bearing for Tests 2-6 in both directions: `app.css` is a heavily commented file, so an un-stripped positive grep could be satisfied by prose alone, and an un-stripped negative grep in Test 5 could be tripped by prose alone.

    RED state: run this test against the UNMODIFIED `app.css` before editing it. Tests 2, 3, 4, 5 and 6 must be reported — Test 5 as a pass (vacuously, nothing declared yet) and Tests 2, 3, 4, 6 as failures. Capture that output; four failing assertions against unmodified `app.css` is the RED signal.
  </behavior>
  <action>
Work in two moves within this one task: write the guard test, observe it fail, then make it pass by editing `app.css`. Do not edit `app.css` before capturing the RED run.

**Move 1 — `web/src/lib/scrollbar-theme.test.ts`.**

Follow the exact shape of `web/src/lib/pane-layout.test.ts`: vitest `describe`/`it`, `readFileSync` from `node:fs`, `dirname`/`join` from `node:path`, `fileURLToPath` from `node:url`, directory resolved from `import.meta.url`. Resolve the stylesheet as the sibling path `../app.css` relative to the test file's directory and read it once at module scope.

Immediately derive a second module-scope constant holding the source with every CSS comment removed (a global, non-greedy regex over the comment open/close delimiters, replaced with a single space so adjacent declarations cannot fuse). Every assertion below runs against that stripped constant. Write a short comment on the stripped constant recording WHY: this file is heavily commented by house style, so an assertion made against raw text could be satisfied — or tripped — by prose that has no effect on any rendered pixel.

Write a small helper that slices the theme block out of the stripped source: find the theme-block opener, find the first closing brace after it, return the text between. Assert inside the helper that the opener was actually found, so a future rename of that block fails loudly instead of returning an empty string that makes the negative gate vacuous.

Implement Tests 1-6 per the behavior block. Use tolerant whitespace in every regex (the file is tab-indented and a future formatter pass must not break the guard). For the hex check, match a `#` followed by six hex digits. Give every `expect` a failure message that states the invariant in plain language, matching the explanatory-message style used throughout `pane-layout.test.ts` — in particular, the theme-block message must name the G-02-1 mechanism (a key inside that block joins a Tailwind theme namespace and shadows the same-named built-in scale).

Add no dependency. If a Node built-in you need is missing from `web/src/lib/node-builtins.d.ts`, extend that file.

**Move 2 — `web/src/app.css`.**

Run the new test first and record the RED output, then make these three additions.

(a) In the unlayered `:root` block, immediately after the `--input` declaration, add three derived custom properties for the scrollbar: a resting thumb color, a hover thumb color, and a track color.
  - Resting thumb: a `color-mix()` in the sRGB color space of `var(--muted-foreground)` at approximately 35% against `transparent`.
  - Hover thumb: the same mix at approximately 60%.
  - Track: the keyword `transparent`.

  `--muted-foreground` is the correct base because it is the one palette token legible against both `--background` and `--card`; deriving through `color-mix()` rather than picking a new value is what keeps the constraint "no new literal color" true. A translucent track is deliberate, not lazy: the stream pane sits on the page background and the detail pane's regions sit against the card surface, so a track that lets the underlying surface show through matches both without either being hardcoded.

  Add a comment above the three properties explaining that these are DERIVED tokens computed from palette tokens, which is why — unlike every palette token in this file — they are declared once here and deliberately NOT mirrored into the `.dark` block: `.dark` sets the palette tokens to values identical to `:root`, so a mirror would be pure duplication. Record the corollary in that same comment: if `.dark` ever diverges from `:root`, these three must be mirrored there too.

(b) In `@layer base`, after the existing `body` rule, add a rule on the root element setting `scrollbar-width` to `thin` and `scrollbar-color` to the resting-thumb token followed by the track token. Comment that both properties are inherited, so this single declaration is what reaches every scroll container in the app, and that no component may re-declare them without fragmenting coverage.

(c) In the same `@layer base`, after (b), add the pre-121-Chromium fallback as WebKit scrollbar pseudo-element rules: the scrollbar itself at a 10px width and 10px height; the track filled with the track token; the thumb filled with the resting-thumb token, fully rounded, with a 2px transparent border and `background-clip` set to the padding box (the transparent-border-plus-padding-box pairing is what makes a 10px-wide track render a ~6px visual thumb with breathing room, matching the standards-track thin appearance rather than looking chunky beside it); the thumb's hover state filled with the hover-thumb token; and the corner filled with the track token. Comment that Chromium 121+ ignores this entire block because the properties in (b) take precedence there — that precedence is intended, and this block exists only for older Chromium.

Do not add `scrollbar-gutter`, do not set any explicit width on the root scrollbar beyond the fallback block, and do not touch the `@theme inline` block, the `.dark` block, `@layer components`, or any `.svelte` file. Do not add anything related to stream date markers — that is Phase 6 (UI-11) and is out of scope here.
  </action>
  <verify>
    <automated>npm --prefix web run test &amp;&amp; npm --prefix web run check &amp;&amp; npm --prefix web run build</automated>
  </verify>
  <done>`web/src/lib/scrollbar-theme.test.ts` fails with four failing assertions against the unmodified `app.css` (RED output captured in the summary) and passes after the `app.css` edit, alongside every pre-existing vitest suite (`format`, `date-format`, `detail-body`, `sources`, `staleness`, `pane-layout`). `svelte-check` reports 0 errors. The production build succeeds and writes a stylesheet into `kernel/webui/build/_app/immutable/assets/`. No npm dependency was added, and `git diff --name-only` shows exactly two changed files.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the built-stylesheet guard so the scrollbar styling cannot silently disappear from the shipped CSS</name>
  <files>scripts/assert-stylesheet.sh</files>
  <action>
Add a fifth check to `scripts/assert-stylesheet.sh`, after the existing G-02-1 collapsed-utility check and before the final `OK:` echo. Change nothing about checks 1-4 — they are the G-01-2 and G-02-1 recurrence guards and must not be weakened or reordered.

The new check asserts that the BUILT stylesheet actually ships the scrollbar styling — that it survived the Tailwind/Lightning CSS pipeline and reached the browser, which is a different claim from Task 1's claim that it exists in source. Assert two things against `$CSS_FILE`:
  1. a thin scrollbar width declaration is present;
  2. the WebKit thumb pseudo-element rule is present.

Match with `grep -qE` and a whitespace-tolerant pattern for the declaration — the built file is minified, so there is no guaranteed space after the colon, while the temp file that `e2e-smoke.sh` fetches is whatever the kernel serves. On failure, emit a `FAIL:` message on stderr naming the file and explaining what a miss means (the scrollbar rules were authored in source but did not reach the shipped stylesheet — for example because they landed in a layer or block the build dropped), then exit 1, matching the style of the existing failure messages.

These are positive greps over a minified, comment-free artifact, so no comment-stripping or region scoping is needed here — unlike the source-level guard in Task 1.

Update the file's header comment block, which currently documents the two gaps guarded (G-01-2, G-02-1), to also record this third assertion and why it exists.

Then re-run the script both ways to prove both invocations still work: standalone against the globbed built stylesheet, and end-to-end through `scripts/e2e-smoke.sh` (which calls this script at line 78 against a temp file fetched from the running kernel).
  </action>
  <verify>
    <automated>./scripts/assert-stylesheet.sh &amp;&amp; make smoke</automated>
  </verify>
  <done>`./scripts/assert-stylesheet.sh` prints its `OK:` line against the built stylesheet with all five checks passing; `make smoke` completes green, proving the script still satisfies its `e2e-smoke.sh` caller against a kernel-served stylesheet. Temporarily reverting the `app.css` scrollbar rules makes the script exit 1 with the new `FAIL:` message (confirm this, then restore).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the scrollbars in Firefox and Chromium</name>
  <precondition>Both Firefox and a Chromium-family browser are installed and launchable on this machine — the two code paths added in Task 1 (standards-track properties vs. WebKit pseudo-elements) can only be distinguished by exercising both engines.</precondition>
  <what-built>Scrollbars app-wide are now thin and drawn from the app's own palette, declared once on the root element in `web/src/app.css` and reaching every scroll region by CSS inheritance. Automated guards prove the rules exist in source and survive into the shipped stylesheet, but no automated gate can prove a scrollbar actually renders: vitest runs in Node, and jsdom performs no layout and paints no scrollbars. Looking at it is the only way to verify it.</what-built>
  <how-to-verify>
1. Run `make dev` from the repo root (starts the kernel on 127.0.0.1:7777 and opens the Vite dev server).
2. Open a webspace route (`/w/<name>`) with enough items that the stream pane scrolls.
3. **Firefox — stream pane:** scroll the stream list.
   - Expect: a thin scrollbar, noticeably narrower than a stock one, in a muted slate tone that sits quietly against the near-black page background. The track should not read as a bright grey channel — the underlying background should show through it.
4. **Firefox — detail pane:** click an item with long extracted text (an email or a Signal transcript works well) and scroll the detail pane.
   - Expect: the same thin scrollbar, still legible against the card surface (which is lighter than the page background). If the thumb disappears against the card, say so — that means the thumb is derived from too dark a token.
   - Also confirm scrolling one pane still does not move the other.
5. **Firefox — search results:** type a query into the search box so results replace the stream.
   - Expect: identical scrollbar treatment (this region shares the stream pane's scroll container, so any difference here is a real bug).
6. **Chromium — repeat steps 3, 4 and 5.**
   - Expect: visually equivalent to Firefox. Chromium 121+ uses the same standards-track properties Firefox does; older Chromium falls back to the pseudo-element rules and should look very close, perhaps a hair wider.
7. **Hover feedback (Chromium only, and only if it fell back to the pseudo-element path):** hover the thumb.
   - Expect: it brightens. On the standards-track path there is no hover state — that is expected, not a defect.
8. **No layout shift:** with the detail pane open, switch between an item whose content scrolls and one whose content does not.
   - Expect: the pane's content does not jump horizontally as the scrollbar appears and disappears. A small shift is a known trade-off of not using `scrollbar-gutter` (deliberately out of scope) — report it if it is bad enough to be distracting, and it can be a follow-up.
9. **Known non-goals — do not fail the check on these:** the internal scrollbars of an embedded PDF viewer are drawn by the browser's own PDF plugin and cannot be styled from page CSS; and there are no date markers in the stream scrollbar, which is Phase 6 (UI-11) work.
  </how-to-verify>
  <resume-signal>Type "approved" if the scrollbars are thin and theme-matched in both browsers across all three regions, or describe which step misbehaved, in which browser, and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none newly crossed)* | This change adds presentational CSS to an already-rendered local-only route, plus one test file and one assertion in an existing guard script. It introduces no new input, no network call, no data path, and no dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260805-j98-01 | Denial of Service | `web/src/app.css` global scrollbar rule | low | mitigate | A scrollbar restyle that renders the thumb indistinguishable from its track — or zero-width — removes the only affordance for navigating a long stream or a long transcript, making the primary view effectively unusable. Mitigated by deriving the thumb from `--muted-foreground` (a foreground-tier token chosen precisely because it is legible against both surfaces) rather than a border-tier token, by keeping the track transparent so contrast is against the real surface, and by the two-browser three-region human-verify walkthrough (Task 3 steps 3-6). |
| T-260805-j98-02 | Tampering | `scripts/assert-stylesheet.sh` (existing G-01-2 / G-02-1 recurrence guards) | medium | mitigate | Editing a guard script is the classic way a guard gets silently weakened — a reordered or loosened check would leave the SPA free to regress to the exact failures those guards were written for. Mitigated by Task 2 forbidding any change to checks 1-4, by re-running the script under both of its invocation modes (standalone and via `make smoke`), and by the deliberate-revert test in Task 2's acceptance criteria proving the new check actually fires. |
| T-260805-j98-03 | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. Task 1 explicitly forbids adding a dependency (the source-scan test runs on the existing vitest setup plus the hand-written `web/src/lib/node-builtins.d.ts` ambient declarations), so the supply-chain surface is unchanged and no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npm --prefix web run test` — full vitest suite green, including the new `scrollbar-theme.test.ts` and the pre-existing `format`, `date-format`, `detail-body`, `sources`, `staleness`, and `pane-layout` suites.
- `npm --prefix web run check` — svelte-check at 0 errors (the standing project bar).
- `npm --prefix web run build` — production build succeeds and emits a stylesheet carrying the scrollbar rules.
- `./scripts/assert-stylesheet.sh` — all five checks pass against the built stylesheet, with checks 1-4 (G-01-2, G-02-1) unmodified.
- `make smoke` — `e2e-smoke.sh` end-to-end, proving the extended guard still satisfies its caller against a kernel-served stylesheet.
- Deliberate-revert probe — with the `app.css` scrollbar rules removed, `assert-stylesheet.sh` exits 1 with the new failure message (then restored).
- Human-verify checkpoint — nine-step manual walkthrough across Firefox and Chromium, covering the stream pane, detail pane, search results, hover feedback, layout shift, and the two named non-goals.

</verification>

<success_criteria>
- Every scroll region in the app renders a thin, palette-matched scrollbar in both Firefox and Chromium, delivered by a single root-element declaration and CSS inheritance — no component file changed.
- Every scrollbar color resolves through `var()` from a palette token already in `app.css`; the codebase gains no new literal color value.
- No scrollbar key is declared inside the `@theme inline` block, and the G-02-1 container-scale guard still passes — the recorded shadowing regression cannot recur through this change.
- `scrollbar-theme.test.ts` fails against the pre-change `app.css` and passes after, asserts only against comment-stripped text so no prose can satisfy or trip it, and scopes its negative theme-block gate to that block alone.
- `assert-stylesheet.sh` keeps its four existing checks byte-for-byte in behavior, gains a scrollbar assertion that demonstrably fires when the rules are absent, and passes under both of its invocation modes.
- Stream date markers are absent — no partial implementation, no placeholder, no hook. That work belongs to Phase 6 (UI-11).
</success_criteria>

<output>
Create `.planning/quick/260805-j98-style-scrollbars-thin-theme-matched-app-/260805-j98-SUMMARY.md` when done
</output>
